# REVIEW — day-one assumptions that still steer a 50-bot army (09-20 12:0xZ)

Owner 09-20: 「そういう初期の頃必要だったモジュールとか更新するべきなんだよな、今もこれからも」. Systematic pass over the board
(87 jobs + `settings`), every job handler and numeric constant in `army_jobs.js` / `army.js` / `feed.js` / `craft.js` / `iron_core.js` /
`dispatcher.js` / `stock.js`, and the three daemon prompts. Every "today" column is MEASURED (`stock`, `targets`, `field`, `gemba`,
`deaths 60`, `mine`, `base-audit`, `hb/*.json`) at 11:45-12:05Z, not assumed.

Facts the whole table rests on: 50 bots · food 9139/448 (20x) · wool 8601/150 (57x) · feather 384/64 · leather 208/64 · sugar_cane 3020 ·
diamond 353 · iron_ingot 66 · fuel 1596/224 · log 2878 · 19 of 50 bots on a **wooden or stone pickaxe or none**, 41 of 50 on a **stone
shovel** (the army's main work is 10 000 cells of earthmoving) · 0 books / 0 bookshelves / 0 enchanting table anywhere · 44 % of bot time
produced nothing in the last hour (base-audit) · deaths/1h 16-21 (9 skeleton, 5 lava, 3 zombie, 3 fire) · mine y16/32/-16 EXHAUSTED.

## Ranked by what it costs TODAY (bot-hours first, then deaths, then idle stock)

| # | where (file:function / job id) | the early-game assumption | today's fact (measured) | change proposed | owner | risk |
|---|---|---|---|---|---|---|
| 1 | board: `farm_1/3/6`, `hunt_spawn`, `herd_sheep/cows/chickens`, `cane_farm`, `fish_lake` → `minBots` | day one food was 19/448 and wool 0: every food/animal job had a **floor of ≥1 bot** so the army could not starve | 10-12 bots held permanently at `deficit 0.00`; `gemba`: 9 farmers "waiting for growth", herders declining "nobody ready to breed" x9, cane at 3020 with no consumer | **APPLIED:** `minBots:0` on all of them — the demand formula then staffs 0 at deficit 0 and re-staffs by itself the minute food/wool/leather drops under target. No job paused, no head-count guessed | board (done) | low |
| 2 | `army.js:kitUp` tool loop (`['stone_','iron_','diamond_','golden_','wooden_']`, only when the bot carries NONE of that kind) | day one there was no iron and no diamond: "the plainest tool the depot has" was the only honest rule, and any tool beat bare hands | 9 bots on WOODEN picks, 8 on stone, 2 with none; 41 of 50 on stone shovels — while 353 diamonds + 66 ingots lie in the depot. A wooden pick is ~4x slower on stone than diamond and yields nothing from iron ore | order the candidates by `C.TIER_RANK` **descending**, and make it an UPGRADE rule like armour: reuse `pickFor(toolOf,'kind',k)` with the same fair-share test (`stock[n] > others-who-have-worse`), not "only if the slot is empty" | top model (`army.js`) | med — fair-share test must stay, else the first 5 bots take every diamond tool |
| 3 | `army_jobs.js:CRAFTED` (l.661) `{torch:64, planks:64, stick:32, book:3, bookshelf:3, enchanting_table:1, stone_shovel:8, stone_axe:8, stone_pickaxe:8}` | the quartermaster was born in the stone age: the only tools it can make are stone ones, batches sized for ~3 bots | it is the ONLY automatic tool producer and it can never make an iron/diamond tool, a bucket, shears, a bow, an arrow or a shield. Depot pickaxes were **0** of any kind at 11:50Z | add `iron_pickaxe:4, diamond_pickaxe:4, diamond_shovel:4, diamond_axe:4, bucket:2, shears:4, bow:2, arrow:32, shield:2`; raise `torch:64→256, planks:64→256, stick:32→128` (x50 rule) | terrain eng. (`army_jobs.js`) | low |
| 4 | `settings.targets` — the day-one shopping list | sized for 3 bots with stone tools, no beds, no books: `planks 224, stick 128, torch 224, cobblestone 1728, dirt 768` and the only tool targets are `stone_pickaxe 16 / stone_axe 20 / stone_shovel 40` | the ravine fill alone still needs ≥10 000 filler blocks; 39 bots declined "build: no filler" / "no dirt" at 11:50Z with depot cobblestone 54 and depot dirt 0 | **APPLIED:** cobblestone 1728→6144, dirt 768→2048, planks→512, stick→256, torch→512, +`book 48`, +`bookshelf 15`, +`sugar_cane 512`. mine_iron went 11→13 bots within a tick. **Still stale:** the three stone-tool targets (replace with iron/diamond once row 2 ships), `string 40` and `iron_ingot 224` show "NOBODY produces" | board (done) / top model | low |
| 5 | `army_jobs.js` tidy sponge: 30-min per-tile memo + "in reach" radius | day one the base was a 50-block clearing: a tile tidied 30 min ago really had nothing left | 39 bots declined `tidy: the audit lists no open work in reach and every tile was tidied in the last 30 min` **x61 one minute after** a fresh `base-audit` listed 274 off-level columns in 34 hole + 22 bump clusters. The base box is now 178x190. This is the muster↔sponge ping-pong the owner named | when `auditWork()` is fresh, IGNORE the tile memo and search the whole `params.box`, not a reach radius; a claimed audit unit is the only exclusion | terrain eng. (`army_jobs.js`) | low |
| 6 | `army_jobs.js:farm` l.1765 `farm:waiting for growth` → 90 s standing loop | day one a farmer standing at a growing field was the best use of that bot (food 19/448) | measured by `base-audit --idle`: 44 % of bot time without output; farmers stood with food 20x over target | when `stock.have('food') >= targets.food`, `return muster(...)` / decline instead of the 90 s wait — the dispatcher then re-uses the bot the same tick | terrain eng. | low |
| 7 | `iron_core.js:524` `C.craft(bot,'stone_pickaxe',1,tbl)` | a miner that broke its pick underground could only ever make a stone one | the depot now holds 15 diamond picks (banked by this review's plan); a stone pick cannot mine obsidian (the Nether gate) and is 4x slower in deepslate | try `A.obtain(bot,'diamond_pickaxe'/'iron_pickaxe',1)` from the depot first; craft stone only as the last resort | top model (`iron_core.js`) | low |
| 8 | `dispatcher.js:demandOf` — "no target for a produced key → deficit 1 while we own NONE, else **0.5**" | day one every new producer deserved half a squad until somebody set a target | `cane_farm` held 2 bots for hours on 3020 idle sugar cane purely because nobody had set `targets.sugar_cane`; `cane_lake` 2 more, 100-180 blocks out in unticked chunks | untargeted producer → `want = min(1, minBots+1)` plus the existing "no target" line in BOARD.md. (Instance fixed on the board with `sugar_cane 512` + pausing `cane_lake`) | churn eng. (`dispatcher.js`) | low |
| 9 | board: guard POSTS — `guard_night` (12 bots, the NIGHT sponge), `guard_muster` (6), `guard_treefarm` (4), `guard_infill_east` (3) | day one bots were unarmed and nights were real: a post of armed bots escorting workers was the only defence | 41 of 50 carry a diamond sword and `core.enabled:["combat"]` is on; `nightSkip:true` makes nights seconds long. The 16-21 deaths/h are skeletons+lava **inside the ravine**, 40 blocks below any post — `light_ravine` (2 bots, torch x20/min) is what actually works | keep `guard_bed` only; delete the other four from the board (they are paused, so they cost nothing today, but they are the template operators copy). Escort = LIGHT + armed workers, not a post | board / operator | low |
| 10 | `craft.js:craftOnTable` — unstackable batches | never hit on day one: nobody crafted more than one tool at a time | measured again today: `armoury_diamond_tools` reported `0/2 crafted (ingredients ran out?)` on **8 of 22** craft steps while carrying 19 diamonds and 60+ sticks; 16 of 31 planned tools were lost to it. Every toolsmith plan since 09-20 works around it with `n:2` | reproduce with `diamond_shovel n:4` at `settings.craftTable` and fix the result-slot/empty-slot path; then drop the "2 per step" workaround from every toolsmith plan | top model (`craft.js`) | med |
| 11 | `dispatcher.js` `maxFronts` (settings 10, code default 3) | day one 3 bots could hold 3 work sites | 50 bots, 87 jobs, 8 fronts already in use. A job on an 11th front gets 0 bots and says nothing | `maxFronts = Math.max(10, Math.ceil(online/4))`, or drop the cap and let head-counts do the limiting | churn eng. | low |
| 12 | `army.js:larderFull()` l.524 — sprinting is gated on `have('food') >= 256` | 256 food was a fortune on day one | always true today, so it costs nothing — but the day food dips under 256 the WHOLE army silently stops sprinting (measured value of sprint: 3.96 → 5.6 m/s) | `>= Math.min(256, (targets.food||256)/2)`, or a flat floor of 64 | top model | low |
| 13 | board: `hunt_spawn.params.leave: 0` (plan text says "leave 2 of every kind alive for the pens") | day one: first food, kill everything in sight | the pens now breed; wiping a wild herd removes the `herd_*` jobs' lure stock | **APPLIED:** `leave: 2` | board (done) | low |
| 14 | `ops/foreman.sh` / `ops/operator.sh` prompts | written for 30 bots | foreman: "docs/GOALS.md has the doctrine, the **x30** SCALE RULE", "tiny things that should be **x30**"; operator: "Owner's orders: all **30** bots working in big squads" — GOALS.md has said x50 since 09-19 | s/x30/x50/ and s/all 30 bots/all 50 bots/ in both prompts | top model (`ops/`) | low |
| 15 | docs: GOALS backlog #4 "enchanting is ready (table + 15 shelves)"; CLAUDE.md header "an enchanting table" | P4 was declared closed | measured: **0 book, 0 paper, 0 bookshelf, 0 enchanting_table** in any chest, pocket or on the board — while 3020 sugar cane and 208 leather lay idle. The quartermaster could always have made them; it never did because `targets` had no `book`/`bookshelf` key | **APPLIED** (row 4): the quartermaster crafted its first 5 books within 90 s of the targets going in. Correct the two doc lines | top model (docs) | low |
| 16 | board note on `mine_iron`: "the diamond level comes with `mine level -54` when iron picks exist" | day one goal: diamonds | 353 diamonds idle, iron at 50 % deficit, levels y16/32/-16 EXHAUSTED, 6 open branches at y0 and 6 at y-32 (deepslate = cobblestone group = the filler the earthworks are starving for) | do NOT open -54. Keep the squad on y0/-32 while cobblestone/iron are the deficits; rewrite the note | board / operator | low |

## What already got fixed today (do not re-file)
Guard escorts → armed bots (`core/combat.js` + weapons in the kit) · tools part of the kit at all · 398 diamonds/303 leather → swords and
armour · `BED_DUE_MS` 3rd-dusk walk → once per 6 h · carry limit 192 → `(freeSlots-8)*64` capped 1024 · churn 1203 → 6-159 assigns/h.

## Board changes applied by this review (each verified in `targets` / `status.json` / `events`)
1. `minBots:0` on farm_1/2/3/4/5/6/7/farm_bank, hunt_spawn (maxBots 6→4), herd_sheep/cows/chickens, cane_farm, fish_lake — 10-12 bots freed, all self-restoring by deficit.
2. targets: `+book 48`, `+bookshelf 15`, `+sugar_cane 512`, `planks 224→512`, `stick 128→256`, `torch 224→512`, `cobblestone 1728→6144`, `dirt 768→2048`. → mine_iron 11→13 bots, first 5 books crafted.
3. `cane_lake` paused (3020 cane in stock, wild stands 100-180 blocks out in unticked chunks: `cane_wild_empty` x3, `travel_fail`).
4. New job `armoury_diamond_tools` (steps, 2 per craft step): pass 1 banked **15 diamond pickaxes** into the tools chests (depot held 0 pickaxes of any kind); pass 2 (rev 1) runs diamond shovels + axes.
5. `hunt_spawn.params.leave` 0 → 2.
6. `node ops/base-audit.js` re-run: the tidy sponge's work list refilled (274 off-level columns, 90 strays, 1 float cluster) — see row 5 for why the sponge still refuses it.

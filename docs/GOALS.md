# GOALS — what the army is for, what to fix next, what was changed
Owner's long-term goal: develop this survival world endlessly with 50 bots — iron → diamonds → Nether → Ender Dragon, plus
mega builds (castle/tenshu). Legit survival only. WORLD 2 (new seed, 09-19): humans are SPECTATORS — they watch and chat, nothing else;
no PvP, no looting, no raids. The only enemies are mobs, terrain and our own mistakes. World 1's history: `attic/world1/` (local only, see its README).

## Scale rule (owner, 09-19): this is a 50-player survival (30 until 09-19, owner: "50体でいこう") — size EVERYTHING x50
A solo player's base has 5 chests, a 9x9 field and one furnace; ours needs ~200 containers, fields of 1000+ cells, furnace banks, squads of 6-14 on
every work, roads instead of paths. When a number looks fine for one player, multiply it by 50 before you put it on the board.

## Doctrine — optimise the army's next 1000 trips, not this bot's current trip
The owner's diagnosis of the first attempt: the AI could not give the *ordinary good sense* of a Minecraft player. Each bot did what
was cheapest for itself right now (dig through, pillar over, leave the hole, farm on ice) and the sum was a wrecked, hostile base.
A good player — and a kind one — invests in the shared world, because everyone, including itself tomorrow, pays for every flaw again
and again. Judge every order and every line of bot code by these five questions:
1. **Does it make the place better or worse for the next one who comes by?** Holes get filled (yours, a creeper's, anybody's), scaffolds
   removed, trees felled whole and replanted, gates closed, paths lit. Never leave a trap to save ten seconds.
2. **Will this obstacle be met again?** Then fix it ONCE, properly, for everybody: a reusable 2-wide stair where there is a cliff, a bridge over
   the gap, a road on the daily route, a chest + table at the work site. Cost of the fix vs (trips × detour/risk) — commons win almost always.
   Never a private shortcut: movement is read-only, and what a bot cannot pass it reports (`no_route`, `terrain_debt.jsonl`).
3. **Are we fighting the land instead of choosing it?** The world is huge and free. Farm where water is liquid and the ground is flat and warm;
   keep animals where grass grows; mine where the ore is; build where it is beautiful. Scout first (`scout` job → `armyctl.js sites`), then
   settle there — an outpost with its own chest, bed and farm, joined to the base by a road. Walking 300 blocks once beats freezing for days.
4. **Is someone already providing this?** One quartermaster cooks, crafts tools and keeps the chests sorted so 29 others don't each open every
   chest. One stair, one mine entrance, one chest index. Specialise and share; don't duplicate.
5. **Is everybody working? (owner: 「全員動かして、数の暴力」)** Thirty bots are the army's one real advantage — use it. Think in squads of
   6-12 on BIG works (a huge branch mine, a huge farm, a lumber camp, filling the crater, roads), not in single errands. A bot standing at
   muster by day is a planning failure: the only good reasons are unfit (hp/food), night on the surface, or a missing tool — and a missing
   tool is itself a job (toolsmith). Big does not mean messy: every squad job still has a zone, a plan and read-only movement.

## Roadmap (owner, 09-19): mid-term goal = **kill the Ender Dragon**; there is NO final goal — after it, keep developing like any long-lived survival world
Owner's aside 09-20 (NOT decided): the true long-term goal may become **all advancements**; he also wants **giant builds**. Design accordingly: keep `advancements` countable (a census of what is done), keep blueprints/build squads scalable to mega-structures.
Work the FIRST open phase with most of the army; later phases may be prepared (scouting, stockpiling, lab tests) but never at its expense.
Numbers are sized for 50 bots. Live numbers: `REPORT.md`, `ops/status.sh`, chest index `bots/army/chests.json`.

| # | phase | exit criteria (all must hold) | main squads / jobs |
|---|---|---|---|
| P1 | **Food + night** | ≥64 cooked food in stock and inflow ≥1/min · 0 bots at food ≤6 · first bed placed + a sleeper skips nights · base lit on the torch grid | fishing + dock chest + haul, mega farm (wheat → bread), sheep/wool trip, toolsmith, public works lighting |
| P2 | **Iron age** | ≥120 iron ingots banked · every bot: iron pickaxe or sword + shield · ≥6 buckets · iron armour for the 10 front-line bots | big branch mine (y 15 for iron, coal on the way), smelting plan, toolsmith (iron) |
| P3 | **Sustainable base** | bread/meat surplus (≥256 cooked food) · animal pens (sheep, cows, chickens) bred to ≥10 each · beds for all 30 in a dorm · storage sorted, roads + stairs on every daily route · an outpost on kind land (liquid water, warm biome) joined by a road | farm expansion, breeding (wheat/seeds), lumber camp with replanting, outpost squad, public works |
| P4 | **Diamonds + enchanting** | ≥40 diamonds · diamond pickaxes for 5 miners · enchanting table + 15 bookshelves (sugar cane + leather) · ≥10 obsidian | deep branch mine at y −59 (iron picks required), lava + water bucket obsidian, cow leather, sugar cane at the outpost |
| P5 | **Nether** | portal lit at base · safe cobble-walled hub on the Nether side · ≥12 blaze rods · nether wart + brewing stand (fire resistance helps) | expedition squad of 8-10 in iron/diamond gear with shields + bows; lab skills `exp_portal`, `exp_nether`, `exp_blaze` get absorbed into army jobs |
| P6 | **Eyes of Ender** | ≥16 ender pearls (endermen hunt at night in the open / warped forest, or piglin barter with gold) → ≥14 eyes of ender · stronghold located and its portal room secured with a chest + beds nearby in the overworld | pearl hunters, gold mining for barter, `exp_pearls`, `exp_stronghold` |
| P7 | **Ender Dragon** ★ mid-term goal | dragon dead · egg/XP collected · no gear left behind | strike force of ≥12: diamond/iron armour, bows + ≥3 stacks of arrows each, water buckets, blocks for pillaring, beds/food; destroy crystals first; `exp_dragon` lab-tested before the real run |
| P8+ | **After the dragon — open-ended** | pick what a thriving long-lived world does next, always several in parallel: End cities → elytra + shulker boxes · villager trading hall (mending, enchanted books) · wither → beacons · automatic farms (iron golem, crops, mob XP) · netherite gear · the mega builds (castle/tenshu, torii, shrine) and a rail/ice-road network between outposts · map walls, monuments to every bot that fell | everything, at scale |

Standing priorities inside every phase: keep all 50 bots working in big squads (doctrine Q5), bank before risk (keep_inventory is OFF), and
fix the commons first (doctrine Q1-2). A phase is closed by writing its date into the Log below.

## Standing rules (short version of the old DIRECTIVE/BASICS — every job handler obeys them)
- **Level first (owner 09-19).** No construction on raw terrain: the `build` job pads every blueprint by itself (footprint+1 levelled, 4 headroom; `params.pad:false` / terrain-spanning blueprints opt out); fields and yards get a `level` build before use. One site = one height.
- **Routes are reversible.** Surface trips step down max 1 block (a 3-drop only towards base on the retry) — a route a bot cannot walk back is a trap.
1. **Output over motion.** A bot that cannot do its job (no tool, no food, 1 hp) stands at muster; it does not wander or circle chests.
2. **Squads, not errands.** Up to `maxFronts` (6) work sites at once, each staffed by a squad at the same site; nobody wanders alone.
3. **One implementation of each basic** (travel, chest I/O, eat, craft, smelt) — in `lib/army.js` & friends. No copies.
4. **Base logistics belong to the quartermaster job**, not to every worker: workers bank raw output and draw kit by the chest index.
5. **Movement is read-only; world edits need a plan + zone** (docs/DEV.md §4, docs/WORLD.md zones).
6. **keep_inventory is OFF**: bank valuables at every base visit; night work only underground or inside the lit base.
7. Never hand-dig stone (no drop, 7 s/block); never dig straight down; never x-ray (mine only ore you exposed).
8. In-game chat is never an order (humans are spectators; the chat daemon only talks).

## Backlog = the top model's WANT-TO-DO list (owner 09-20: "やりたいことリストも作って") — ranked; take the top one, prove the cause, ONE change, verify in the field, log it below
Rows 1-6 of the bootstrap backlog (site, depot, food, night, tools + mine, x50 infrastructure) are DONE (see the Log). What I want next, and why:
| # | want | why / first step |
|---|---|---|
| 1 | **50 bots that WORK: idle share < 20 %, < 6 job changes per bot-hour** | measured 09-20: 50-63 % of bot time produces nothing, 900-1200 ASSIGN/h. Dispatcher churn rules (opus), then re-measure with `base-audit.js --idle`; every squad job gets a cells/min/bot number in REPORT.md so slowness is seen the same hour |
| 2 | **Finish the base ground**: ravine S (~10 000 cells, lava first), pinholes 0, entombed 0 | `audit_fill` + `entombed` sensor are the judges; fill order `near`, ride-up, lava quenched from above |
| 2b | **USE WHAT WE OWN** (owner 09-20: "在庫の有効活用はしないのか？") - measured idle stock: gold_ingot 1006, lapis 5882, wheat 17 828, wool 10 328, redstone 3708, raw_copper 3093, sugar_cane 2995, logs 2772, sandstone 2619, coal 1876, diamonds 267, bones 195 | GOLD -> piglin bartering on the Nether floor = ender pearls + fire resistance (P6 without enderman hunts); LAPIS + the table -> enchanted swords/armour/picks (`enchant` job, bots hold 414 levels); WOOL -> beds by the hundred for the End fight (beds explode there) and coloured blocks for giant builds; DIAMONDS -> armour for the strike squads; COAL/LOGS -> smelt cobblestone to stone bricks for the plaza and the wall; WHEAT/PAPER/WOOL -> villager trading once a village is found (emeralds -> iron gear, mending); never dump a building material while a chest has room |
| 3 | **P5 NETHER**: portal (obsidian 44 in stock) built + lit by a job, nether-side hub with a safe walled stair, blaze rods | new job types `portal` / nether travel in `army_jobs.js`; fire resistance is not there yet -> gold + iron armour, bows; a lab run on the sandbox server first |
| 4 | **P6 ender pearls + eyes**: enderman hunting squad (night trips, `needsNight`), or piglin bartering once gold flows | `howto ender_pearl`; needs looting swords -> enchanting is ready (table + 15 shelves), wants lapis + levels: an XP routine (smelting, mob grinder at the ravine S before it is filled?) |
| 5 | **P7 stronghold + P8 dragon**: eye throws triangulated by 3 scouts, a road or nether route to it, beds/bows/water buckets drill in the lab | the fight itself is a scripted squad job: crystals by bow + pillar, then perch hits; rehearse on the lab server (cheats allowed only there) |
| 6 | **A depot that serves 50**: withdrawals measured in seconds, stale index healed, one hall per category near its consumers (build stone beside the works) | 29 s for 4 dirt on 09-20; opus measures where the time goes |
| 7 | **Perception that beats the owner's eyes**: every owner-first finding becomes an audit (done: weeds, fills, hunger-with-food; open: builders standing on a finished job, farms over target, guests at our stores) | one line per finding in REPORT.md `BASE AUDIT`, digest conditions for the operator |
| 8 | **Looks**: perimeter wall that follows the ground, paved plaza over the filled ravine, lit roads to the terrace and the mine, no dirt in anything permanent | after #2; blueprints exist (`wall_ring`, `road`), needs a follow-ground variant |
| 9 | **Less Fable, same quality**: opus engineers with precise briefs + file ownership, Fable for judgement and review; briefs and review checklists kept in docs/DEV.md | weekly limit; see memory `code-by-fable-only` |

## Lessons from world 1 (paid for in full — do not pay twice)
1. **Site choice decides everything.** A base in snowy taiga had no liquid water, no animals, no cane; every job fought the land. Scout FAR (400-800+ blocks) before settling; walking once beats freezing for days.
2. **One site = ONE height; level first.** The mega farm on a y86/y87 mosaic could never be watered; decks over voids and craters swallowed bots. `level` / `fill_void` before building; never deck a hole.
3. **Never trust an event without looking at the world.** "plugged", "planted", "build_done" lied (client prediction, stale counters). Read results back from the server and LOOK (`look`, `mapshot.js`) before believing a number.
4. **Never break a non-empty container.** Removing/moving chests destroyed stock; empty it first, verify, then break.
5. **Irrigation = closed, capped holes AT FIELD LEVEL.** A source above ground or an open hole floods the field; water below the soil level hydrates nothing.
6. **Audit what we built.** Compare the world with the blueprint / the planted count on every visit (`structure_damaged`, `hedge_damaged`, `crops_vanished`, `farm_degrading`, `flood`, `void_under_pad`); our own jobs undo each other more often than anybody else.
7. **Long-range scouting is a job, not an accident.** Biomes, herds, water, villages come from `scout` squads on rotating bearings; without them the army did not know what was 300 blocks away.
8. **Code is written only by the top model (Fable), one owner per file.** Operators and the foreman steer the board and file evidence in docs/BUGS.md; six authors once wrote six frameworks.
9. **Atomic saves, checked edits.** Every shared file is written tmp+rename; `ops/check.sh` after every edit; never a `//` comment on a chained line (it ate the dispatcher's `.sort()` for 40 min).
10. **Waiting is waste, idling is a planning failure.** Sponge jobs with big head-counts, shifts for long commutes, plans that free their bot when done — nobody waits for an LLM.

## Log (newest first) — `date | bottleneck | change | result`
- 09-21 16:5xZ | the stronghold lies under a river 23 deep; a stair from the water cannot start | camera found the PORTAL ROOM x93..109 z1511..1521 y-43..-36, 12 frames y-40 around 104,-40,1516 (1 eye in) -> settings.end.room (+stand 108,-42,1515); a camera-read 223-cell covered switchback from the N bank (door 116,96,1494) to the room's E wall 109,-42,1515 (no liquid within 2 of any cell), job `end_way` (jobs_end `work:'way'`) cut by jobs_nether's route cutter, now exported as `route.work/walk/plan` (+ water plugged like lava in the overworld); portal/dragon walk it (`viaWay`) | cut in ~65 min, Aoi door->room 97 s, room->door 113 s, 12 frames read, 0 deaths; job auto-paused. 11 eyes still needed
- 09-21 16:3xZ | WHY THE CAVE KEPT FILLING: `core/recover.js` (go back for your kit) sent every bot rescued (killed) out of the water cave straight back to its death spot after respawn - it checked settings.keepOut only (7 `core_error recover handle timeout` = those return trips) | a death inside a `settings.avoid` box is written off (no recovery trip). Also: 18 raw_iron_block smelted by `smelt_iron_blocks` -> depot iron 0 -> 44 (+52 carried), quartermaster made 9 buckets for the field water repair | watch the cave count fall
- 09-21 16:0xZ | IRON INGOTS 0 army-wide (312 in the depot at 12:1xZ; spent on re-kitting ~20 dead bots: swords, shields, buckets, armour via obtain/craft) -> the field water repair waits 'no bucket ... craftable (3 iron_ingot)' x35 | `smelt_iron_blocks` steps job (2 bots, repeat): 18 raw_iron_block in the depot -> 162 raw iron -> ingots (uncraft at the depot table, smelt at the furnace bank) | ingots pending
- 09-21 15:5xZ | ~15 bots trapped AGAIN in the underground water cave -528,59,-300 (many STALE, several with diamond tools; incl. bots I had rescued from it) - road_village crews commuting home from x-638 on partial paths still reach the hill funnel despite the avoid weight | road_village PAUSED (it had been raised to 27 bots); an engineer cuts a camera-verified EXIT stair from the cave to walkable ground and seals the hill shaft, so the bots walk out with their kits instead of being killed | evacuation pending
- 09-21 15:4xZ | fields 2 and 4 lost their WATER cells to dirt (audit: 8 + 9 at y68; farm_4 'dry 100' since the morning): the `void_fix_base_field_*_pad` fills (fill_void over the field pads) laid their dirt SKIN on the field's water - two blueprints wanting different blocks in one cell (`dig:ours` x45) | `army_jobs.js build`: a TERRAIN blueprint (level/fill_void/clear_area/quarry) drops every cell a structure of ours owns (army.js `ours()`, the same registry tidy uses); the two void_fix pads paused; base_field_2 + base_field_4 build jobs back from the archive = the repair
- 09-21 15:3xZ | Yuzu reached the stronghold fix 96,62,1514 (2 000 blocks S, 0 deaths): it lies UNDER A RIVER (water in the whole 17x17 window); the stair cannot start from water; the nearest land is the N bank ~z1502-1506 (skyshot survey) | end_stronghold PAUSED; an engineer maps the stronghold with the camera (stone bricks, end_portal_frame cells -> settings.end.room) and cuts a camera-verified access way from dry land under the riverbed | portal room coordinates pending
- 09-21 15:2xZ | base_pen_3's gate stood OPEN 2 h (herd 6 -> 2); no verb could shut it (`dig` refuses our protected gate, `place` counts it 'already there') | new steps verb `use` {at, open:false} = right-click, clicks only when the state differs, reads it back (army_jobs.js + armyctl KNOWN_VERBS + DEV.md) | Sayaka shut it 15:2xZ, verified
- 09-21 15:0xZ | the 4 eyes are spent (throws + the lost confirm eye); Hina declined 'no eye' ON the fix and walked 2 000 blocks home | `jobs_end.js`: with a fix on the board the job needs no eye (legs + stair are eyeless); the dip point is on the board (`settings.end.dipped` 96,61,1514) and no second confirm is thrown within 30 of it | the next stronghold slice walks back and cuts the stair (water-aware)
- 09-21 14:5xZ | 9 bots (GEMBA: 5x muster + 4) stood at -527,59,-299, Iroha/Kanade hp1 f0: traced Iroha - the road crews' straight commute home from segments 14+ (x-638) crosses a CHERRY-GROVE HILL at ~-520,78,-278 that has NO route to the base (island 151 vs the base's 120); the trap escape cut a stair there and the bot fell 19 blocks into the underground water cave | avoid box widened to x-560..-495 / z-352..-250 (hill + cave, w 400); 8 rescued (Himari responsive) | NEXT if it recurs: close the hill's shaft into the cave (escape scars -518..-526,77-79,-274..-270)
- 09-21 14:5xZ | the hourly WARNING STORM `travel_fail timeout/stop` (40-56 per 10 min, d 200-550 = road crews on the 400-block commute) | `army.js travel`: default time = max(240 s, 1 s per block of the walk); explicit opts.ms unchanged | re-measure travel_fail/h
- 09-21 14:4xZ | **Hina ON THE FIX 96,61,1514** (sh_arrived); the confirm eye DIPPED (dy -3.7) = the stronghold is underneath; the stair stopped at step 0 'water in the tread' (one fixed bearing) | `jobs_end.js stairDown`: every step takes the first of the four bearings whose tread holds no liquid; all four wet at step 0 = step 8 off to dry ground and cut next slice | sh_found pending
- 09-21 14:3xZ | foreman's 'STRAY COBBLE STRIP' x2 was road_village's own ramp (seg 2: -303,68,-406 -> -303,74,-353, width 5 -> y72 at z-368): `ops/base-audit.js` did not know roads | base-audit: every road segment's band (width + 2 shoulders, deck-4..deck+1) counts as planned -> the STRAY alert is gone. Same audit: base_field_4 has 9 WATER cells turned to dirt (the 'farm_4 dry 100' of the morning) -> the archived `base_field_4` build job put back active (2 bots) = the repair
- 09-21 14:3xZ | **STRONGHOLD FIX: ~96,1522** (Hina: bearing 0.21,0.98 from the base, crossing throw 0.1,1 from -12,110,-443, den 0.2 = a ~12 degree cut, so +-a few hundred blocks; 1 525 from the origin = inside the first ring). `jobs_end.js`: a stronghold slice more than 200 from muster no longer walks home to kit (`need()` only at home); hop 120 -> 400 | Hina walking the fix down; refine throws on the way
- 09-21 14:2xZ | stronghold search looped: Hina threw 5 eyes from the BASE (all bearing 0.21,0.98 = S by a little E), walked the 330-block crossing leg to ~-10,-450 and ended the slice there; the next slice's `need()` kit fetch walked her back to the depot, so the second throw was always from the base again | `jobs_end.js` phase A: the crossing throw happens where the leg ends, in the same slice, and triangulates against the first -> `sh_fix` | fix pending
- 09-21 14:1xZ | the lake trap caught Aika and Yuzu (hp1 food0) at -526,59,-300 AFTER the +60 avoid weight: camera = the E dead end of an UNDERGROUND water passage (water y57-59 x -538..-525, roof ~y63) off the lake lobe | `settings.avoid[].w` (per-box weight, default 60); the lake box now w 400 (near-veto); rescued both; `base_dorm` paused (14 cells: beds orient:rejected / resting, 30+ min at 0 cells - BUGS line) | watch the box: a 9th bot in it = fill the passage
- 09-21 13:4x-14:0xZ | two bots (Aoi, Chika) fell 13 blocks into a rock pocket at -66,94,-85 through a missing route FLOOR cell -67,109,-83 (hidden under the barter-post pass's stray block until the corner fix dug it out); a route job out of sight of the gate looped `nether_lost` | Nether engineer: `nether_corner_floor` placed the 5 corner floor cells; escape legs extended into the pocket (pinned job, the bot cuts its own stair out); `netherSide` runs a `route` job's work from where it stands when no gate is in view; wearGold hold dropped (army.js `goldKept`) | Chika home in 46 s; barter 4 passes 132 ingots -> 5 pearls (~5/h/bot), now 2 bots; blaze iron passes 3-5: 5+4+0 kills, 2 rods banked, 1 death (Kanade at the corner post, hp 10) -> retreat at 14 hp; eyes crafted so far 4
- 09-21 13:5xZ | 4 eyes in the depot, pearls/rods still arriving | `end_stronghold` ACTIVATED: 1 bot, 3 eyes (throw, read the entity's bearing, cross 320, throw again, triangulate) - the search runs while the Nether front fills the other 12 | first eye_thrown / sh_fix pending
- 09-21 13:5xZ | `end_eyes` crafted 0 with 6 pearls + 2 rods in the depot, Mio looping `eyes_crafted made 0` twice a second: jobs_end.js asked for item 'eye_of_ender' - that is the thrown ENTITY's name; the item is `ender_eye` (`armyctl.js recipe eye_of_ender` = unknown item) | renamed every item use (the entity lookup for the throw stays eye_of_ender) | **first 4 EYES OF ENDER** crafted by Fuuka 13:5xZ (pearls 2 + rods 0 left)
- 09-21 13:4xZ | THE DRAGON CHAIN MOVES: Nether engineer built the covered BARTER POST off route v3 (booth -67..-66,112,-78 over the piglin plateau): 96 gold -> 5 pearls in 3 passes, 0 deaths (depot 6 pearls); blazes with a BOW (`moves.shoot`, one implementation shared with jobs_end): Mio 5 kills / 2 rods banked from the stair-corner post seq 460, 0 deaths (Hina died retreating over the bridge from seq 470) | army.js: `wear()` keeps a worn gold piece in the Nether / on portal jobs (`goldKept`); bare:'iron' `ironUp` also fetches bow + arrows and crafts a stone sword when none is shelved | need 16 pearls + 8 rods
- 09-21 12:2x-13:4xZ | pearls: 0 of 16, barter paused (0 adults at the shelf); blazes: bare bots died at the doorway | Nether engineer: BARTER POST = side route `nether_barterpost` (3 steps up S off the v3 stair top to a roofed booth -67..-66,112,-78 open over the piglin plateau; `params.routeKey`, `yieldTo` so two routes never fight a corner, `complete`), `work:'barter'` + `params.post` trades from it (no stair search); `wearGold` holds a worn gold piece against army.js `wear()`; kit re-read before the gate (a missed crossing had banked the gold). Blazes: bow via ONE `moves.shoot` (moved from jobs_end.js), post at the stair corner (seq 460, out of the doorway's line; the bridge line killed Hina at seq 463), both-lane drop pickup, 32 bread, a hurt bot without food goes home. Also: `steps` verbs `light`/`route` (BFS over both lanes), bridge roof (interior covered), Aoi self-rescued by a pinned route job (`nether_escape_aoi`, 48 s, 44 dug) | barter 3 passes: 96 ingots -> 5 pearls (3.3/64), 2-6 adults seen per pass, 0 deaths; blaze iron passes: Hina (post 470) 31 arrows/1 kill/died, Mio (post 460) 24 arrows/5 kills/0 deaths/2 rods banked, Kanade running (1 rod); depot now 6 pearls, 2 rods
- 09-21 13:3xZ | the lake W of the base caught Chika + Riko (frozen underwater at -528,59,-301, STALE 4-9 min, rescued) - 5th and 6th bot in it today; road crews commuting to segments 14+ route past it | NEW `settings.avoid = [{box, why}]`: `army.js fieldCost` weights every cell in such a box +60 for EVERY bot (a weight, not a veto); the lake lobe x -560..-515 / z -352..-290 registered (camera: y61 slice) | watch stranded/stale in that box
- 09-21 13:2xZ | where the bread went (part): `village_breed` carries ~94 bread per trip to the villagers (Hina 13:2xZ) - a legitimate sink, now covered by the self-regulating baker (targets bread 512). Cavity engineer: entombings under the base came from escapes that climb straight up into a protected cap (chest/farmland) - `army.js sideExit`/`capAbove`, `cavity_core` job; shut_in 5-6/h -> 0, rescue kills 2-3/h -> 0 (first 30 min). `fix_depot_hole_367` closes the one leftover of the first sideExit version
- 09-21 13:1xZ | bots ENTOMBED under the depot/fields (5 rescue kills this morning; 261 roofed pockets y62-67) = pillarEscape/stairUp climbing into a chest/farmland/pad cap | army.js `capAbove`+`sideExit` (sideways at depth, sealed, pillar flush; also frees a bot already shut in); jobs_cavity: flood-tunnel entries `tunnelMax`, retreat-fill, portal never a void, enclosed pockets lit only from inside; board `cavity_core` (bare, 3 bots) | 12:40-13:10Z in the base box: 3 entombed (was 8-17/h), 0 shut_in (5-6/h), 0 rescue kills (2-3/h), 6 `side_exit ok`; core targets 0 -> 61, 3 filled; pocket scan 261 -> 274 (first tunnels left 2-cell stubs before the failure-close; natural-cave dead ends untouched)
- 09-21 13:1xZ | road_village re-blocked within one slice of the retry: a pass whose bot never reached the segment (`travel_fail timeout/stop` 12-70 short, `build_pass done 0`) counted as 'changed nothing', and the old counter stood at 8 | `jobs_road.js`: only a pass that ended within 16 blocks of the segment's line counts toward `blocked`; an expired block restarts the count and clears the mark; board: idle/blocked reset on all 14 unbuilt segments | muster 20 -> 3 after the re-activation
- 09-21 13:0xZ | GEMBA: 22 of 48 bots still, 20 at MUSTER - `road_village` had AUTO-PAUSED 'road complete - 24 segments' at 10/24: every one of the 14 unbuilt segments carried a `blocked` mark (set by passes that changed nothing, most while the paving was being banked as surplus before 09:1xZ) and `left = !built && !blocked` read that as done | `jobs_road.js`: a block mark expires after 45 min (retried), a road with any unbuilt segment is never complete (all-blocked = 10 min decline for that bot); road_village re-activated with 16 bots | re-measure muster count + road_seg_built
- 09-21 12:5xZ | route v3 DONE 473/473 (0 deaths since 11:50Z); `blaze_kit` re-run crafted 3 iron swords (first run: the worker's chest index read 0 iron while stock showed 312 - transient); `nether_blaze` re-activated: 1 bot, bare:'iron', den 25. `end_hunt_1` PAUSED (0 kills in 163 fights, pulled the fill crew away every night - rule 0b); pearls come from the barter post the Nether engineer is building | first blaze_pass pending
- 09-21 11:4x-12:4xZ | fill_ravine_s 0 cells/min for hours (`gaveUp 1974/waiting 1971`, 36 runaways) and base_cane `done 0/left 25` x22 passes | fill engineer: probe - 2 208 of 2 560 'open' cells were a cave under the grassed crust owned by nobody -> fill work = columns clear to grade (+2 sideways), the cave is `roofed` (cavity's); skin swap only on an exposed face, runaway counts real digs; the dead bucket descent revived (moves.waterDrop), filler loaded before going down, per-column way claim, no pour onto a body, ride own shaft up, `waitBy` in build_pass; base_cane: an all-`wait` remainder no longer reads as 'water in work', so the cobblestone fetch runs; board: fill_ravine_s `requires.anyItem water_bucket`, `bare:true` | fill 60 min before 166 cells (2.8/min), after 19 min 325 cells (17/min), 0 runaways, sky work 691 -> 258, box air 3 169 -> 2 704 (1 death: gravel poured onto own head, fixed); base_cane `build_done`
- 09-21 12:3xZ | FOOD, third time today: depot bread 1 502 -> 0 within ~1 h after an operator paused `bake_bread_industry` under rule 0b (it WAS over target then); the job is switched on and off by hand, so the depot swings between glut and famine | `targets bread 512` + `bake_bread_industry.produces:['bread'], minBots 0, maxBots 4`: the dispatcher now scales the bakers with the bread deficit (0 bots at >= 512), nobody pauses it again | targets row: bread 223/512 deficit 56 % -> bakers staffed automatically
- 09-21 12:2xZ | iron: after the mine engineer's round 2 (mine_blocked 36 -> 0, same-vein pairs 51/466 -> 5/94) raw_iron banked fell 312 -> 13 per 78 min because the squad sat on level y-54 (iron 0.2/100); `armyctl.js mine`: 7 of 8 levels EXHAUSTED (160 branches x 256), only y32 still has open branches (iron 4.2/100) | `mine level 32`, mine_iron minBots 6 / maxBots 12 (was 14-30: 30 bots on a spent mine is make-work, rule 0b); the freed hands go to the dragon chain. NEXT for iron beyond this: a second mine box, or an iron-golem farm at the village (PLAYBOOK). Pearls handed to the Nether engineer: a covered barter post off route v3 beside the piglin plateau
- 09-21 11:4x-12:0xZ | the mine's WAY HOME (BUGS 583/623/627 + the lake 555/621): MEASURED from results.jsonl - `to-trunk no_progress` -458,-56,-651 and the Akari/Tamaki box -279,-50,-331 were ONE cause: two miners took the SAME cave vein in the same second (`vein_done` pairs; 51 of 466 veins in 78 min were dug twice within 30 s), collectVein brought both into its 1-cell pit 2 under the level and walkLine's shared-cell rule deadlocked (first name waits, the other has no cell to go to). The lake pocket -539,62,-345 was NOT a mine exit: Hazuki/Fuuka/Riko fell in from `road_village` segs 16-23 (place_failed / build_runaway at its rim); the mine then called the pond `underground` (box reaches there, the pocket is roofed) and `reconnect` tried to cut 14 blocks DOWN through the pond floor to branch -535,48,-322 | iron_core: `walkLine` leaves a shared cell by one stair step (`climbStep` = reconnect's stepDig, target side first) after 3/8 s, a pillar failing twice becomes a stair step; `claimOre`/`releaseOre` (bots/iron_claims.json, cross-shard lock, 6 blocks, 4 min) = one miner per cave vein; `notOurs` (in liquid, or > 6 above every dug level off the stairs) = not underground, reconnect refuses `not_ours_*`; iron_miner: the same failure from the same cell 2x -> stair step, 3x -> `mine_stuck_spot` + hand-back to the army's trap escape | 78 min before / 25 min after: mine_blocked 36 -> 0, hung on mine tasks 8 -> 2 (both on the surface in depot/field pits after a haul, army side), same-vein doubles 51/466 -> 5/94. raw_iron 312 -> 13 banked: the squad sits on level -54 (iron ore mined 439 -> 15, redstone/diamond band) - a level choice, not this fix. OPEN: road_village drops bots into the pocket (jobs_road.js), swim to shore (moves.js), face/branch veins unclaimed (559), 607/610 tuff pockets
- 09-21 12:0xZ | blaze doorway: the Nether engineer's first `work:'blaze'` pass with ONE bare bot (stone axe, no armour, open bridge) saw 14 blazes, hit 4, killed 0 and died; 2 more bare deaths finishing the far end | DECISION: blazes are fought in IRON. `army.js bareDown` `params.bare:'iron'` = diamonds and valuables still banked, but worn iron/chain/leather/gold armour stays, an iron (else stone) sword, shield, bow and arrows stay, and a missing sword / armour piece / shield is FETCHED (`ironUp`). `nether_route` + `nether_blaze` set to bare:'iron', blaze `den:25`; `blaze_kit` steps job crafts 3 iron swords + 2 shields (depot had 0 of each). The blaze job waits for the route's roof + doorway fences
- 09-21 10:5x-11:5xZ | far gate lost in the flood; returns landing at a game-made gate -370,68,-656 (z-81 frame 126-130 blocks out, on the edge of the 128 search) | Nether engineer: `work:'steps'` verb `light` relit z-77 (6/6) and registered it; `degate` got `opts.box`+`unlight` (was refusing any lit frame) - z-81 frame and the overworld spare taken down (portal cells 0, obsidian 0 left); `core/combat.js` no longer pulls `nether route/blaze/steps` bots into chases (Sayaka died chasing off the bridge); route: cover-before-advance within 16 of the end, lane cross-over, BFS walk over both lanes (`routeWalkSeq`), finisher role (crew index >= 2), breach approved | returns land at home -326,69,-517 from z-77 x6 since 11:0xZ, arrivals at z-77; head 448 -> 472/473 (bridge floor + both walls stand, roof and the doorway fences left); blaze test pass 1 (bare, stone axe, no sword): 14 blazes seen, 4 hits, 0 kills, hpLow 5, died at the den - bare is hopeless there. 3 bare deaths on the route/blaze today after v3 (Sayaka, Hinata, Chika), all to blazes at the far end
- 09-21 11:2xZ | the foreman's 3rd 'lake trap' report (-539,62,-345, one death, 3 bots STALE 13+ min) | LOOKED: a sealed UNDERGROUND water pocket (1 air cell over the water, roof at y64), no swim-out; the workers were not dead but stuck 13 min inside `bank()` walking to 8 chests per category from a place the depot cannot be reached from | `army.js bank()`: two unreachable chests in a row end the visit (`bank_unreachable`); rescued Fuuka/Hazuki/Riko; `base_dorm` re-activated (2 beds + a gate missing while its job was paused) | OPEN: the mine exit that drops bots into that pocket
- 09-21 10:2xZ | FOOD again: 14 bots at food<=6, depot bread 0, wheat **4 099** - an operator had PAUSED `bake_bread_industry` (and cut it to 1 bot) on `PLAN IDLE`, reading `food 1213/448` as plenty; that number counts raw WHEAT as food. The idle passes were real but temporary (`none of wheat in the chest index` x11 while farmers still held the harvest) | re-activated, 4 bots, desc = STANDING CONVERTER, never pause on the food number, only when depot bread > 512 | depot bread 0 -> **1 393** in 8 min. OPEN: the `food` target should not count wheat as eaten food (it misleads every operator)
- 09-21 09:1x-10:5xZ | blaze rods: `nether_route` passes came home `placed 0, dug 0, steps 40`, head 0/468 - v2's TORCH cells (seq 0/6) held v1's wall blocks and a torch cell was never dug; `buildCells` stands had to be strictly closer, held sneak for the whole build (no climbing) | Nether engineer: `routeTunnel` (jobs_nether.js) cuts FROM INSIDE: stands only on the walk, works seq [here-6,here+3] in reach, one hand step (`routeStep`), crouched only at a real rim (`rimAt`), lava beside a cell plugged through the new hole from a stand traced beforehand (`plugSight`), cave floors by `moves.bridgeTo`, second bot cuts the second lane (`laneWalkOf`), spare stone picks, gold boots, head read over the walk, slices that start in the tunnel walk home first (`route_home`). v2's first live pass opened a lava SOURCE (-53,111,-81) in its leg-2 wall: 2 deaths (Erika slain by Piglin, Nanami lava) + the stair and hub corner flooded; legs re-sited (v3: stair cut WEST in the solid core from -55,98,-82, camera-checked), the source shut from the new route by `nether_unflood` (`work:'steps'`), hub corner dry (camera: lava 0) | head 0 -> 16 -> 57 -> 109 -> 192 -> 381 -> 448 (lane 0) / lane 1 reached seq 462 of 472 in 7 passes (~80 min); 25-132 blocks/bot-min at the face (80-94 with picks), 4 of 5 lava sources plugged (hp 20 each; 177,94,110 in the descending stair's wall is not: lane 0 blocked at seq 449, lane 1 passes), 0 deaths in the tunnel since v3, 1 at the gate (Suzu, ghast fireball on the shelf going home). Crew stopped at `stopAt:452`: the far end is 11 blocks from the spawner and woke 6 blazes - the breach (`params.breach`) is the top model's call
- 09-21 09:3x-09:5xZ | depot trap #2 (Akari shut_in 09:2xZ): it was NOT a missing chest but a 2-deep 1x1 hole UNDER the chest -344,69,-512 (the chest is the roof, so no pillar escape). Eval scan of every chest/barrel/furnace/table in x-400..-300 z-540..-450: this was the only one. A pinned `steps` plan could not reach its bot for 10 min: the bot's current job holds it (village_trade shiftMin 60; a `front`-less job also counts as a new work front) - `exclude` on village_trade freed it, Akari dug out N and walked free, then the dispatcher took her mid-plan; fill re-issued to Kaede. Also `army.js bank()` keeps ONE water_bucket like the best tool (water engineer: 502 `banked` lines put the reflex bucket back on every pass; carriers 6 -> 34 of 50 with the new well at -334..-333,-531..-530) | CODE GAP for later: `handover: banking N items first` runs while a bot is boxed in (Akari hung 3 min in it); a pinned plan should pre-empt a shift for a TRAPPED bot
- 09-21 09:3xZ | owner 「ダッシュジャンプ…めっちゃバグってる、直せるなら再開しても良い」 | opus engineer MEASURED the old hop on Hina (26 hops): 26/26 overshot the path node, 20/26 stepped back 0.4-0.9 m after landing, 3.18 m/s vs 5.2-5.4 plain sprint (the pathfinder counts a node only within 0.35 and <1 block vertical, so a node under the arc is never reached and the bot is turned round mid-air). `army.js dashRule`: hop only on a straight level run of the pathfinder's next 5 nodes, prune overflown nodes while airborne, 3 ground ticks between hops; `settings.dash` = true | list of names | Hina 6.0-6.3 / Mei 5.9-6.1 m/s vs 5.3 plain; step-backs ~2/100 hops; 0 ceiling/crop hits. Enabled for ALL 50 at 09:3xZ
- 09-21 08:4x-09:3xZ | owner 「水バケツ持ってないやつ多い」: 6-9 of 50 bots carried a water bucket (fall reflex needs one) - `kitUp` handed one only to build/delegate/deck/cavity and only a FILLED one from the depot (0 there: `bank()` deposits the carried bucket every pass), nobody could fill an empty one (no water source) | WELL: blueprint `well` (2x2 infinite pool, stone rim; `pool:true` water cells = a pool source counts as a closed side, allowFlow pour, a cell with 2 pool sources beside it self-fills) built by `base_well` at rim -335,68,-532 (east of road 14; seeded by two `steps` pours after the build crew found no water); `army.js kitUp` -> `waterUp`: EVERY overworld bot without one takes a filled one from the depot, else an empty `bucket` and scoops it at the well - only when all 4 cells are sources (Aoi/Kurumi had scooped the first lone source), not throttled by the 5-min gear throttle; quartermaster counts water_bucket as `bucket` (ceiling 2x target = the army's whole bucket budget), `targets bucket 20`, batch 8; 25 buckets crafted by `steps` (75 ingots) | carriers 6/50 at 08:47Z -> 12 (09:00) -> 25 (09:09) -> 31 (09:19) -> 34 + 6 carrying an empty one still to fill (09:29); army holds 41 buckets = the budget (20 -> 2x = 40 ceiling), 0 errors, 1 `well_scoop` no-route
- 09-21 09:1xZ | WARNING STORM 41x `travel_fail timeout/stop` in 10 min, 17 of them to the depot -371,69,-500 from 100-420 blocks out = the village-road crew walking home for paving: a fetch was capped at 192 for ~600-cell segments AND `offload` banked the paving back to 16 on every depot pass (`road` was missing from `KEEPS_MATERIAL`; `road_village (surplus) cobblestone` x22) | `army.js KEEPS_MATERIAL` += road; `jobs_road.js`: a segment > 120 blocks from muster fetches up to 384 | re-measure travel_fail to the depot in 30 min
- 09-21 09:0xZ | FOOD: escalation `no cooked food, 7 bots at food<=6` while the depot held **2 386 wheat** and bread 5; `bake_bread_industry` (repeat:true) withdrew 192 wheat = 64 bread and then asked for 9 crafts of 21 - craft 4 failed every pass, a failed step set `st.done` even on a REPEAT plan, and 22 bots declined it for good ('plan finished') | board: 189 wheat -> 3 x 21, onFail:continue, 4 bots, rev 102; `army_jobs.js steps`: a failed step on a repeat plan ends the PASS (decline 2 min, start over), never the loop. Also the depot trap that hung Mio twice = one missing chest in row z-503 (-351,67,-503): rescued, a 1-bot `steps` plan put a chest in the slot | depot bread 5 -> **312** in 6 min
- 09-21 08:2x-09:0xZ | iron 146/800, mine_iron: 23 bots re-walked ONE dead face (`to-face 12:-1 blocked` x29/h), 500 failing `mine_reconnect`/h (Noa x494 in 3 min from one cell), veins leaving drops (leftOnFloor/ore 0.83), every void read `cavern 2000` | LOOKED (eval at the face): the wall was a crafting_table a miner's interrupted craft left IN the branch. iron_core: walkLine pockets a stray table deep in the mine, withTable places beside the line; a face 2 miners cannot reach closes on the board (`faceFail` -> `mine_face_closed`); pointed_dripstone (waterlogged = read as water) / rail / cobweb knocked down before placeAt; reconnect bans a failed target per spot + backs off 20-60 s, stepDig 4 directions / walks <=3 drops / takes wall filler; iron_miner naps after a failed lost-surface; collectVein skips an unreachable drop instead of quitting; caveOre skips ore it came back empty from (30 min); voidSize `opts.wall` (jobs_cavity, default unchanged) = our dug galleries, passed by caveAt | 12:-1 blocked 0 since 08:28Z (walked on to len 57), mine_reconnect fails 0 in 35 min (was 500/h), mine_blocked 14->0 per 12 min, leftOnFloor/ore 0.28, raw iron banked 85/10 min (36-min window, was 54), mine_cave boxes >=100 wide 26% -> 8% and `big` appears (most y0 voids are real 50-high caverns); open: lake shore swim (555), Mio's depot pit (587), shared-vein claims (559)
- 09-21 08:5xZ | owner asked where the trunk road stands: `road_village` 5/24 segments in 2.4 h with 14 bots, and **1 137 of its 1 199 slices in the last hour were 3 ms no-ops** `road: bridge 1/1 cells (0 spans)` - seg 4 (a 2-cell bridge at -303,67,-344) had its spine ORIGIN missing; `bridgeSeg` starts at spine(1), `segState` judges the whole 5-wide deck, so the segment was never built and never had work | `jobs_road.js bridgeSeg`: once the spine reaches the far end, every deck hole is filled (`buildCells`), a hole it cannot fill = `road_blocked` + 10 min decline for that bot (never a hot loop) | seg 4 `road_seg_built` 08:48Z, the crew moved on to real segments. Also found: `voidSize` returns the 2000-cell CAP for 884/884 mine faces (floods our own corridors) - handed to the mine engineer
- 09-21 08:4x-08:5xZ | owner 「黒曜石を手で掘ってる」: MY route v1 climbed from the hub's W door straight THROUGH the far gate (-49..-46, y97-101, z-77) - the gate filter protects the portal cells and their 6 neighbours only, so its stair cell (-46,101,-77) = the frame's corner was dug with a stone pick (the gate still reads lit) | route v2: the climb runs 2 blocks S of and above the gate (z-75/-74), then N 9, E at z-84/-83; `scratchpad routecheck` read ALL 7632 cells through the camera before the put: 1811 dig / 332 place / 8 lava / **0 obsidian or portal cells**; also `army.js bank()` no longer re-WEARS when `strip` (bareDown took the armour off, bank put it back on: Nanami crossed a bare job in diamond chestplate + leggings) | LESSON: read every cell a route will touch through the camera BEFORE the put, not after
- 09-21 08:3x-08:4xZ | the dragon needs 8 blaze rods and the army had never found the fortress (`settings.nether.fortress` null, the walking search lost bots to the lava sea) | LOOKED FIRST: the SkyEye camera flew a 192-block grid over the Nether and listed nether_bricks + spawners: the nearest fortress and a BLAZE SPAWNER stand at the_nether **187,77,125** (walled brick platform, floor y76, x184-190 z122-128, a rock cliff 7 blocks W), ~300 blocks from the hub; per-cell camera profiles showed the Nether ROOF (y108-112) solid along the whole line (195/196, 161/166, 34/34). New blueprint `nether_route` (legs of `nether_road`/`nether_stair` MERGED: air beats stone so corners open, a torch never replaces needed rock, `seq` along the route) + `jobs_nether.js work:'route'` (head on the board `settings.nether.route.head`; each pass walks the finished way and cuts the next 40 cells) + `work:'blaze'` (hold the covered doorway 2 cells inside the platform wall, fight blazes within 10, pick up, retreat to eat) + `nether_spoils` (rods/pearls/eyes are ALWAYS_KIT, so nothing ever banked them: now banked at the start of every portal slice). Job `nether_route` (2 bots, bare) | first pass Rin 10 min: placed 30, dug 6, head 0 -> 2 of 462 (the air half of the climb from the hub's W door)
- 09-21 08:0x-08:2xZ | host froze 07:44Z (server AND node loops stalled 146 s) and rebooted; container reset lost Java | openjdk-25 reinstalled, `ops/up.sh`; owner 「ダッシュジャンプやめて」: `army.js dashRule` returns unless `settings.dash:true` (listener stripped from all 50 by eval; old slices re-arm it until they end); `bedDue` skips type `end` and every `needsNight` job (the night hunters were walking home to bed at dusk) | 50/50 online, dash listeners 0 after old slices roll over
- 09-21 07:1x-07:4xZ | owner: 「今日中にエンダードラゴン討伐が目標です」, 16 eyes = 16 pearls + 8 rods, army holds **0 pearls**; the barter front had crossed 36+ times and traded nothing, and the `nether_barter_road` lane had killed 3 bots at the same cell | FIRST: LOOKED. rcon + a step-1 SkyEye survey of the Nether arrival region proved two things the reports had wrong. (1) **`pigsSeen: 30` was thirty ZOMBIFIED piglins**, which barter nothing — the live piglins are 4-6 and they stand on a plateau at **y107-116** while the shelf is at y98, so this front is a CLIMB, not a crossing, and the 652-cell / 213-placement lane was aimed at the wrong problem. (2) The nearest cell we can STAND on within 8 of an adult costs **28-31 placed blocks**, not 213 (Dijkstra over the camera's solid/air map, cost = blocks placed). So `jobs_nether.js work:'barter'` was rebuilt around **`stairSearch` + `stairTo`**: a corridor-bounded best-first search over cells THIS bot can READ (key = cost, then distance to goal — unguided Dijkstra is hopeless when walking is free and every cost level is a shell through open cavern), whose route's natural prefix is walked by `nTravel` and whose placements go to the proven `buildCells`. The goal is frozen and written to the BOARD (`settings.nether.barterGoal`) so every bot extends the SAME stair instead of starting a new one at a new piglin. `nether_barter_road` paused (bots 0) — it was also jamming the one-at-a-time gate | **NOT DONE: still 0 pearls.** What is proven live: the search finds reaching plans every trip (8-31 placements, 20-40 cells) and reports them (`nether_stair`); what fails is LAYING them — `buildCells` came home `placed 0, steps 0, done 24 of 35` because every stand it considers is within 3 of the target cell and over a void those cells ARE the void. Four physics facts bought and now asserted in code: a block needs a **face** to be placed against (so an ascent over pure air is impossible without pillaring), a **crouched bot cannot climb** (`climbStep`, pulsed jump, crouch dropped only where no drop is within 1 — now wired into `safeStep`), a crouched bot **cannot step down** either (the open hole: the good routes want a 1-block descent), and **where a bot waits is a design decision** — 3 of my 3 deaths were bots standing still on a one-wide perch at -51..-56,26..28,-75..-78, so the trade loop now ends the trip wherever `walkableArea < 8`. Offline harness `/tmp/nprobe2.js` runs the exact production search against camera reads, so the next plan can be tested without risking a bot
- 09-21 06:2x-06:5xZ | iron 45/800, `iron_ingot` 0, and 20 miners banking building rubble: MEASURED three things. (1) The mine had **no miners at all** - depot food fell under 8 units/bot, `readiness` handed every one back (`mine: missing food` x48 from 24 bots in 20 min), and 7 such declines made the dispatcher REST the whole job 20 min (`muster -> mine_iron x68 / mine_iron -> muster x52` = the army's top churn flow of the hour). (2) `census(bot,40).ores` through eight miners' own eyes: **44, 60, 68, 84, 85, 90 EXPOSED iron ore** each - the caves the branches walk into and wall off - against a branch yield of 2.0 iron/100 cells and 24 cells/min for twenty bots (27 h to target). (3) `claimBranch` rebuilt each branch record without `iron`/`caveTry`, so the yield ledger the level choice depends on was being erased as it was written | iron_core.js: a branch that meets a void now ASKS what it is (`jobs_cavity.voidSize`) - a pocket is bridged as before, a **cavern is harvested and routed around** (`caveOre`: every exposed ore in reach of the level goes through the existing `mineVein` - corridor-level tunnelling, whole body, `collectDrops`+`collectVein`, trail back to the anchor; no pathfinder, no private tunnel) and goes on the board as an ore prospect (`mine_cave` + `settings.mine.caves`); the same look runs ONCE at every stint's face (`prospected`), iron first while iron is short; filler is any stone sort (`fillerCount`, was cobble-only - two of six miners sealed caves with 37 diorite in the pack); `readiness(bot,{afterDepot:true})` = a WELL-FED miner with a shift's bread goes down when the depot could not supply 8 units, instead of resting the job for the whole army; `iron`/`caveTry` preserved on re-claim. Board: sponge `tidy_spawn` 16 -> 4 (it held 16 of 49 bots at prio 60 while mine_iron at prio 92 and deficit 0.96 stood at ZERO), `haulAt` 12 | **BEFORE: 0 raw_iron banked by any mine job in 3 h, 3 `vein_done` in 3 h, 0 of 20 miners staffed, squad 24 cells/min. AFTER (18.2 min, verified live): 155 raw_iron BANKED = 511/h, 433 iron ore blocks mined = 1 427/h, 1 239 cells driven = 68 cells/min, 363 `vein_done`, 162 caverns classified onto the board, `iron` 45 -> 180 of 800 and `iron_ingot` 0 -> 46 (the world held none), y16 iron yield 2.0 -> 3.1/100, `mine_not_ready` 48/20 min -> 6/18 min and the job never rested.** THE DEAD RESERVE IS REOPENING: y0 went 0 open / 125 cave -> **125 open / 0 cave**, y32 143 cave -> 84 with 56 open (~250 of the 656 dead branches back in play, y32 now measuring iron 2.6/100 at ore 17.2/100). y16 is confirmed the right level (measured iron 3.1/100 vs y48 1.0). OPEN: drops that fall >3 blocks into a cavern are left (`leftOnFloor` 1-2 per vein) - deliberate, a bot never steps into a void; 200 copper armour pieces at prio 94 outrank the mine (rule 0b?)
- 09-21 06:1x-06:4xZ | owner: 「今日中にエンドラ討伐が目標です」; the army held 0 ender pearls, 0 blaze rods, 0 eyes after 24 h | THE END FRONT (`bots/skills/lib/jobs_end.js`, docs/END.md): one job type `end` with `params.work` = `enderhunt` (night squad, pairs, the gaze guard lifted for ONE chosen target because `army.js safePitch`/`kill` deliberately make it impossible to start a fight with an enderman anywhere else) · `eyes` (pearl + blaze powder -> eye_of_ender, banked, target 16, rests the JOB when an ingredient is missing instead of churning bots) · `stronghold` (throw an eye, read the THROWN ENTITY's bearing, cross it, triangulate, walk the fix down in legs, then a lit staircase with the 6 neighbours read before every dig) · `portal` (12 frames, property read back) · `dragon` (crystals by bow FIRST, then the head on the perch) | `end_hunt_1` LIVE: the squad reaches the ground and patrols in pairs, 0 deaths - but **0 endermen in fight range**: a probe found the only two loaded endermen at y-43 and y52 (the ravine), not on the night surface, and both test nights were skipped in <3 min by the own-bed cycle despite `needsNight` (docs/BUGS.md 06:2xZ). `end_eyes` LIVE and correctly blocked on 0 pearls + 0 blaze rods. `end_stronghold`/`end_portal`/`end_dragon` coded and staged PAUSED. **The deadline now hangs on blaze rods (Nether fortress, one source, no substitute) and on pearls from piglin bartering (861 gold = ~55 pearls) - the enderman hunt is the backup, not the plan.**
- 09-21 0x:xxZ | owner named the architectural flaw: 「土被せと道路が干渉してる / lockみたいな仕組みがないの？目標のブロックが決まってないわけ？差分管理というか」 — a lock answers "who may swing now", never "what is supposed to stand here", so two jobs with contradictory targets for one cell dug and placed it for 20 h (`build_runaway`; `cap_ravine_nm` x `fill_ravine_m` = 1196 cells) | THE PLAN COMPILER `bots/army/plan.js` + report `ops/plan-check.js` + `docs/PLAN.md`: every source of intent (build jobs, road segments, the archive, keep-outs) expands ONCE into `target(x,y,z) -> {block, owner, layer, kind}`; contested cells are resolved ONCE, at plan time, by a stated precedence (structure > road > pad > fill > cap; live beats finished for terrain; then priority, smaller footprint, id) and the loser's cells are rewritten away — never both. The owner's MATERIAL RULE lives in the map: `layer:'surface'`/`'structure'` = exact block, `layer:'body'` = ANY stone sort, so a buried cell is never "wrong" and never worth a dig; a terrain job's own top cell carries the ground material (3 590 cells rewritten to dirt), which is what the `cap_*` class existed for. `plan.diff(world, box)` = missing/wrong/extra — the ONE work list for building, repairing and the camera audit (`plan.persistent(prev,cur)` = escalate, never retry) — and `plan.lock/lockStanding` leases cells into the per-cell files `blocks.js` already checks, so a bot can hold the column and shaft it needs to get out (Kokoro, 19 blocks) | MEASURED on the live board: 377 k target cells, 200 chunks, 460-entry palette, 2.2 s, ~60 MB; 556 overlapping pairs of which **24 LIVE, unchained pairs actually disagree about the block** (base_yard_pad x road_mine 175 cells, cap_base_yard_pad x road_mine 170, cap_base_core_pad x road_mine 85 …), 6 PLAN ERRORS a human must fix (3 grade conflicts, 2 roads inside keep-out `ravine_s`, `base_depot_south` entirely buried in the finished hall+depot) and 3 cap jobs with nothing left; first full camera diff over the base ran in 21 s with 0 unseen columns. Library only — no job handler migrated yet (patch list handed over)
- 09-21 05:3x-06:xxZ | owner: 「幹線道路という仕組みを強化し…何度も通るであろう道は先に整備し…skyeyeでの緻密な設計が必要」+「幹線道路以外から幹線道路への行き方は穴を掘ったり、ブロックを置いたりしても良いのでは？」+「空が見えているか、ディメンションがどこか、によって重み付け」 | THE ROAD NETWORK (docs/ROADS.md): `ops/road-plan.js` surveys the whole corridor with SkyEye at step 1 and A*s the line over the real ground (contour, |Δy| ≤ 1, keep-outs grown by half the road width, water/ravine = a bridge segment) → straight one-height SEGMENTS + ASCII profile + picture; job type `road` (`lib/jobs_road.js`, extension module) builds them with any number of bots (per-segment claims, shares a claimed segment rather than declining - 4 declines had parked the whole job for 10 min), rails MEASURED against the world (drop ≥ 3 → fence; Nether: 2-high wall + roof where the sky is open), gaps by `moves.bridgeTo` span by span, `road_damaged` re-opens a segment = the repair. Named exception to rule 4: a **road spur** (≤ 32 blocks, work site → trunk) may cut and fill under a reasoned, time-boxed `allowTerrainEdit`. Cost model `roadCost(bot, mv)` prices every step off the pavement: 0 on a road, 0.6 open sky, 2.5 underground, 4.0 Nether (needs the one-line hook in army.js `strictMovements`) | `road_mine` (muster -367,-488 → mine head -330,-500, 50 cells, 3 segments, y68) designed +0 % over the straight distance, 0 cut / 0 fill, and BUILT live; `road_village` (624-block trade route, 863 cells, 118 segments, 1 bridge) designed and on the board PAUSED, picture `/tmp/road-road_village.png`
- 09-20 17:3xZ | owner: 「eye」= SkyEye, and it had never looked at the Nether; 「ネザーを掘り下げるってなんやねん」「アホ計画すぎる」; 「skyeyeが見やすいように建築するのもコツ」 | OUR EYES WERE BLIND THERE: mapshot scans from bot+40, so the Nether rendered a flat bedrock ceiling (y127..127) - under a roof the scan now starts at the bot's HEAD (`lib/mapshot.js`), and the first real picture shows the arrival platform ringed by RED (floating = void under it), lava to three sides, mobs at the edge; the 1330-cell dig-down `nether_stair` is RETIRED (superseded, do not re-activate) and the gate-pairing perfection is off the critical path (9 crossings already work) | to the GOAL instead: `nether_fortress` ACTIVE (6 bots, walks read-only and bridges the gaps toward Nether -784,-592) for blaze rods, `craft_gold_boots` + `nether_barter` (1291 gold idle) for ender pearls - eyes of ender: we hold 0 of either ingredient after 22 h
- 09-20 17:2xZ | owner: pathfinder can place while moving - use it for the bridge (I had switched `scafoldingBlocks` off myself and never connected it; a hand-written bridge put Aoi into the lava 16:53Z) | `lib/moves.js bridgeTo`: pathfinder scaffolding inside a corridor, no dig/tower/parkour/sprint, sneak held every tick, terrain guard opted out via `allowTerrainEdit` for the call only (two live failures first: the guard re-applied read-only movements and pathfinder's goto RESOLVES on an empty path) | PROVEN: Hotaru, 2-wide 9-deep gap, placed 2, 2.5 s, 0 hp, test blocks dug out again; handed to the Nether engineer (spans <= 6, rails before the next span). Measured the same hour: raw iron 413/h (was 129/h) after the cave/branch fixes; cavity fill only 119 blocks / 25 min (back with its engineer); rule 4 of CLAUDE.md rewritten: travel is read-only, every exception is a named, guarded technique
- 09-20 17:0xZ | CORRECTION (coordinator): "74 iron axes = 222 iron" was WRONG - smelting an iron tool yields ONE nugget, so a bought tool is worth ingots only while the army is short of that tool, and it held 38 axes for 50 bots. The real iron gap is ARMOUR (helmet 5 / chestplate 8 / leggings 7 / boots 4 ingots), shields, buckets, an anvil (31) | jobs_industry buys strictly by PIECES LACKED per kind (`settings.targets` minus stock.js, no static floor): `iron_axe 38/8` buys nothing, `iron_chestplate 19/30` buys eleven; per-kind `maxPrice` (books up to 64, armour 12); `trade_done.ironSaved` = ingots we did not have to forge, counted only for kinds we were short of; emeralds are HOARDED for the armorer's armour and the librarian's books, purse 448 so one visit can buy an armorer's whole stock (105 pieces x 4-9 em = ~700, one bot-hour). Board targets set: iron_helmet/chestplate/leggings/boots 30, iron_axe 8, iron_pickaxe/sword 12, enchanted_book 8. Plus the VILLAGE SITE CHEST (-744,70,31, via A.stash/A.unstash so the depot index never counts it) - a trip tops up there and leaves what did not sell | throughput measured with the HONEST metric (emeralds EARNED, not the purse): **925 wheat sold to 6 villagers in one 6.5-min visit, 469 emeralds/bot-hour** (was 120-192 wheat to 3 villagers, 286/h). Still 0 iron armour and 0 enchanted books bought: the village has no armorer and no librarian - 9 villagers, 1 unemployed, so `work:'breed'` (96 loaves fed) is the gate. The librarian re-roll ran live end to end (`book_none` on the toolsmith -> `book_rerolls_done`, bounded exit); the dig/replace re-roll and the `anvil` verb are still unproven
- 09-20 16:5xZ | owner: 「交易の最適化とかも」- 624 blocks each way is the whole cost of a trip and one pass served 3 of 9 villagers, the cargo was a round number instead of what the village can absorb, and a trader reached the village on hp 1 / food 0 with four loaves in her pocket | jobs_industry `trade`: SEVERAL PASSES until a pass trades nothing; CARGO FROM MEASUREMENT (`settings.industry.absorb` = Σ(remaining uses x price) per item, written every visit - wheat 624, flint 600, coal 480, carrot 336, leather 288; a fresh measurement that does not name an item means nobody there buys it, so wool and paper stop riding 1 250 blocks); BUY BY DEFICIT against `settings.targets`; `nibble()` at every phase boundary + legs capped at 5 min so the handler re-enters and eats, 32 bread per trip; `trade_price` reports every demand-driven rise; emeralds are measured as EARNED, not as the purse | **286 -> 729 emeralds per bot-hour**, wheat sold per visit 120-192 -> 377-480, trip 10.4 -> 5.6-6.3 min; the job banked **74 iron axes + 219 emeralds in one hour** = 222 iron-equivalent/h from 2 bots (the mine: 8-17 iron per bot-hour)
- 09-20 16:5xZ | after the 7 workstations went down the village read `farmer 2, leatherworker 4, toolsmith 1, weaponsmith 1, unknown 1` - nine villagers and ONE unemployed, so the loom, lectern, barrel and smoker can never be claimed and wool/paper stay unsellable. The village is short of PEOPLE, not workstations | `work:'breed'` (bread at every adult - 3 loaves make one willing - and they breed into the free beds: 12 beds for 9 villagers, wheat is our largest glut) + `work:'librarian'` (the player's re-roll: while a librarian has never traded, break OUR OWN lectern and put it straight back until the book is Fortune/Mending/Unbreaking/Efficiency, then buy it, which locks it for ever) + verb `anvil` (book + tool for a few levels - no bot ever reaches level 30, deaths wipe XP). Board: `cane_to_paper` and `bake_bread_industry` feed both | breeding VERIFIED 16:47Z `breed_fed {villagers:8, loaves:32}` x3, depot bread 34 -> 539, paper 15 -> 3 533. The re-roll and the anvil verb are NOT field-proven yet: no librarian until the first baby, no anvil until 31 iron - docs/INDUSTRY.md says so plainly
- 09-20 17:0xZ | owner: x50 forgotten (gate, chests); neighbours of dug cells not read; drops not picked; idle bots; 'cobblestone short?' | depot EAST WING built in 4 min by 8 bots (tools 9 -> 33 doubles, ores 31, food 20, salvage 19), digest remedy for chest_full = rows not single chests; gates re-sized x50 (home apron 16x15 + road STANDS, Nether platform 15x15 + 5-wide causeway in work, dead frame 14 -> 5 obsidian); mine: 65 % of every level had been written off at the first cave (959 of 1280 branches `why:'cave'`), now bridged and re-opened (y48: 0 -> 101 open branches), every dug cell reads its 6 neighbours, veins uncapped, tuff/raw-iron bodies followed; PROBED Mashiro: 20 coal lying in her vein cavity 10-15 blocks behind her -> drops-before-leaving + sweep + tunnel-face survey for giant veins ordered; 'waiting for cobblestone' was NOT a shortage (6073 in the depot): the exact-material rule sent builders to muster instead of the depot -> `build_fetched`; enchanted tools could not dig at all (mineflayer digTime vs 26.1 item format) -> `enchantsShim`; idle 9 -> 0 (tidy sponge had been cut to 2 bots while it was broken; mine maxBots 30 for 101 open faces); OWNER-LOCKED guards pause too | measurement of iron/h after the mine fixes pending
- 09-20 16:3xZ | owner: is the bucket landing verified? (it was NOT - I had claimed 'weak to lag' unmeasured); why is a shared technique in a job file?; what does Sonnet decide?; spare kits; rescue kills | MEASURED on Yuzu at ravine S: trials 1+2 failed (lost 5 / 5.3 hp) because mineflayer emits `physicsTick` BEFORE that tick's position packet - the server still holds the previous position, eye-to-floor > 4.5 -> no water; `setImmediate` + fire at feet-to-floor <= 2.8 + repeat only while the hand holds water_bucket: **3/3 at 9 / 11 / 16 blocks, 0 hp lost, one use, water scooped, no source left** -> NEW `lib/moves.js` = the one place for player techniques (`waterDrop`, reflex `fallGuard` installed from the heartbeat; `dropIn` calls `waterDrop` first); `rescue` refuses off-overworld bots and responsive bots whose own escape has not failed (2 Nether rescue-kills = 54 % of the iron lost in 30 min); help desk (sonnet) got read-only eyes + 8 turns; tools census: 164 pickaxes on 50 bots, Aoi 3 diamond picks -> surplus rule with the kit engineer | engineers' proofs: escape `no_route` 106 -> 3-7 per 10 min, STILL 16 -> 3; tidy 0.1 -> 2.5-4.4 cells/min/bot, hole -324,-387 closed; recover: every death priced (`lost`), ~27 % of iron back; gates: 5 crossings + 4 returns on the home gate, causeway to the exact partner column in work
- 09-20 16:4xZ | owner: 「ブランチマイニングのアルゴリズム悪いのでは？鉄って何個も連なって出るでしょ？」「ダイヤも」 - 2580 raw iron in 20 h = **12.0 raw iron per miner-hour over 212 bot-hours**, and `armyctl.js mine` called every level y48..-32 EXHAUSTED. MEASURED from the branch ledger: 959 of 1280 branches were closed `why:'cave'` at a mean 81 cells of 256, so **65 % of the footprint of every level (189 488 cells) was written off** - y16, the best iron level in the game, was dug to 11 % (4691 of 40 960 cells, median branch 6 cells, 15 minutes) and then declared exhausted, which is why the squad ended up at y-48/-54 on 2 % ore. `veinOf` also cut every vein off at 24 blocks and did not know `raw_iron_block` or tuff at all, so the giant 1.18+ iron veins (the 5 raw_iron_blocks in the depot came from one, 03:50Z = the best hour of the whole run at 33 iron/miner-h) were walked past; `census ores` counts **658 ore blocks still exposed inside our own levels** (96 iron, 50 diamond) | `iron_core.js`: a face is followed as the WHOLE connected body - 26-neighbour (diagonals are one vein), `raw_iron_block` IS iron ore, cap by SAFETY (no cell touching lava/water, ≤ 24 blocks from the face, VEIN_MAX 320) instead of by count; a body with a raw block or ≥ 8 ore is a GIANT vein whose TUFF is opened as a corridor (96 cells) and which is announced (`mine_vein`) and written to `settings.mine.veins`. A CAVE is no longer the end of the rock: the branch bridges it walled and lit up to 16 cells (`cave_bridged`) and a branch closed at one is handed out again 3x (`caveTry`) - `exhausted()`/`branchOutlook` count those, so a level is spent only when its caves are. Per-branch `iron` is now recorded and `I.levelYield(L)` = ore / iron per 100 cells advanced; while iron is the deficit the squad takes the level with the best MEASURED iron yield (the book - peak y16, linear to 0 at y-24/y56 - only stands in under 400 cells driven). `army.js` fair share: an enchanted tool outranks a plain one of the same kind (half a tier) and a RISK bot with no Fortune pick opens the tools chest itself and takes the best Fortune stack, putting the plain duplicates straight back - the chest index counts by NAME, so a Fortune iron pick had been losing to every plain diamond pick and never reached the face - and the owner's second hypothesis 「掘ったブロックに隣接しているブロックを見ていないのではないか？」 MEASURED by walking our own corridors with a probe: **1.6 leftover ore faces per 100 m at y-54** (44 redstone, 16 diamond, 7 gold, 3 iron over 4500 m) and 0.3 per 100 m at y-32 - so `digCell` now calls `noteOre` on EVERY cell the mine opens (branch, trunk, hub, landing, stair, reconnect gallery and the cells of a vein excursion: only two call sites read faces before) and `drainSeen` mines from that queue what is in reach and logs what this pick cannot take. Kit (owner 「鉄装備2セット持ってるやつとかいるぞ」, measured 164 pickaxes on 50 bots, Honoka 28 / Chino 28 AT THE MUSTER): `surplusTools` = one of each kind (2 pickaxes on a mine/ores job) + the worn armour + one shield, everything else rides back to the shelf on the next `offload()`; `steps`/`scan`/`trade`/`haul`/`depot` loads are cargo; builders, miners, deck and cavity crews carry ONE water_bucket in the HOTBAR for `moves.js fallGuard` | PROVEN: offline against a synthetic giant vein - 10 ore + 1 raw block chained diagonally, 6 tuff cells bridged, ore beyond the tuff reached, the lava-touching ore refused (old code: 10 cells, no tuff, no raw block); live: the 8 levels went from `EXHAUSTED` to **189 488 cells of rock open again** (y16 alone 133 branches / 30 225 cells) and the squad's level choice left y-54. Kit surplus live within 4 min: `banked {job:'muster (surplus)'}` from 26 bots, `ores_ravine (surplus)` from 17, Aoi's 2nd diamond axe + spare iron armour + 2nd shield back on the shelf; all 5 water buckets in the hotbar. **The pickaxe glut cannot move**: all 9 tools chests are 54/54 FULL (`chest_full cat:tools` x42), 341 pickaxes on the shelf and 168 carried for 50 bots while `toolsmith_picks_fix` banked 438 stone pickaxes in an hour - that job is make-work (rule 0b) and the depot needs a tools row. NOT DONE: intermediate landings y8/24/40 - the stairwell's landing grid is 16 and any inserted landing moves rows already dug (`mine_level_refused level 24`, row 49), so the iron band between the levels needs a SIDE FLIGHT off an existing hub (a second stairwell geometry), not a `mine level` call; and the XP concentration rule is still unmeasured
- 09-20 16:3xZ | owner: 「地下にbotが誤って掘った穴が多すぎます、敵mobが変な位置に湧く原因になります」「地下から緊急脱出を試みる際に作った階段を埋めないのは何故か？」「バケツ降り出来ないのか？」 - NOTHING WE HAD SEES UNDERGROUND: `ops/base-audit.js` flies the camera OVER the base, so 490 `dug_out` scars, every shaft a trapped bot cut and every pocket our own fills closed over were invisible; a dark cell with a solid floor is a mob spawner and a scar that reaches daylight is a mob highway into the yard | NEW `bots/skills/lib/jobs_cavity.js` (job type `cavity`) + NEW `ops/cavity-census.js`. SEE: flood-fill every connected AIR component of the base box (6-neighbour, y 30..120) and classify it - `mine` (the miners' stairwell +2, never touched) · `planned` (blueprint cells incl. their `air`) · `open` (a pit = fill_void's work) · `escape_stair` (narrow and rising, AND the narrow NECK cut out of a cave a bot chimneyed into - the level-by-level widening is where our hole ends and the cave begins) · `natural_cave` · `cavity` (enclosed, dark spawnable floor). Two pairs of eyes, ONE implementation: the job's `work:'survey'` through a bot's loaded chunks, and the spectator camera at 100 % coverage from the CLI. FIX: the owner's algorithm, unchanged - 1x1 shaft from a FREE surface cell (never a building/field/pen/road or within 2 of furniture; 1-3 cells of tunnel at depth instead), the two cells under the feet READ before every dig (lava/water/gravel/bedrock/our own blocks veto the column), water bucket when the drop is > 3, torch, fill bottom-up standing on its own fill, ride the shaft up, restore the surface block as found, scoop the water. Plus `module.exports.waterDescent(bot,{rim,column,floorY,stop,scoop,openBelow})` - the reusable descent the owner asked for, exported for any job | MEASURED: census 100 % of x -408..-230 / z -559..-369 in 17 s (34 189 columns x 91 cells), 675 components; the first list was 3 targets because `level`/`fill_void` cells were counted as PLAN and vetoed 96 % of the ground - terrain blueprints are ground, not plan, and the honest list is **48 targets / 295 dark spawnable floor cells / 1 628 cells to fill**, clustered exactly where the `dug_out` events are. An ACTIVE `fill_void` owns its own columns (it dropped 78 -> 48: no second job in one column). Fills read back from the world, not reported: `cavity_filled {at:[-361,60,-535], placed:7, restored:cobblestone, tookS:74}` and `{at:[-307,57,-461], placed:3, tookS:59}` - both columns solid from the cavity floor to the grade, the surface block back as found, and a second scan of each shaft column shows every neighbour still grass_block (0 blocks placed or dug outside the hole and its own shaft). 0 deaths on the job, census 48 -> **45 targets / 295 -> 275 dark spawnable cells** in the first half hour. OPEN, and it is the same wall the mine-head pit hit: ~60 % of the failures are `no_route` in the ravine quadrant - the bots cannot WALK to the holes, which is terrain debt, not this front; and the water descent pours, costs 0 hp and is exported, but stepping INTO an already-open column is not yet reliable (it swims across the 1-wide shaft). Head-count 5, `bots` raises it as the terrain opens up
- 09-20 17:0xZ | the same front, second unit: `fill_ravine_s` has read `done 0` (`no_los`/`unreachable`) for 14 h because the fill CRUSTED OVER the ravine's overhangs - 3 081 air cells in its box x -334..-289 / z -486..-461 of which only ~1 300 are still open to the sky; my first rule ("an ACTIVE fill_void owns its columns") handed the other 1 780 to the job that cannot reach them | the line between the two jobs now runs through CELLS, not columns: the fill keeps what is open to the sky, an air cell with a solid block over it is this front's, inside an active fill's box too (that IS the classifier's `open` vs `cavity`). `params.dig` lifts the depth and size caps inside a known excavation of ours and sorts BIGGEST first; a component the mine's stairwell merely passes through is no longer "the mine" (0.4 share instead of one touch - it was hiding a 959-cell cavern) and the miners' cells are dropped from the fill set one by one instead. THE WAY DOWN IS NOT MINE ANY MORE: my poured-column descent was safe (0 hp) but swam ACROSS a 1-wide shaft, so the file now calls `moves.waterDrop`/`stepOff`/`scoop` - and since a 1x1 shaft cannot be bucket-dropped from INSIDE it, the digger opens the NEIGHBOUR column at depth and steps off THAT (`sideDrop`), which is what the technique's rim contract wants. Plus `bot.__armyInFill` while a worker is in its shaft, workers fill their own bucket at the pond -408,62,-455, shafts >= 4 apart, and every cell the entry opened is filled back | census of the box: **53 targets / 1 778 enclosed cells** now reachable (was 0 - they were excluded); bots get in (`cavity: shaft at -323,68,-473`, `filling 93 cells at y52`). The wall is the crust itself: ~half the failures are `no_route` to rim columns AT y68 - above the `ravine_s` keep-out floor (base.y-3 = 65), so the keep-out is NOT the cause, the broken lid is; the job now tries all four rim columns before it gives a hole up
- 09-20 16:1xZ | the Nether crossing "worked" and every trip still ended in the wrong world, and both scouts sent this shift were killed at the far gate | MEASURED, one cause at a time: (1) 0.6 s after a transfer `blockAt` is still null, so "am I in a portal cell?" read FALSE, nobody stepped out, and 15 s later the cooldown expired under the bot's feet and the gate sent it home (`offCellS 0.6, offCell:false`) - now the arrival waits for the ground to be READABLE and then gets out and checks, four times, inside the cooldown; (2) a portal cell priced at 400 was still the cheapest way across a 9-cell pocket, so the pathfinder walked bots back through the gate - `nether_portal` is now in `blocksToAvoid` for every walk of ours and only the deliberate entry lifts it; (3) `clearOfGate` insisted on 3 cells of room and walked into the frame's obsidian for the whole cooldown while a free cell lay one step east - out is out, room is a preference; (4) `bot.game.dimension` AND `bot.entity.position` lag the world by seconds after a transfer (Aoi did a whole far-side pass while her position read an overworld cell 300 blocks away) - `netherHere()` now asks the GROUND (Nether biome / bedrock at y127 / Nether block underfoot) after waiting for it to be readable; (5) the two dead scouts were killed by an operator `rescue`, not by the Nether - the 8-cell arrival pocket makes army.js file `stranded` and the digest answers with a kill (docs/BUGS.md 16:0xZ: `rescue` must refuse off-overworld bots; a trip holds the alarm down meanwhile) | PROVEN live: `portal_through to:"the_nether" pos:[-44,98,-79] offCellS:0.7 offCell:true hp:20` and again at 16:11:14 with 0.3 s, `portal_back pos:[-326,69,-518] from:[-43,98,-80]` = home to the block; the partner gate at the_nether -41,-65 is being built AT THE ARRIVAL LEVEL (that column's floor is y23, the shelf ends at z -72), reached by a 3-wide railed causeway (`work:'pair'`), and `work:'degate'` takes the old frames down from the far side
- 09-20 16:1xZ | owner: 「エンチャント,工業化など鉄は様々な方法で産出できます」 - 2580 raw iron dug in 20 h, every mine level y48..y-32 exhausted, iron 100/800, while lapis 7038 / diamond 336 / 22 books / 15 bookshelves sat unused and no bot had ever opened an enchanting table | NEW `bots/skills/lib/jobs_enchant.js` (verb + job type `enchant`) + blueprint `bots/blueprints/enchant_room.js` + the zone row in docs/WORLD.md + `settings.enchantTable -344,69,-517`; `iron_core.bestPick(bot, block)` now takes the FORTUNE pick for an ore cell and the plain pick for rock (digCell passes the block), so an enchanted pick is spent only on what pays. The verb reads the three offers off the window (`craft_progress_bar` does survive ViaBackwards), matches the HINT against `want`, re-rolls like a player by burning a book in offer 1, and reports what it reads back OFF THE ITEM | PROVEN LIVE: room measured at full power (offer 3 = exactly 30 levels); `Karin enchant_offer ["7:unbreaking 1","9:efficiency 1","30:efficiency 4"]` -> `enchant_reroll {burnt:book, seedChanged:true}` -> `enchant_offer_taken ["8:efficiency 2","19:fortune 1",...]` -> `enchanted {item:stone_pickaxe, got:[{efficiency,2}], levelsSpent:2, rerolls:1}`, byproduct an `enchanted_book`; `enchant_room` build job read the world and auto-paused "build complete". **THE BOTTLENECK IS NOT THE TABLE, IT IS XP**: measured over 19 min the army EARNS 2031 XP pts/h and LOSES ~3350/h (9 deaths/h x ~450 pts kept-100), best bot 26/30, nobody has ever reached 30. XP is per-bot and untransferable, so spreading the mine's ~2800 pts/h over 13 miners means none of them ever crosses 30 - concentrate it (fewer ore-breakers, `shiftMin`, and let MINERS empty the furnaces) or Fortune stays theoretical
- 09-20 16:0xZ | owner: 「余計な穴とかは事故の元なんだよなマジで」 - the groundskeeper produced NOTHING (`tidy_spawn`: 7 bots, 0.1 cells/min/bot for 195 min) because every hole/bump unit it took was refused for ever: a small OAK GROWING IN THE 3-DEEP PIT at -324,-387 made its 13 neighbour columns `occupied` (leaves), ores in a bump read `kept:coal_ore`, one of our own torches on a bump read `kept:grass_block`, and the same unit was re-taken every few minutes (x4, x6) | `army_jobs.js` tidy: a TREE whose foot is not at the zone level is part of the defect (felled whole with `BL.harvestTree`, logs banked, its leaves in the fill range cleared instead of counted `occupied`, sapling replanted on the finished ground outside our zones); an ORE in a bump is MINED with the tier that drops it; our torch on a bump is taken and put back on the new ground; a unit that fixes nothing twice rests 2 h and says `tidy_stuck` ONCE (new digest line in `armyctl.js wait`); an active terrain build shields only the COLUMNS it still has an open cell in, never its bounding box (`fill_minehead_pits` kept tidy off its own open pit with `left 0`); `fill_void` fills FARTHEST FROM THE ENTRY first inside a tier and keeps the entry column open like a stairwell column until nothing below grade-3 is left; `mine_head` blueprint lost its four chests (ONE storage complex) and tidy's furniture-litter rule now also runs inside our own zones | MEASURED: `tidy_fix` n>0 on 36 of 65 units in the 20 min after the deploy (before: 0 of ~30), `felled` on tree units, `tidy_spawn` 0.1 -> 4.4 cells/min/bot in `ops/gemba.js`
- 09-20 15:4xZ | owner: 「地下から緊急脱出を試みる際に作った階段を埋めないのは何故か？1人なら階段である必要ないのでは？」「穴にハマったbotが階段を作ったりせずにハングする、出る努力がまるで無い」 - 366 `no_route` in 30 min, ALL from ONE 3-deep pit at the mine head: the gate read `area` (120 - cells you can DROP into, one way, down the mine) while the truth was `island` 11, so nobody judged six bots trapped; they stood there up to 38 min with cobblestone in their pockets until an operator killed them and the kit was lost | army.js: TRAPPED is now a FACT read from the world (`trapped()` = reversible island < 40 AND the goal off it AND 2 route failures or 20 s; the heartbeat's `boxed` flag says the same, `__armyInFill` builders are never trapped), and a trapped bot acts by itself in a player's order: **step out of the hole / pillar straight up** (`pillarEscape` - the column IS the hole's fill, one wall block is mined when no filler is carried, never up into gravel/sand/liquid, never a tower in the open, and it steps off its column), only then a stair - which is **back-filled behind the climber** (`sealBehind` after every step, the mouth closed from the rim, whatever is left is named in `escape_scar`). Plus `trap_found` (the SPOT, once an hour, for the fill jobs), `no_route` deduped per island+goal per 5 min with a count, one escape per spot per 5 min | VERIFIED live, world re-read after each: Yuzu out of an 8-deep hole in 25 s (`pillar`, 7 blocks - the column at -319,-470 now stands solid y59-68, flush with the rim, 0 open cells), Aoi/Ume/Sayaka `step_out` in 1-2 s, stair escapes in 8 s with 0-1 scars (since sealed). **`no_route` 103 per 10 min -> 2**, GEMBA STILL 16/50 -> 3/50, 9 escapes by 8 bots at 8 distinct spots in 20 min (no loop). Two lessons paid for in kit: a `stranded` line is what an operator answers with `rescue` = a KILL (Tsumugi was killed on a report that said `area:120`), so `stranded` is now said only about a bot that is stuck by BOTH measures; and the first brake refused a bot's SECOND escape after the first had worked (Yuzu fell into the next hole and was killed) - a brake now only stops a repeat of a FAILED escape
- 09-20 15:5xZ | owner: iron comes from more than digging (enchanting, industry); 「バケツ降り出来ないのか？」; mine-head chests + mound; food alarm (0 bread in the depot, 15 238 wheat, 33 bots on rotten flesh - I had removed `bake` as make-work) | `bake_bulk` steps plan (3 bakers x 192 a round; first version failed: no table within 12 -> `goto craftTable` added; bread carried 153 -> 361 in 3 min); mine-head chests = `mine_head` blueprint furniture: `armyctl.js chest rm` (new) took them out of the books, the blueprint change is with the army_jobs.js engineer; mound = 63 natural grass blocks at y69 that tidy keeps -> `base_infill_minehead_mound`; engineers: NEW jobs_enchant.js (room, `enchant` verb with re-rolls, Fortune picks for ore only), `waterDescent` exported by jobs_cavity.js for the fill's entry, Nether handed to a fresh engineer with the camera census (overworld: ONE gate; false crossings were `dimOf` = unknown during respawn)
- 09-20 15:50Z | "gates multiplying / bots bouncing out of the Nether" was a MEASUREMENT bug, not a world bug | camera census of both dimensions (SkyEye + rcon tp + findBlocks -> `settings.nether.gates`): exactly ONE overworld gate (ours, lit), the spare at -284,-607 already removed, and NO gate at -130,-240 or -203,-338 where bots claimed to arrive -> `bot.game.dimension` is briefly falsy during a respawn packet and `dimOf` returned 'unknown', which `!== 'overworld'` read as a crossing. Fixes: `dimChanged()` counts only a KNOWN differing dimension; `netherSide`/`doWork` refuse to run unless the bot really is in the Nether; plus the owner's basics (never place/dig/measure while feet or head are a portal cell, `clearOfGate` walks out by hand with control states, no loops in the frame, the 5x5 cage replaced by a 7x9 platform with nothing within 2 of either face, arrivals spread off the cell by roster index) | 0 bots off overworld, 0 deaths since the pause, obsidian 25 banked; `work:'pair'` written to build the exact partner at Nether -41,-65 - three round trips still to prove
- 09-20 16:0xZ | owner: 「アイテム回収に行かないせいでアイテムがロストします / マイクラの基礎が分かっていればこんなミスはしません」 - a dead bot respawned, re-kitted from the depot and went back to work; its diamond pickaxe, iron armour and 900 cobblestone lay where it fell and despawned 5 min later. 267 deaths in 20 h ate ~1300 iron and NOTHING measured it | new core module `bots/skills/core/recover.js` (`settings.core.enabled:["combat","recover"]` + `ORDER` in core/index.js, rolling restart): (1) RUN BACK - the 'death' listener records where/when/what/whether it burned, `onDeath` decides once (overworld only, not lava/fire/void, worth a trip = an iron/diamond tool or armour piece or >=64 of anything, not down in a keep-out HOLE - same `y < base.y-3` test A.travel uses, not a spot that killed this bot twice in 10 min), `onTick` re-raises while `dist/4 + 20 s < time left` (an alert goes stale in 30 s and kitUp runs first), `handle` travels read-only, picks up within 12 blocks for <=60 s, wears what came back and reports the MEASURED inventory difference; prio 70 (combat 80 comes first), one attempt per death, aborts at hp<8. (2) WALK PAST IT AND TAKE IT - `looseGear`: iron/diamond/netherite tools and armour lying within 12 blocks are picked up (gear only, never near settings.dump, overworld, once per bot per 5 min) - tidy's `litter` kind is BLOCKS, nothing in the army ever looked at a dropped pickaxe. (3) EVERY DEATH IS PRICED: one iron/diamond table behind `death {lost:{iron,diamond,items}}` (army_worker.js, 2 lines) and `recovered/scavenged {back:{…}}` - loss minus recovery is one subtraction | VERIFIED LIVE 15:39-15:52Z: `Hina recovered {at:[-740,81,56], items:{torch:8}, tookS:15}` (full alert path: raise -> job yields -> travel -> pickup -> measured diff); on a REAL triple death at the ravine rim `Hazuki scavenged {diamond_pickaxe:1, iron_helmet:1, leather_chestplate:1, …} back {iron:5,diamond:3}` in 8 s and `Nanami scavenged {diamond_sword:1, iron_chestplate:1, iron_boots:1, shield:1, obsidian:14} back {iron:13}`; brakes fired correctly on a Nether death and on a ravine-floor death. v1 gap: no Nether recovery (the portal trip outlasts the 5-min despawn)
- 09-20 15:3xZ | owner: 「工業化しないのか？在庫の有効活用はしないのか？」 - iron 132/800 and 105 armour pieces missing while 17 828 wheat / 10 328 wool / 2 995 cane / 1 876 coal / 188 leather sat on the shelf, and the nearest village (624 SW) had never been visited | INDUSTRY front, new extension module `bots/skills/lib/jobs_industry.js` (job type `trade`, verb `collect_farm`) + docs/INDUSTRY.md + a village zone row in docs/WORLD.md. `work:'survey'` = one bot PATROLS the village (Paper ticks a villager only within 32 of a player) and re-measures villagers/professions/beds/stations/golems every 2 min into `settings.industry`; `work:'trade'` = load glut → 624-block walk → sell to every villager that buys it → buy the iron gear the army lacks → bank, with restock/`tradeDisabled` respected, sleeping villagers skipped and never a hand raised at one | LOOKED (`look`, `mapshot`): 9 villagers, 10 beds, 2 composters + 1 blast furnace, 1 golem at -760,63,29, bounds x -766..-703/z 10..57; 0 deaths and **0 `no_route` on the whole 624-block route**. FIRST REAL TRADES: 320 wheat → **16 emeralds** (Tamaki), then 320 wheat + 96 leather → **51**, Yotsuba 320 wheat → **16**. THE LOOP CLOSED 15:53Z: with the new weaponsmith standing, `trade_done {villagers:4, sold:{wheat:375, leather:12}, bought:{iron_axe:6}, emeralds:36}` - **18 ingots of iron gear bought with wheat in one trip**
- 09-20 15:3xZ | the village buys wheat/carrot/beetroot/pumpkin/leather/flint and sells LEATHER armour only - no shepherd, no librarian, no smith, so wool, paper and coal are unsellable there and no iron is on offer (offers read out of the live trade windows, `events 20 trade_offer`) | `work:'post'`: the buyers we need are a RECIPE away - an unemployed villager claims the nearest unclaimed workstation, so the job crafts `loom` (18 wool→1 em), `lectern` (24 paper), `grindstone` + `blast_furnace` + `smithing_table` (coal in, IRON ARMOUR and iron tools out), `barrel`, `smoker` at the depot (two furnace passes first: stone and smooth_stone are smelted, not crafted) and places them on columns probed with `armyctl.js ground` on FREE grass at x -746..-738 / z 28..33. 7 ingots spent against 620 missing. Plus: the trade loop banks what the village did not buy instead of walking it 1 250 blocks again | **PROVEN 15:48Z: `station_placed grindstone -740,70,33` → 15:52Z `village_seen professions {farmer:2, leatherworker:4, weaponsmith:1}`** - a villager took the job, so the village now buys our 1 876 coal and will sell iron sword/axe at journeyman. Six stations to go (the first pass crafted only one: a bot carrying 800 wheat from an interrupted load made every withdrawal in the recipe chain fail with "inventory full" while the ingredients were in its hands - `work:'post'` banks its pockets before crafting now)
- 09-20 15:2xZ | the first trade really sold 320 wheat for 16 emeralds and this code reported `trade_none` and bought nothing | `bot.inventory` LAGS while a trade window is open (docs/DEV.md §3, the same trap as chests and crafting): every count between opening and closing a villager window now goes through `inWin(win,name)` - the sell size, the purse, and the read-back of what the server actually gave. Also: the next `openVillager` times out while the previous window is still open (wait for `bot.currentWindow` to clear), banked emeralds go back out as working capital, and a motionless surveyor was rightly called `hung` - it patrols the clusters instead | verified live: `trade_done {sold:{wheat:320,leather:96}, emeralds:51}`
- 09-20 15:4xZ | owner: 「採掘は非常に行っており…それなのに鉄が不足」「道がなくて進めませんが大量」 | MEASURED: 2580 raw iron mined in 20 h, 1891 forged into gear (66/61/64/70 armour, 68 picks, 66 shears), 21/21/23/23/17/8 still exist - 267 deaths ate ~1300 iron and nobody ran back for a kit; 366 `no_route`/30 min came from ONE 3-deep pit at the mine head that `fill_minehead_pits` read as `left 0` (and its `way` sent a bot in) -> 6 bots trapped 38 min, rescued = kits lost | steps plan `minehead_pit_stair_fill` (exit stair first, far end first, stair last): 44 steps ok in 2 min, columns re-read = y68, trapped bots all back at work; engineers: core/recover.js (+`lost` on every death event), army.js escape = pillar, not stair (trapped = island < 40, acts by itself); nether gates re-paired exactly at overworld/8 (-41,-65); armyctl.js REFUSES `job active`/`patch`/`put` on a `desc: OWNER-LOCKED` job (the foreman had re-activated west_terrace_cut_3 against its prompt)
- 09-20 15:3xZ | owner: fills are an ALGORITHM problem (「アルゴリズム力」「マイクラは実質的に競技プログラミング」): drop/bucket descent, dig a stair beside the pit, stair filled farthest-first, and for underground cavities top shaft -> bucket down -> fill riding up; gate: a bot INSIDE a portal cell cannot place and the landing is too tight (5 `rescue` kills in 40 min = 5 kits lost); nobody runs back for dropped kits; mine: EVERY level 48..-32 EXHAUSTED, 8 open faces for 15 miners at y-48 (diamond level) while iron is 88 % short | engineers briefed per file: army_jobs.js entries `dropIn`/`dig_stair` + farthest-from-entry order, fill_plan.js strategies + invariant, jobs_nether.js walk-out-of-portal + 7x7 landings, NEW core/recover.js (death run within 5 min), NEW jobs_cavity.js (census of enclosed dark voids + the owner's algorithm); board: `mine level 24` (fresh rock between the exhausted levels, iron band), maxBots 15 -> 20 | pending their live proofs
- 09-20 14:4xZ | owner: 「整地が穴を掘って置くを永遠に繰り返している」 - a CAP (`level` cap mode) and the FILL under it owned the same grade cell: the cap digs the fill's cobblestone (it is in its `only` list) and lays dirt, the fill re-fills the cell in the instant between dig and place, and `unlid` takes the dirt off again while the column is open. Measured: `build_runaway` cap_ravine_nm -323,68,-437 (7 digs in one slice), cap_base_yard_pad -368,68,-478, void_fix_base_yard_pad -368,68,-489 | army_jobs.js `build`: ownership is PER COLUMN and read from the WORLD (`ownedCols`, 30 s cache) - while an ACTIVE fill/pad has an unfilled cell in a column, the cap skips it (`owned` in build_pass, counted as waiting, never dug, and a cap whose rest is all owned steps aside 5 min instead of marking itself stuck); a cap never opens ground whose dirt is not in its POCKETS; the visible-material rule (EXACT) is explicitly STRUCTURES ONLY - a cell of level/fill_void/clear_area/quarry is done when ANY solid block stands there; `putCap` arms a new cap `paused` with `after:<the fill that owns the ground>` instead of racing it; the whole-job pause of 14:1xZ is gone | cap_ravine_nm active from 14:22Z: **left 1220 -> 113 in 8 min, done 59/pass, 0 `build_runaway`** (was 7 digs of one cell per slice)
- 09-20 14:4xZ | owner: 「12時間以上経過して未だに穴埋め終わって無いってありえない」 - the south ravine trench (x -306..-298 / z -461..-482, floor y52-55, sheer 14-high walls) had NO way in: `left 3616, waiting 2920, done 0` for hours, because the cell-choice rule never drops a builder 14 blocks and a `ramp` is a solid column only somebody already on the floor can raise | a LADDER RUN: army.js `climbRule` (the one ladder rule, applied to the live Movements too - a step down into a ladder cell is a 1-block move, not a 14-block fall, so the whole crew walks in and out on it) + army_jobs.js `ladderWay` - one builder (board claim `j.way`) hangs ladders on a wall face from the rim down, standing on what it has just placed (sneak holds a bot on a ladder, 0.15/tick otherwise), and the run is dug + filled like decor as the floor rises past it | `climbRule` live on all 50 (`__armyClimb` probed), ladders craft (`obtain ladder 6` = 6 on Ayame), and THREE failure modes were measured and fixed in turn: the entry (the goto ends while the bot still falls), the rung's line of sight (our own ladder clips it - every face is offered now), and the killer: mineflayer reads a ladder as `boundingBox:'block'`, so a CONTIGUOUS column is one solid wall the pathfinder can only stand on top of - the run is now a ZIG-ZAG over two neighbouring columns, a stair whose every rung has open air above it. Live: runs reach 2 rungs; the zig-zag went in at 15:01Z and wants a builder on the rim (fill_ravine_s was down to 1 bot, raised to 10). fill_ravine_s left 3618 -> 3216 meanwhile. Fallback if the next runs fail: the gravity ramp (136 gravel per entry, dropped from the rim with A.gravityDrop)
- 09-20 14:4xZ | owner: 「かまどや倉庫を一箇所にしないのか？倉庫を拡張しないのか？」 + the 14 build doubles 74 % full | ONE complex: `base_depot_south` (blueprint depot_rows, origin -372,68,-521, 3 `build` rows x 14 doubles x 3 high = 126 chests = 3402 slots) fills the flat gap between the depot's food row z -512 and the furnace row z -523 - same x lines, same aisles, 6 bots; docs/WORLD.md zone row added. Plus tidy learns FURNITURE LITTER: an unregistered furnace/table/chest/barrel that is no blueprint cell of ours and in no `settings` list is emptied, dug and banked (never within 1 block of a registered chest - the census reads the second half of our doubles as foreign) | predicate probed live on the real coordinates: the stray 2x2 furnace cluster -345..-344,70,-461..-460 + floating table -346,70,-460 = LITTER, every depot and mine-mouth chest = kept
- 09-20 14:4xZ | owner: "羊の世話ってなんのためにやってんの？こういうタスク無くせ" - wool 10 328 / target 150, leather 187/64, feathers 384/64, food 16x, cane 6x | husbandry and surplus jobs REMOVED from the board (herd_*, wool/craft beds, cane_wild/lake, forage, fish, bake, farm 2/4/5/7); kept: farm_1/farm_6 and cane_farm at 0-n by demand, hunters cull strays | rule: a job whose product stands > 3x over target has no place on the board
- 09-20 13:52Z | dispatcher churn: 1203 ASSIGN/h = every bot changed job every 2.5 min (walk + handover bank + interrupted slice); median stint 115 s, muster<->tidy_spawn 285+284/h, fill_ravine_n held 0-1 of 16 bots | dispatcher.js: tenure that holds in both passes (`holdFast`, broken by an idle heartbeat task), no squad drained below half (`drainable`), pool sourced muster/fallback/overflow first then the lowest-priority squad, a job that 3 bots decline for the same reason RESTS 10->30 min, a bot that declined a job 3x is not sent back for 20 min, surplus bots OVERFLOW into a terrain squad with open cells instead of the sponge (`roomFor` = open cells/8, cap 2x `bots`), a head-count falls at most 1 bot/min (`headFall`), plus dimension filter (Nether bots never get overworld jobs), herd demand from `audit_pen.outside`, `maxFronts = max(10, online/4)` | 1203 -> 237 assigns/h (4.7 per bot-hour, target < 6), 0 bots at muster, best window 9 moves/10 min; `assignsPerHour` + top flows now in status.json/BOARD.md and a CHURN line every 10 min
- 09-20 13:50Z | 6 bots "tried to swim in lava" in 2 min on one job (lava is in blocksToAvoid, but the pathfinder walks ALONGSIDE it, cuts diagonals over it and drops onto ledges beside it) + the far gate stands on a ledge with no walkable bearing | jobs_nether `nTravel`: every Nether walk gets an exclusionAreasStep pricing lava within 2/3-below/2-above at 100, maxDropDown 1, no parkour/tower/dig, `allowSprinting` PINNED false (A.travel re-asserts it from the larder every trip), bridging only from a safe stand · NEW blueprint `nether_stair` + `work:'stair'`: a 2-wide 3-high roofed lit corridor from the hub doorway down to y33, DUG through rock and BUILT over void by the same cell list | **0 deaths since the lava rule** (was 10 in 45 min); stair 490 of 1330 cells, 43.7 blocks/bot-min; obsidian 2 in depot so the 2nd gate waits on `mine_obsidian` (re-activated)
- 09-20 13:45Z | owner: "ネザービビりすぎじゃない？" then "ゲートの周り囲みすぎててハングしてる" - 12 scouts, 4 bearings, and not one got 20 blocks out | `work:'fortress'` (4 squads by roster index, 50-block legs in <=16-block hops - a 50-block A* goal over broken Nether terrain always fails -, <=4-cell bridges, `nether_scout` per leg, sightings converge the squads), death brake now 6 in 30 min not 2 ever, scouts cut their own way out (`openDoors`: four plain 2x2 doorways + landings + torches, the fence gate withdrawn), `explore` aborts the moment the gate sends a bot home (one scout "explored" 563 blocks of OVERWORLD and filed a dungeon spawner as a Nether sighting) | NOT SOLVED and now MEASURED: the far gate stands on a ledge - 42 of 81 columns within 12 blocks have no floor within 4 below, the Nether floor is ~65 down. 10 deaths/45 min, every fall at y24-38. Needs a decision: relocate the gate (2nd overworld gate 128+ away) or build a stair down; docs/NETHER.md
- 09-20 13:15Z | 6 miners standing on their own branches (Aoi 88 min), mine_iron 11 bots -> 364 output/10 min | LOOKED: level 0 branch 56:1 had driven into an abandoned MINESHAFT - an oak fence in the floor cell is `solid` to the block API but 1.5 high, so stepTo could not enter and mineBranch returned `stuck` every 20 s while claimBranch handed the bot its own branch back. openCell/mineBranch now take a fence/wall/rail/web out of the floor and re-floor the cell (walkLine always did); a `stuck` face is released at once, skipped 10 min, and closed by the second miner. Plus: growth opens a landing when the band has no FREE branch (claimed counts as none), and iron_core `inHand()` verifies the held block before every place (sword/placeAt bug) | branch 61:1 len 17->56, 66:1 2->36; gemba: no mine bot in STILL, output 1122/10 min from 5 bots, useless 9 -> 1
- 09-20 13:13Z | owner: low TPS - our LIT GATE had spawned 74 zombified piglins in the overworld (outside the mob cap; the bots rightly never attack a neutral mob) | `portal` job `closeGate`: the bot that comes home LAST takes one frame obsidian out with a diamond pickaxe, reads the six inner cells back as AIR and puts the obsidian straight back (`settings.nether.lit` is the truth, `params.close:true` = job `nether_gate_out` by hand); no bot crosses while `settings.restartPending` | verified 13:12:25Z `portal_out {was:6, cells:0, frameBack:true, gaps:0}`, probe: 6/6 inner cells air, 14/14 frame cells right, 1 piglin left in view
- 09-21 06:0xZ | NETHER front: bartering could never have worked, and the reason was terrain nobody had looked at | `nether_barter` had crossed and come home **36 times with 0 trades** - the old test compared the bot's CURRENT position with the hub chest and the arrival cells are 10 blocks from it. Flew `SkyEye` under the bedrock roof and read every column: (1) **no mob spawns within 24 blocks of a player**, and the whole 383-cell arrival shelf is inside 24 of a squad standing on it, so no piglin can EVER appear there - the census found 0 of 6; (2) all 6 piglins and 17 of 69 zombified piglins stand on ONE other ground, 291 cells at x -69..-52 / z -83..-47 / y100-116, roofed by natural rock at y113; (3) the cheapest walkable line to it is **26 placed blocks** (Dijkstra over the camera's solid/air map). So bartering got a place: `work:'barterspot'` (`laneCells`+`padCells`) builds a 3-wide kerbed lit lane + a 9x9 pad at -65,110,-72 levelled to the MEDIAN ground (652 cells, 213 to place, 22 to dig, 0 lava - dry run against the camera map), registers `settings.nether.barterSpot`, and `work:'barter'` walks there and reports `nether_barter {offered, pearls, per64, pigsSeen}`. EDGE DOCTRINE HOISTED PROPERLY: sneak was decided ONCE from the two ends of a walk, so a hop that set off from a wide shelf and met a 1-wide arch nine blocks later walked it upright - exactly the ground that killed Aoi, Fuuka and Koharu at -54..-55,28,-80 in two minutes; it is now re-asked every 400 ms of every Nether walk from the cell the bot is standing in, with the lava picture refreshed on the same beat. Also: rcon `locate` fixes the fortress at **-784,~,-592** (899 blocks) and a bastion at **-320,~,-352** (387); `nether_pair` and `nether_dead_frames` paused (both could only decline - obsidian 0 in the depot, no sword - while queueing for the gate the lane needs); a `portal` job needs `priority`, not `prio`, or the dispatcher never staffs it | STILL 0 pearls, 0 rods: the lane is building, `nether_barter` re-activates on `barterSpot.built`. Gate throughput is the next code gap - 4 bots on one portal job jam each other (`sharingCell:1`, `stood in the gate for 30 s`)
- 09-21 05:5xZ | owner: 「地面を埋める石は何でもよいが一番上は土でなければならない / 最終的に露出するブロックはこだわり、それ以外は多少雑でも良い」 - and the corollary he added at once, 「ただ、現状は土被せがないとマイグレーションされないからね」 | army_jobs.js: a TERRAIN blueprint's buried cells take ANY stone in the pockets and never wait for a material (that half already stood: TERRAIN_BP is exempt from EXACT), and the SKIN - the top cell of every column plus the outer ring within 1 of grade, i.e. a raised pad's visible flank - is laid by the FILL ITSELF in `args.top` (default dirt), exactly, never a substitute, swapped on a finished column only when the block is already carried and the column under it stands; the skin material is fetched by the stack, the body not at all; roads and structures keep EXACT everywhere · the `cap_*` jobs therefore are NOT retired but RESHAPED into a migration: `args.cap` work list = the camera's own `base_audit.json -> surface.wrongCols/rubbleCols` (a column not on the list is not the cap's and is not counted as `left`, so it pauses itself when its area is clean), each column re-read in the world, never a column an ACTIVE fill still owns | proven on cap_base_mine_pad before the wiring: **339 blocks in 11 passes, then it paused itself**, and the three old dig/place cells were RETIRED (`build_nodig`, owner:null) instead of pausing the job - 32 runaways in that window, 0 whole-job pauses. Migration size MEASURED per cap: north_tiles 190 · core_pad 137 · yard_pad 72 · ne_valley 45 · mine_pad 19 columns
- 09-21 05:2xZ | THE SOUTH RAVINE, 20 h old (owner 「二度手間って言葉知ってる？」,「深い穴がいくつも空いている…バケツ着地からの穴埋めとか砂利を流し込むしか無い」): `fill_ravine_s` auto-paused on -333,58,-483 and `cavity_base` ran at 0.6 blocks/bot-min | PROBED the cell first - it is a TORCH sealed under ten blocks of the fill's own cobblestone, so the dig is `unreachable` for ever, `decor` only earns a 180 s `lockSkip`, and `todo(0,true)` (the walk a starved fill takes EVERY time) ignores lockSkip and st.bad by design: the blacklists were void exactly when they were needed. army_jobs.js: `job.noDig` on the BOARD (a cell this job dug twice is never dug again, 4 h, honoured by the `all` walk too) and `build_runaway` now PAUSES only when another ACTIVE build job owns the same cell - it names that job - and otherwise retires the one cell (`build_nodig`) and keeps the crew working · the owner's POUR RULE: an open column > 4 deep with <= 2 open neighbours is handed out as ONE cell and emptied from the rim with gravity blocks (`fill_poured`), gravel fetched 256 at a time instead of 32 · LAVA is quenched with water from a spout wall 3+ above the pool (never a stand beside a source), and a pool under a closed roof is retired instead of being re-discovered by ten builders every five seconds · jobs_cavity.js: TWO dead references the previous owner left - `G.lava` was never created by `newGrid` (every survey threw, the job was 100 % dead) and `rideOut` was called and never written (`error: rideOut is not defined` killed 7 of 12 passes with placed:0) · a failed approach costs 6 s not 15, two tries not three, and six barren rounds end a hole (`fillS` was 193-291 s of 238-318 s passes) · owner's two later rules the same session: the cavity worker's x/z COLUMN is its own and nothing above its feet (or a mate's) is ever placed from below - that is exactly how Kokoro buried itself at -332,45,-483 under 19 blocks - and `voidSize()` MEASURES a void before anything is decided (tiny/small/big/cavern, shared with the miner's `why:cave`) | RAVINE BOX HONEST SIZE (318 air components, the estimate the owner has been waiting for): tiny 278 comps/482 cells, small 27/240, big 5/121, **cavern 8/2558** - and 2514 of those 2558 are TWO components (1344 at -329,50,-486, 1170 at -334,40,-486) that are the OPEN TRENCH itself, fill_void's work, not holes. The closable hole front is 310 components / 843 cells; its cost is 310 shafts, not the blocks | fill_ravine_s done/pass 0 -> 12 and no longer self-pauses; cavity_base 0.2-0.3 -> 1.6-6.7 blocks/bot-min (12 finished passes in 30 min against 1-2), `rideOut` errors 7/10 min -> 0, census alive again (85 components, 1480 cells, coverage 100 %); ravine box MEASURED open-to-sky 2594 / enclosed 895 / lava 176; deaths 0. NOT REACHED: the lava pool is ROOFED (`pours` 1-3, `spouts` 0) so no water can reach it - 176 cells need an operator-planned unlid from the rim; several bots per cavity component still not written; buckets 13 in the army, 0 in the depot, iron_ingot 9 (the quench is supply-bound)
- 09-20 13:10Z | P5 stage 2: the far side had no floor, no room and no road, and every builder that MOVED over there died | `portal` job gains `params.work` (one round trip per slice, never a bot left on the far side): `landing` (the 5x5 read back until safe), `hub` (NEW blueprint `nether_hub`: walled + roofed + lit 9x9 room, chest + crafting table carried over and registered under `settings.nether` ONLY), `road` (NEW blueprint `nether_road`: 2-wide COVERED walkway - movement stays read-only, what is built is a road everybody reuses), `scout` (sightings to `settings.nether.sightings`) + `safeStep`/`placeStill` (blocks.js direct: `A.placeHard`'s remedies walk, and that is what killed the hub builder) | landing **safe:true**; hub **left:0 of 413 cells, 76.6 blocks/bot-min, 26 s away, chest + table STAND**; road NOT built (2 deaths total on the far side, both diagnosed: placeHard's reposition, and a mob in the un-gated hub door) - 3 measurement bugs fixed (hub record clobbered by the landing step, unloaded chunks counted as finished, step candidates sorted by the wrong distance)
- 09-20 12:5xZ | the depot was empty while the pockets were full (cobblestone depot 36 / carried 3743, dirt 0/392, `build: no filler|no dirt` x72 per 10 min), the kit handed out the PLAINEST tool only when a slot was empty (19 of 50 on wooden/stone picks, 41 on stone shovels, 19 diamond picks on the shelf), and an unstackable craft batch died at run 1-2 | army.js: `offload()` in travel() takes material above a personal 16 home whenever a trip passes a build chest (jobs that CONSUME filler keep their keep-lists), kitPlan makes tools an UPGRADE path with a fair share by NEED (the neediest mate is ahead of me, equals are first come); craft.js crafts a whole batch in ONE window, counts results over all slots, checks a free slot and treats a click Paper never confirms as "look at the window" instead of a failure; larderFull's sprint gate is now min(256, targets.food/2) floor 64; army.js is dimension-aware (dimOf, travel {dim}, depot/kit/escape refuse off-overworld, blueprint cells keyed by world) | 30 min later: 1839 blocks banked by 13 bots, filler declines 72 -> 23 per 10 min, 46 of 50 bots on iron-or-better picks (was 31), 21 diamond shovels out of the chest, 206 short-batch craft reports in 3 h -> 0 in 25 min
- 09-20 12:4xZ | gemba: fill_ravine_s 32 bots, NOT ONE cell in 10 min (no_los 229, unreachable 129) - the rest was a sheer 15-deep trench and the new rule never drops into a pit: no way in | `ramp` x -301/-300, z -461..-475 from the finished middle segment, crew 12 | 0 -> 6.2 cells/min/bot, left 5407 -> 4284 in 6 min, still 15 -> 8, useless 15 -> 5
- 09-20 12:35Z | **P5 STAGE 1 CLOSED**: the round trip. Both earlier scouts died in a step that MOVED them on the far side, and the army was DIMENSION-BLIND (a bot in the Nether was handed `tidy` and dug herself in under the roof at y123) | jobs_nether.js: arrival is made safe WITHOUT WALKING (`sealNear` + `landing` = 5x5 floor, walls 2 high, door gap, torches, all at arm's length, `safe` READ BACK from the world), step out/step in for the portal cooldown, `nether_return` (`dim:"the_nether"`, `settings.nether.returnJob`), 2 deaths at a gate pause the job · army_jobs.js (3 sanctioned places): `overworld(bot)` - `muster` stands still off-overworld, `withHandover` skips bed/canteen/banking/respawn-bed/upTheStairs there | **`portal_back` VERIFIED twice 12:29:54 + 12:30:51Z** (Kanade, 0 deaths, the_nether -38,98,-76 -> overworld -326,69,-518); guard proven live: 0 banked in the Nether, 208 cobblestone banked 7 s after coming home; landing reads `hotNear 0, holes 2, safe:false` (2 floor holes, 1 out of reach) -> stage 2 in docs/NETHER.md
- 09-20 12:20Z | P5 had no code at all: nothing in the army knew a second dimension exists | blueprint `nether_portal` (4x5 frame, 10 obsidian, 2-deep plinth, paved lit apron, STEPPED shoulders - an isolated 1-wide post can never be topped out from the apron, a block is placed against the TOP FACE of the one below it) + job type `portal` in the NEW extension module `lib/jobs_nether.js` (obtains flint_and_steel, strikes the gate, crosses, seals lava at the arrival WITHOUT walking, shells, comes home; 2 deaths at one gate = the job pauses itself) + `nether_return` (`dim:"the_nether"`, `settings.nether.returnJob`) + docs/NETHER.md | VERIFIED: frame 14/14 at -328..-325,68..72,-518, `portal_lit` 6/6 cells, `portal_through` x3 to the_nether -38,98,-76 (ViaBackwards takes the dimension change cleanly). NOT verified: `portal_back` - the far gate opens over lava at y98, 2 scouts died, and the dimension-blind dispatcher/tidy dug a third under the Nether roof (docs/BUGS.md 12:1xZ). `base_portal_go` PAUSED, 0 of 50 bots off overworld
- 09-20 12:2xZ | owner: "半日以上経っているのに進捗率が悪すぎる / 明らかに地形が悪い / 渓谷埋め、座標指定がおかしいのでは" - the ravine crew spent its slices getting walled in, three 40-block lines of filler stood ABOVE grade, and the base reads as grey rubble | army_jobs.js `build`: pit ordering by TIER (my own layer within 8 blocks first, one step up second, a tower third, cells more than 3 below my feet last) + `wallsIn` (never a block one above any bot's feet in the 8 columns around it) · `fill_flush` (a fill with nothing to do sweeps the 2 layers over its own grade and digs its own filler back out) · `build_tail` (a crew is sized by the cells that are OPEN NOW, 8 per builder, the rest hand themselves back) · a fill WITHDRAWS its filler, never crafts it, takes a pocket-load per trip and never draws a substitute below `settings.targets` · `fill_complete` on a filled keep-out · a finished terrain job puts its own dirt `cap_<id>` (blueprint `level` cap mode, `needStock`) · a VISIBLE face takes the blueprint's own block (audit_surface 705) · tidy ignores its 30-min tile memo while the camera's list is fresh · a farmer with the larder over target declines instead of waiting 90 s · quartermaster: iron/diamond tools, bucket, shears, bow, arrow, shield from the SURPLUS | fill_ravine_s 0.2 -> 25 cells/min/bot (left 10305 -> 5655, ETA 1982 -> 29 min), escapes per cell 0.35 -> 0.015, deaths 7/30 min -> 1/26 min; y69 strays 114 -> 0 (audit STRAY alert gone); tidy_pass 6/10 min -> 101/2 min; NEW BOTTLENECK: depot cobblestone 0 / dirt 0 against 926 + 582 carried - every terrain job is supply-bound (BUGS 12:2xZ)
- 09-20 12:05Z | day-one rules still steering a 50-bot army (owner: 「初期の頃必要だったモジュールとか更新するべき」): food/animal jobs pinned at `minBots>=1` on 20x food, the quartermaster able to craft only STONE tools, `targets` a 3-bot shopping list with no book/tool/filler row, 19 of 50 bots on a wooden/stone/no pickaxe beside 353 idle diamonds | systematic review of all 87 jobs + every constant in army_jobs/army/feed/craft/iron_core/dispatcher/stock + the daemon prompts -> `docs/REVIEW-early-assumptions.md` (16 ranked rows, each with the exact constant and new value); BOARD applied: `minBots:0` on 13 over-stocked producers, targets +book 48/+bookshelf 15/+sugar_cane 512 and planks/stick/torch/cobblestone/dirt raised to x50 numbers, `cane_lake` paused, `hunt_spawn.leave` 0->2, new `armoury_diamond_tools` steps plan | 10-12 bots freed from deficit-0 jobs, staffed 29->36-39 of 50; mine_iron 11->13; depot pickaxes 0 -> 26 diamond + 13 diamond shovels; first 6 bookshelves ever crafted (P4 was logged as done with 0 books in the world). Top-3 code gaps handed on: kitUp hands out the PLAINEST tool, `CRAFTED` is stone-age, the tidy sponge's 30-min tile memo blocks a fresh audit's 274 columns
- 09-20 11:40Z | 5-7 miners stuck at one y32 branch cell, every recall `budget` (iron squad half idle) | a zombie in a cave pocket 3 above the line held defend() 25 s per walkLine pass: defend disengages after 3 s from a mob out of reach (`mine_mob_ignored`, 2 min), fight/hold time no longer eats the walk budget, toSurface legs scale with the measured route | mine_blocked/hung/recall_failed 7+3+3+9 -> 0 per 15 min, 21 banked, recalls 3/3
- 09-20 11:40Z | class-specific audits only see the classes they know; the owner noticed every slow/ugly thing FIRST (25 bots on a 150-cell tail, fills at 0.8 cells/min/bot, farmers waiting with food 20x over target) | ops/gemba.js is now a library + CLI with two CLASS-AGNOSTIC yardsticks (per-bot output in 10 min; `left` per build job over a 24-sample ring in bots/metrics/gemba.json); the inspector runs one 60 s watch every 10 min into REPORT.md § GEMBA (top), status.sh prints it first, `wait` gained slow_job/army_still/useless_bots, escalate.sh kind `gemba` (a `!` line standing 30 min), foreman+operator must answer every `!` line with what they SAW and CHANGED | live: REPORT § GEMBA shows SLOW fill_ravine_s 1.4 cells/min/bot vs player 60 (ETA 9 h), USELESS 8 bots, STILL 21/47; digest and `escalate.sh check` verified
- 09-20 11:30Z | our own jobs undoing each other: pads "complete" over voids, farmers eating the field's water-cap posts, builders entombed/lava deaths in the ravine fill | army_jobs.js: a finished terrain pad PUTS its own `fill_void` (measured `unlid`, `after`-chain, 2 rounds) · `farmSoil` - a farmer only works columns its zone's blueprint calls soil, the build job may take a crop off a cell it paves · solid fills never wall a mate in, a boxed-in builder rides the fill up, lava cells quenched from above/2+ away, pinholes reported instead of "complete" | base_infill_n278_n401 12 void columns -> 0 (clean complete); base_field_6/7 9+9 missing posts -> 0; 0 of 50 bots at walkableArea <= 3 (was 3 at area 1); ravine deaths 9/40 min -> 0/8 min
- 09-20 11:25Z | place_failed storm (25 bots on one cell, 119/30 min) + obtain crafting too little + no sensor for entombed bots | army.js: army-wide 10-min rest ledger for cells that beat 2 bots (`resting`, one event per rest) · obtain in passes solved on the POCKETS for the full n, honest failures, silence when interrupted · `entombWatch` in heartbeat (area<=3 for 60 s -> `entombed` + `boxed` in the heartbeat) · withdraw takes the nearest container that covers the need | place_failed 30.7 -> 6.3 /10 min (geo 13.0 -> 3.6); `obtain stick 64` 63/64 -> 64/64; withdrawal measured: 23.9 s of 24 s is walking, the container opens in 0.05-2.3 s; entombed: 0 of 50 bots boxed (no false positives)
- 09-20 11:2xZ | owner: tasks far too slow; bots entombed in the fill; flower pinholes | measured: 63 % idle, 1210 ASSIGN/h, fill 0.8 cells/min/bot, bedtime ~10 % | order:'near' (x15 on ravine m), bedtime 6 h, terrace cut done in 20 min, `audit_fill`, prank tuned (cap 12, half health, 3 min life, skeleton 15 s / horde 90 s); opus: churn, ride-up, entombed sensor
- 09-20 10:45Z | iron 119/224: a worked-out mine level waits for an operator to open the next one (y16/0/-16 at 160 branches x 256, squad 220 blocks out) | iron_miner `growLevel`: every dug level of the iron band y-16..48 EXHAUSTED -> the miners open the nearest unused landing themselves (board `params.args.level` + rev, `mine_level_opened`, at most one per hour); `armyctl.js mine` now says EXHAUSTED and the mean blocks out per level | crafted-cache dry run opens y48 (160 free branches); field: y32 still has 53 open branches, watch `mine_level_opened`
- 09-20 11:0xZ | sticks: obtain_failed x74/30 min with 2821 logs (stale index entries counted but never visited) · dirt 36 in the depot, no producer | withdraw falls back to stale chests · `targets dirt 768`, hill cut n252_n558 + escort re-activated | stick craft verified; dirt: watch `stock dirt`
- 09-20 10:5xZ | Chika trapped 16 deep (no escape ran) · fill_ravine_n `done:0` x393 | walkableArea = surface water only + digOut climbs deep open pits flight by flight · fillCell pulls flowers, `occupied` rests 5 min | Chika out and hunting; ravine 0 -> 70 cells/3 min
- 09-20 10:1xZ | warning storm: 38x no_route from ONE hunter in a low river 150 blocks W | verb `drop`, 3 supply runs from the rim, stairwell job | NOT solved (canopy catches the items; A* stops at 45 nodes) - bug filed, bot parked in `chika_hold`; 7 idle bots -> tidy 22
- 09-20 09:5xZ | 12 bots no_route on the ravine floor (no walkable exit, segments at different heights) · iron squad on exhausted levels | walk-in stair as `ramp` of fill_ravine_n + `base keepout exit`, m paused until level · `mine level 32` | route foot->rim verified; mine: watch `stairs_done`/hub on y32
- 09-20 09:0xZ | 8 of 50 bots hungry WITH food carried (no regen, no sprint) | meal reflex every 4 s inside slices + digest `starving_fed` | 8 -> 2 in 2 min, rest after the rolling restart
- 09-20 09:1xZ | PUBLIC repo github.com/fa0311/minecraft-bot-army (`ops/git.sh publish`, guarded); world-1 records gathered in `attic/world1/` (local only), the LIVE finished-jobs archive moved to `bots/army/jobs-archive.jsonl`, guest names to untracked `server/guests.json`. Owner: "花の除去が出来てない" -> `audit_weeds` (268 on the first run) + tidy kind `weed`. NE valley: `fill_ne_valley` stone sub-base before the dirt cap.
- 09-20 08:00Z | the sponge worked from its memo while the camera saw 470 off-level columns; `no_los` storms on level builds; floating crowns over fields (owner); growth audit cried wolf | base_audit.json = tidy's work list (`work`/`strays`/`floats`, file-lock claims, `tidy_fix` verified); stands chosen by sight + walkability, fill from inside / gravity drop, no tickets for geometry; `audit_floating` + tidy kind float, a trunk in a terrain cut is felled whole (`tree_cleared`), fell finishes crowns over our zones; cane/lumber growth judged against their own clock, ripe >= 50 % = backlog | rough 438 -> 320 columns and floating 942 blocks/25 clusters -> 223/8 in ~25 min, place_failed no_los 136/30 min -> 0, wood_spawn no longer alerts
- 09-20 07:50Z | **P4 CLOSED**: enchanting table at -344,69,-517 with 15 bookshelves (verified in the world), diamonds 371, obsidian 44, diamond picks for everybody | next: P5 Nether (portal, hub, blaze rods) - code to be written; meanwhile the ravine fill, infill levelling (77/88 tiles done), beds (49) and the every-bot-sleeps routine close P3
- 09-20 07:20Z | EFFICIENCY (owner: "惜しみなくやるべき - 階段化/スコップ・斧/走る") | measured: NO bot ever sprinted (terrain_guard's 2 s timer reset the flag) → v7, sprint 0 → 77 % of moving samples, flat road 3.96 → 5.6 m/s · mine stairwell becomes real stair treads (cobbled_deepslate_stairs, laid by commuting miners; no jump per step): y69↔y16 round trip 58 → 34.5 s · ravine workers are no longer "in the mine" (95 reconnect tunnels/h → 0) · the mine's home lane is mended by whoever walks out (mineshaft fences/rails/cobwebs) · dispatcher TENURE: 982 job switches/h (every bot every 3 min, 13-21 % of bot time lost) → a bot holding a job < 10 min is not pulled by another (urgent ≥ 96 excepted), leftovers in a second pass · hung watchdog counts inventory change as work · /trigger tp lists ALL players; /tps for everybody | churn to be re-measured after an hour
- 09-20 07:05Z | LEGS (owner: "効率を上げる作業は惜しみなく - 階段化 / 道具 / 走る"): sampler of all 50 bots = 64 % of bot time MOVING at 3.96 m/s, 0 % sprinting (`larderFull` sprint never reached a running bot: terrain_guard v6's timer kept its own closure and reset `allowSprinting` every 2 s); mine stairs = one JUMP per step (1.6 steps/s up, the physics' jump cadence); 95 `mine_reconnect` tunnels/h from the RAVINE floor into the stairwell (overhang = "roofed" = "in the mine", ~35 s of every fill slice); 19.6 job switches per bot-hour | terrain_guard v7 (timer + wrapper call the LIVE module; re-installed through `strictMovements`/`travel`, sprint asked per trip, food > 6) · iron_core: TREADS = `*_stairs` blocks as step floors (`stairCells().treads`, laid by climbers from the step below looking up the flight, both lanes, hollow floors hang on the neighbour, 32 stair blocks in the miner's kit while `treadsLeftBy[level]` > 0), no jump on a tread, sprint in the walled stairwell and on looked-at trunk/branch cells, `feet()` reads half blocks · a bot in a `settings.keepOut` hole is never "in the mine" (army_jobs `upTheStairs`, iron_core `underground`) · army.js `pitExit` (pockets under our own paving: lid opened from below, column filled flush, lid material last) + `trimPillar` (no pillar above the ground beside it) · crater-ledger reader tolerant · base_field_1/3 paused (dead water claim: 47 declines/10 min) | flat road 69 blocks: 3.96 -> 5.6 m/s, sprint share of moving samples 0 -> 77 %; stairwell y69<->y16 (53 steps): down 25.9 -> 18.5 s, up 32.4 -> 16.0 s (1.6 -> 3.3 steps/s), round trip 58 -> 34.5 s; ravine reconnects 25 -> 0 per 10 min; Yui/Tsumugi out of their pockets after 35 min. OPEN: dispatcher churn (docs/BUGS.md 06:55Z) is now the biggest walking sink
- 09-20 05:58Z | one miner died in lava every 3 min at the same face (-32 67:1, 11 deaths = the army's whole death rate) and the mine cache was wiped twice by an entrance flip-flop | iron_core: 2-cell lava/fire look-ahead on every line walk (plug a trickle, wall off a lake, never step in), branch `hazard` ledger + repair claim, a dead bot never resumes that branch, per-key cache set-aside, lost ledgers re-learned by walking open galleries | no lava death since 05:34:48, level 16 (160 branches) re-learned in 10 min, deliveries continue
- 09-20 06:00Z | the owner saw what we did not (escaped sheep, strays by the depot, bumpy base, dead furnaces, sunken fields, junk picked up again, no shovels, idlers) | PERCEPTION: `ops/base-audit.js` (spectator camera, LLM-free, every 30 min: surface vs base level, stray blocks, heads inside/outside pens, furniture registry vs world, fields, structures vs blueprints, growth + loaded share, zero-output bot time) → REPORT, foreman prompt, escalation · CAUSES: escape digging ran inside our own buildings and after death (steps left in pen 1 → 76 sheep out; 26 strays by the depot), prune hid finished pads from the pad audit, a creeper's 5 furnaces fell under the damage threshold, junk was dropped at the depot and picked up again · CHANGES: `ours()` registry (no escape edit/placement inside our structures, none after death), 88 infill `level` tiles, `cane_block` inside the base, dump at the ravine rim, shovel/axe shelf + getTool, sprinting while food ≥ 256, ravine + quarries being FILLED (3 jobs, 12 645+ cells), hunt pigs only with a pen-safe rule | pen 1: 7 → 29 sheep and rising; furnaces 20/20; drops at the depot 60/min → 2/min
- 09-20 05:50Z | the owner saw sheep outside the pen, stray blocks, a bumpy base, furnaces gone, bumpy fields, a far cane farm | ONE registry of what we built (`army.js ours()`): escape edits never touch it and die with the bot, blocks.js refuses `pen`/`ours`, tidy `stray`, herder `pen_litter`; audits read the archive (pads, furniture, craters); `plan-base` reads the archive, emits 88 INFILL pads + zone `cane` (blueprint `cane_block`); farm levels its tiles; obtain retries on logs; containers are approached from the aisle; junk goes to `settings.dump` (ravine rim), never to the ground of the base; `getTool` shovels/axes + quartermaster shelf; the craft table can no longer be re-registered from afar (it sat in the ravine: every craft failed) | pen 1 clean, flock 7 -> 29 and rising, 20/20 furnaces, 9 infill tiles done in 10 min; mine branch ledger lost by my --put (entrance moved 60 s) - levels/stairs restored
- 09-20 05:45Z | PERCEPTION: the owner saw escaped sheep, junk blocks, a bumpy base, furnaces that were air, idle bots - no report did (we only knew what bots say about themselves) | `ops/base-audit.js`: spectator camera at 1 block/px + books, plan vs WORLD every ~30 min (inspector = clock, foreman runs it first): audit_rough/stray/pen/growth/furniture/field/structure/idle -> REPORT `BASE AUDIT` + STANDING headline, foreman prompt + picture, escalate kinds, `events 20 audit` | first run (36 s) found by itself: sheep 6 in / 83 OUT + 4 deepslate + open gate in pen 1, 5 furnaces = air, 25 stray blocks (hall, field_1), 483 off-level columns + 11.7k never-levelled, sunken farmland in fields 1/6, 33-37 % standing bot time; the old cane farm measured LOADED 100 % (19-05h) = its slow growth was not unloaded chunks
- 09-20 04:30Z | phantoms 2-4 kills/h, depot torch 0 at "583/224", pads "complete" with low columns, roads between 4-block walls, REPORT "0 bread baked", depot of 70 SINGLE chests | every bot owns a dorm bed and lies down every ~3rd dusk (bedDue/bedtime in withHandover); quartermaster judges supplies in the DEPOT (+ceiling 2x), `targets` shows depot + carried; padAudit per column + mine stairs are not terrain; road.js terraced shoulders (`natural` air cells); craft verb reports `cooked`, inspector bread ledger of any job + 49 beds; placeChest/redoChest (look BEFORE the click, sneak-click the partner) | 5 bots slept in own beds 04:22:50Z; torches crafted 4 rounds, depot 0 -> 93; 4 uneven pads found and re-run; road 2 profile 71-70-69|68|69-70-71; depot 42/42 double chests
- 09-20 03:30Z | P4: obsidian | miner mode `job:'obsidian'` (diamond pick + 2 water buckets; pours on the shore of a lava lake met by a finished branch, waits, scoops the water back, trenches the obsidian with solid ground under every cell, re-seals the shore; real 9.4 s dig time) · mine GROWTH rule (new mouths to k 80, branches to 256, miners pick the iron level while iron is wanted, `mine_exhausted` once per hour) · stone stays at the face when stock ≥ 2x target · interrupted climbs are no longer `mine_blocked` · the hung watchdog resets while a bot is excused | obsidian 40 banked (enchanting table 4 + portal 14 covered); level 16 80 → 160 branches; one miner lost to a skeleton in the -54 trunk with 11 obsidian
- 09-20 02:50Z | P4 chain in place | job type `cane` (shore cells touching water, harvest leaves the base standing; wild trips until the farm holds 32) · herd: shear → cull with a breeding cap (`max`), sheep byte at metadata 18 · quartermaster crafts paper/book/bookshelf/table to targets when feasible · paused `enchanting_corner` plan in the hall · cane/paper/books banked as salvage · a lost registered craft table is replaced by the nearest one | pen 1 went 111 → 10 sheep with ~230 wool, dorm asked for 50 beds; cane_farm 8 plants (wild cane is scarce: ~3 h to 138 by replanting); still missing for the table: 138 cane, 4 obsidian (engineer at work), 15 leather
- 09-20 01:55Z | P4 half met in one evening | 12 miners worked levels y16 / 0 / -16 / -54 out (80 branches x 128 each): diamonds 198, gold 159 + 99 raw, redstone 1320, lapis 1526, 3 diamond pickaxes; level -32 opened; bug-line wake-ups batched to one per 20 min (URGENT wakes at once) | open for P4: enchanting table + 15 bookshelves (sugar cane at -270,63,-343, leather from the cow pen) and ≥ 10 obsidian - two Fable engineers at work (cane/cull/books · obsidian casting in the mine)
- 09-20 01:35Z | P2 done in substance, P4 begun | mine: stone picks got `needpick` on two redstone ores in the -54 trunk and mis-marked 66 branches → work check before the kit, other lane, levels by pick tier; forge makes iron pickaxes/shears FIRST and keeps 32 ingots before more armour; herd job rests as a JOB (`restUntil`) when nothing can be lured; glut rules (seeds, stone) run BEFORE the chest visit; furnace round always carries logs while fuel is short; interrupted bank visits are not 'unreachable' | 12 iron pickaxes, 8 shears, 10 iron armour sets, 4 diamond pickaxes + 1 diamond sword, food 3332, stone 22069, pens: sheep 5, cows 7+, chickens; deaths/1h 0-4
- 09-19 23:57Z | P1 CLOSED (food 658/448, bed + sleeper skip nights, base lit, deaths/1h 0) · P3 started | new job type `herd` (lure with wheat/seeds through the fence gate, close it, breed every 5.5 min; verified by entity count, not events); hunt_spawn paused while the pens fill; dispatcher starts successors behind ≤10 leftover cells / material waits (never water); guard_night cut to 4; quartermaster counts stock like the board | pens: 4 sheep + 1 lamb, 2 cows + 1 calf, 1 chicken, all gates shut (probe 23:57Z); base plan built except dorm (10 cells) and wall; stairwell at y-54 (152 steps), miners' not-ready/no-branch spin in repair
- 09-19 22:45Z | day one → iron age | fill_void ran the pad rule and dug MOATS round its boxes (roads got the blame) → NO_PAD + moats filled, roads paved bottom-up; an OPEN fence gate was a wall for the pathfinder (6 builders locked in their dorm) → passable; keep-out holes get an `exit`; `locked` = contention (no retry/ticket); torch in a fill cell may be taken; quartermaster crafts torch/planks/stick to targets; helpdesk tickets no longer wake the top model; mine level 16 EXHAUSTED (80 branches, 446 ore blocks → 5 iron armour sets + iron pick) → `mine level -54` | deaths/1h 60 → 2, food 213/448, torches 976, planks 339, wheat seeds 226; bed + night skip; stairwell goes on to the diamond level
- 09-19 22:2xZ | "road trench": zones cut into islands by 1-wide / 3-deep moats, roads paved as decks over them | the build job PAD RULE ran on `fill_void` (ground level = the fill floor) and cut headroom in the ring around every fill box; roads placed paving on side support | `fill_void` in NO_PAD + the ring is never cut below origin y; nothing goes on an open fillable ground cell; `road` has a `fillOnly` sub-base (`unlid`: shallow decks come off, column filled, re-paved; a cave mouth > 3 deep is bridged from the rim); rings filled by fill_ring_* jobs; road 5 re-run as the test: 38 deck columns -> 0
- 09-19 21:35Z | day one, food + iron chain | dispatcher clock read 26.x `time query day` (it free-ran: NIGHT at noon); quartermaster (`scan` job) added + serves food → ore → logs per round and smelts the first 32 ingots whatever the fuel reserve; fishery accepts shallow open water; fields get `params.waterFrom` (lake -441,62,-422); hunters only hp ≥ 16 | first buckets, all 9 water cells of field 1 poured (`water_cell left:0`), 309 wheat planted there (+41 on the lake strip), fish flow but are eaten at once (food 43/448), cobblestone 2574, fuel 361, torches 250, deaths/1h 60 → 40
- 09-19 20:40Z | day one, the mine | stair bridges caves (supportChain: supports under floors + shell corners), unrepaired report 1/5 min + hand-back, stone haul at 304; miners may go down well-fed while the larder is < 64; quarry blueprint inside the ravine keep-out; keep-out rule in travel; bed + sleeper; respawnBeds = camp bed | stairs_done level 16 at 20:40Z (66 steps, 0 defects), cobblestone 1 → 739, first coal/iron/torches; night skip verified; food still 20/448 (fishing with 5 rods started 20:44Z)
- 09-19 20:10Z | cobblestone 1/1728: 23 builders blocked, nobody owned a pickaxe, pads were decks, one cell stalled the build order | blueprint `quarry` (terraced pit, legal only inside settings.keepOut) + `getPick` day-one chain (build, ores); build: no lids (ground cells filled from the natural ground up, decks repaired), a missing material rests only its cells (`needs:` note), <=0.5 % terrain leftovers = complete (`build_leftover`), no night gate; mine junk rules honour targets.cobblestone; `bank` verb verifies; guards do not chase archers unfit; hunters go where the surplus is | quarry_ne: cobblestone 1 -> 88 + 3 stone picks in 11 min; base_yard_pad: 10 decks -> 0 (3 real voids reported); 0 skeleton deaths 19:52-20:08Z
- 09-19 19:00Z | WORLD 2 STARTED (Paper 26.2, seed -1930374656508956714, 50 bots, base -320,68,-448 picked from the air) — day-one blockers | water was a wall for the pathfinder (river) → swimmable; lumber refused to work without an axe → fells by hand; EVERY 2x2 craft threw (`Recipe is not a constructor`) → fixed; first table now goes to the muster yard; aerial flat score ignored a ravine through the base → hole-aware score + plan-base survey check/keep-outs (in work); chat daemon answered death lines as players → only real online humans, one bot, one line | 44/50 crossed the river in 3 min, 300+ logs in pockets by 19:15Z, first-night deaths 13 (unarmed)
- 09-19 | WORLD RESET decided (owner): new seed, humans become spectators, no PvP/looting/raids | everything that existed only for human hostility deleted from the CLI/ops/inspector/chat daemon and the docs (RAID digest line, WAR block, HOSTILE escalation, moderation watch, chat `act`; lib + dispatcher by their owners), old-world coordinates removed from CLI templates/howto/census, docs reset (WORLD skeleton, this backlog, BUGS carried-over block); world-1 history → `attic/` (GOALS-log, BUGS, WORLD) | pending: seed choice → `ops/new-world.sh`

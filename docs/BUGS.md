# BUGS — reported by operators / foreman / helpdesk, fixed by the top model (code owner)
Format: `- [ ] time author: symptom | evidence (event line / probe output, coords) | where you think it is`. ONE line per bug; grep before adding (no duplicates).
Done = tick `[x]` + the fix in a few words. A new open line wakes the top model (`ops/escalate.sh`); lines whose author contains "top model" do not. World 1: `attic/world1/BUGS.md`.

- [ ] 09-20 12:1xZ nether engineer: the army is DIMENSION-BLIND — the dispatcher reassigned Koharu (in the Nether at -37,98,-76) to the `tidy` sponge and the hung watchdog declined her portal job (`hung: army:portal: standing in the far gate`, a bot must stand still 80 ticks in a gate); she then ran `tidy` with OVERWORLD coordinates at -27,121,-78 and boxed herself in under the Nether roof | evidence: `armyctl.js events 20 "portal|nether"` 12:05-12:11Z, `bot Koharu`, decline/Koharu.json | `hb.dim` is already in every heartbeat. DONE 12:2xZ: army_jobs.js `muster` + `withHandover` (bed/canteen/banking/respawn-bed/upTheStairs) skip off-overworld (proven live 12:30Z), and `portal` is in the hung watchdog's STILL_OK. STILL OPEN: dispatcher.js eligibility by `dim`, and army.js travel/bank/`skyAbove`+`digOut` (the escape doctrine cut a staircase into the Nether's bedrock roof, `off_world dig_out_roof` 12:30:23Z) — full list: docs/NETHER.md OPEN table

## Carried over from world 1 — world-independent CODE gaps that were still open at the reset (evidence: attic/world1/BUGS.md, same wording)
- [ ] w1 top model: `A.travel` hops > 44 blocks use `GoalNearXZ` (Y free): a bot "arrives" in a cave under its target (muster slot at y56 under the base) | lib/army.js travel, long-hop branch
- [x] w1 top model: `steps` reports `plan_done` although steps failed under `onFail:"continue"` (a repair plan whose dig AND place failed looked finished) | army_jobs.js steps: count failed steps, report `plan_done {failed:n}` or `plan_failed` — top model 20:1xZ: `plan_done` carries `failed:n` + note and the job note says "plan failed" when steps failed under onFail:continue.
- [ ] w1 top model: `farm` holds its bots 90 s per pass in "waiting for growth" (9 farmers idle while other jobs starve; waiting is waste) | army_jobs.js farm: nothing to plant/harvest -> hand the bot back (decline), keep 1-2 caretakers
- [ ] w1 top model: `farm` plants only what the farmer carries: 2787 seeds sat in the depot while cells stayed empty | farm: withdraw seeds from stock until the box is full
- [ ] w1 top model: `pillared_out` leaves junk pillars and often does not free the bot (`free:false`), also on open ground | lib/army.js escape: never pillar under open sky; dig the pillar back down once free
- [ ] w1 top model: a miner was left underground 26 min with a STALE heartbeat after the mine job was paused; nothing reported it | dispatcher/inspector: stale heartbeat + y < 50 -> `hung` report; recall must confirm every miner surfaced
- [ ] w1 top model: miners switching to a surface job left the mine by `travel()` and each cut a private staircase (`dug_out pit:false`) instead of walking the mine stairs | job switch from the mine job -> `recalled` walk first
- [ ] w1 top model: several miners stand stacked on one cell (5 died in ONE creeper blast); no spacing / no creeper avoidance underground | iron_core walk/queue: keep >= 4 blocks apart, hit-and-run creepers
- [ ] w1 top model: guards/chasers work far outside their job's area (a tidy bot 200 blocks away with task "army:guard") | lib/army.js guard/kill: leash to the job site, give up the chase beyond it
- [x] w1 top model: `build` declines the WHOLE job when one minor item is missing (4 gate cells of 360) and ignores equivalent materials in stock unless `args.mats` is set | build: skip cells it cannot supply, default substitutes for filler blocks — top model 20:1xZ: a missing material (any: bulk, mats, furniture, torch) rests ITS cells 5 min and the pass goes on; only when nothing else is left the builder steps aside and the job stays active with note `needs: a, b (N cells wait)` (seen 20:07Z base_hall: cobblestone -> torch -> furnace skipped in one pass, note written). A wrong block is no longer cut out before its replacement exists (field paths).
- [ ] w1 top model: position lock `locked` x6 on one cell with nobody there (stale/leaked lock file?) | blocks.js acquire: verify expiry of a dead owner's lock
- [ ] w1 top model: `kill` verb on night expeditions made 0 kills in 15 steps and the bot drifted back to base | steps kill: search radius / mob filter; a plan must stay at its `goto` site
- [ ] w1 top model: only the rod HOLDER can staff `fish`, and he rotates to other jobs between slices (job unstaffed churn) | dispatcher: item-bound jobs keep their holder (stickiness) or the rod is handed over via the chest
- [ ] w1 top model: `bot.state.guard` in manager.js targets `e.type === 'hostile'` = calm ENDERMEN too (only when the `guard` action is on) | bots/manager.js self-defence: exclude enderman
- [ ] w1 top model: hurt bots sat at food 16-17 with bread in their pockets and never regenerated (regen needs >= 18); `feed.eat()` has the rule — find which job slices never call it | feed.js / worker loop (not re-verified)
- [ ] w1 top model: `job_slice.deaths` of the mine job is a lifetime counter, so the board's deaths/1h per job over-counts | iron_miner.js `I.bump(bot,'deaths')` -> per-slice delta
- [ ] w1 top model: FEATURE GAP — no job type for an animal PEN + breeding (P3), none for sugar cane / paper (P4) | army_jobs.js
- [ ] op 2026-09-19: status shows 0/10 manager shards (bots offline), REPORT.md 2995s stale, field empty; all jobs unstaffed as a result. ops/up.sh needed (operator may not run it) | ops/up.sh
- [x] 18:59Z helpdesk (Akari, no_route): Job base_field_1_pad targets -336,?,-432, unreachable from Akari's 120-block area at -112,63,-201, which is walled by water on the west and by rising terrain to the east. Code owner: pick a reachable pad site or supply a route. | ticket: cannot path to -336,?,-432 (walkable area 120) @-112,63,-201 — top model 19:01Z: water was in the pathfinder's blocksToAvoid (world 1 had ice); now swimmable, liquidCost 6.
- [x] 19:04Z foreman: `plan-base` put the CORE zone (x -319..-285 / z -482..-448, pad y68) across a RAVINE: ground y55 @-310,-455, y48 @-300,-470, y44 @-300,-482 (look Honoka: a 9+ cliff running NNE-SSW through the base centre, skeleton at -320,49,-458 shooting). Leveling it means ~10k fill blocks with 0 cobble in stock, so I paused base_core_pad. WORLD.md claims "relief 4" for this window, so skyshot's flat-window score misses ravines | plan-base footprint check: sample ground per column and refuse or move a zone whose relief is > 3 (put the core on flat y68 land west of x -321 and leave the ravine as a zone boundary or a later fill job) | ops/skyshot.js relief + armyctl plan-base — top model: plan-base now checks every zone against an aerial survey (`--survey`, refuses BAD zones) and lays the base around `settings.keepOut` (ravine); skyshot flat score counts holes.
- [x] 19:04Z foreman: wood_spawn box x -400..-240 / z -720..-600 (the forest "N of z -600" from WORLD.md) is unreachable: Kokoro `travel_fail timeout d398 -> -354,-650`, `job_slice lumber: cannot reach the plantation`, 4 logs total after the bootstrap. The job's plan text said "BASE SITE x -387..-253 / z -536..-360", which does not match its box. I patched it to the flower forest NE (-190,-330,-90,-240) | `base set --wood` / bootstrap: pick the nearest forest a bot has actually REACHED (scout samples with trees), and write the plan text from the box — top model 19:01Z: water was in the pathfinder's blocksToAvoid (world 1 had ice); now swimmable, liquidCost 6.
- [ ] 19:04Z foreman: bootstrap order is broken: 26 bots sat on base_core_pad + base_field_1_pad for 20+ min with ZERO output while the army had 0 planks, 0 sticks and no craft table (settings.craftTable null). `obtain_failed stone_axe missing #tool_stone:3,stick:2` fired x44 across 44 bots, and guards were `guard_unarmed` | bootstrap/plan-base: pads should be `after` a first-tools step (logs -> ONE craft table at the base -> wooden pickaxe -> cobble -> stone tools for the squad), and obtain() should not retry a recipe while the army holds 0 logs
- [x] 19:06Z foreman: `lumber` cannot bootstrap a fresh world: `declined x45 {"job":"wood_spawn","why":"lumber: no axe and cannot craft one"}` while the army holds 4 logs. A player punches the first tree by hand, so the job refuses the one thing that would unblock everything. As a stopgap I put a `steps` job logs_hand (collect oak/birch_log by hand, 6 bots, forest -140,-285); hand-collect may leave floating trunk tops | army_jobs.js lumber: no axe and none craftable -> fell by hand (whole tree, top down) and let the first logs become table + axe — top model 19:01Z: lumber fells by hand when no axe can be had and tries a wooden axe from the first 3 logs; water is no longer a wall for the pathfinder (river at x -119).
- [x] 19:18Z op: 7 bots shot by Pillager at forest -145,-327 (wood_spawn, 12 deaths in 15 min, no defence/avoidance of pillager patrol/outpost); I PAUSED wood_spawn; no_route x7 near -320,55,-422 are underground bots (y<58, self-escape) | lumber/combat: flee or fight pillagers, or pick a forest without an outpost — top model: runbook row "mobs kill bots at a job site → armed `guard` escort with params.post, never pause the only producer"; guards hold the post unarmed and focus archers when armed (20:1xZ).
- [x] 19:34Z foreman: `bank` step reports ok:true with an EMPTY chest index — Misaki placed chest -338,69,-460 (depot_first step 7 ok) then step 8 `bank ok` while still carrying 12 wooden_sword + 56 stick (stock depot 0); Juri `bank ok` likewise and Karin then `withdraw: none of string in the chest index`. A chest a plan places is not registered anywhere (settings.chests was {} → I ran `chest add tools -338,69,-460` by hand) | steps place of chest/barrel inside the base → auto-register (settings.chests + chests.json); `bank` with 0 registered chests must be ok:false "no depot" | army_jobs.js steps bank/place, lib/army.js bank — top model 20:1xZ: cause: settings.chests was {} at 19:34 (A.bank had nowhere to go) and the verb returned true unconditionally. `bank` now compares the pockets with `keep` afterwards: ok only when everything left, else "NOTHING/only n deposited, still carrying {…} - no chest registered for <cat> | full/unreachable". `place` of a chest/barrel registers it (step field cat, or every category when no depot exists) -> `furniture_registered by:place`.
- [x] 19:34Z foreman: guard_night sends 25 mostly UNARMED, armourless bots at night after skeletons around the muster (-348,69,-461): 15x "shot by Skeleton" + 3 zombie in 30 min, 8 bots now parked at muster with hp 0-5 / food 13-17 and no food to reach 18 → no regen, useless all next day | guard: unarmed or hp<10 → do not chase ranged mobs, stay in the huddle/under cover; weak bots with food<18 must eat any raw meat carried (Himari mutton:2 at f12 did not) | army_jobs.js guard, lib/feed.js — top model 20:1xZ: guard: archers (skeleton/stray/bogged/pillager/witch) are chased only by ARMED guards with armour or hp>=14, who take the nearest archer FIRST; everybody else fights one only within 4 blocks and holds the post instead of walking the patrol ring (deaths 19:52-20:08Z: 0 by skeleton, was 15/30 min). build/tidy night gates dropped (params.dayOnly brings them back). Raw-meat eating of hurt bots = feed.js, see the 20:05Z line (not my file).
- [x] 19:34Z foreman: the `level` blueprint throws away its own cut: base_field_1_pad dug 32 cells (grass/dirt band x -333..-325 z -441..-416 now bare dirt) and then 18 bots `build_blocked: no dirt carried or in stock` x18, `place_failed noitem dirt` x6 — the dug dirt was never kept/banked for the fills | build level: keep dirt/cobble from cut cells for its own fill cells (or fill first from the cut pile), do not decline while the pad itself has surplus above y | army_jobs.js build / blocks.js clearAndFill — top model 20:1xZ: the pockets rule and build's own bank call kept only 64 dirt (rest banked/TOSSED: junk_tossed dirt x61/x54 at 19:41Z); build now keeps 5 stacks of its ground-fill block. Level pads are filled from the natural ground UP (no lids; decks found are taken off and filled; >6 deep = reported): probe base_yard_pad 20:05Z: the 10 decked columns of its note are gone, 3 real voids remain for fill_void.
- [x] 19:34Z foreman: wood_spawn lumberjack Riko at -53,88,-137 (d438 from muster, task lumber:travel) is ~150 blocks outside the job box x -190..-90 / z -330..-240; died there to a creeper 19:28 | lumber: never travel outside params.box; a tree target must lie inside the box | army_jobs.js lumber tree pick / travel target — top model 20:1xZ: not a tree outside the box: server log 19:26:33 "Riko was shot by Skeleton" -> respawn at the WORLD SPAWN (no bed), 19:27:02 travel_fail d543, and -53,-137 lies on the straight line spawn -> box. lumber already filtered trunks by box; `fell` now gets the box too (+2). The real gap = respawning 500 blocks from work without a bed (board/bed, not lumber).
- [x] 19:37Z foreman: the RAVINE on the base's east edge (rim x -325..-321, floor y44-57, z -455..-415; stray craft table -307,57,-436 on its floor) swallows base bots: no_route from inside it by Madoka -316,65,-440 (standing on air), Mashiro -323,53,-421, Riko -323,52,-420, Noa -315,53,-442, Shiori -317,57,-420, Juri -330,64,-435; base.png shows red floating cells along the rim where pad fills were decked over it | travel/pathfinder must not take drops > 3 off the base level; the base needs a rim barrier (fence/wall line x -323, z -455..-415) + one stairwell out (blueprint `stairwell`, needs ~40 cobble, stock 0) — sequence it right after the first stone pickaxes | lib/army.js travel maxDrop, plan-base zone edges — top model: plan-base now checks every zone against an aerial survey (`--survey`, refuses BAD zones) and lays the base around `settings.keepOut` (ravine); skyshot flat score counts holes.
- [ ] op 2026-09-19 19:41Z: CHEST FULL salvage (stick not banked): all 4 categories (tools/build/salvage/food) share ONE chest at -338,69,-460, 0 chests in stock (82 planks, 1 crafting_table carried by Yuzu); 63 sticks in depot | bank overflow: craft+place a 2nd chest per category or let banking skip sticks/dirt; unstaffed base_tree/mine/dorm_pad are only lower-priority (bots all busy, 9 muster bots hp<=9 fail minHp:10), left as is
- [ ] 20:05Z foreman: ROOT CAUSE of the hurt-but-never-healing muster (19:34 line): feed.js:49 eats raw meat only when `starving || opts.rawOk`, so a HURT bot at food 15-17 never touches the porkchop/mutton/chicken it carries (10 muster bots hp3-7 f15-17 with raw meat at -360,69,-483, failing every job's minHp; 0 cooked food exists, 0 furnaces) | feed.js eat: raw is OK when `hurt && food <= 17` too (regen needs 18); stopgap on the board: steps job eat_muster (the eat verb passes rawOk)
- [x] 20:05Z foreman: quarry_ne (7 bots, ~25 min) produced 0 cobblestone: NO quarry bot carries a pickaxe (the depot had 5 wooden_pickaxe; other plans took them), it only dug a 1-2 deep DIRT pit x -303..-288 / z -430..-408 (y67 vs base 68, Chika dirt:342, Shiori dirt:181) and each pass reports `dig:locked` x41 + gaveUp 208-455 of 339-633 cells | build (quarry/level): a stone cell needs a pickaxe - withdraw or craft one (logs+sticks are in stock) before digging, decline "no pickaxe" instead of digging dirt forever; find the lock leak (also `locked @-343,68,-497` x12 with nobody there) — top model 20:1xZ: by design the pit goes down layer by layer: 3 layers of dirt first, stone from y66. 20:00Z 8x `pickaxe wooden_pickaxe` (getPick: depot -> stone -> wooden day-one chain, also in `ores`), cobblestone 1 -> 88 carried + 3 stone pickaxes + 6 stone hoes by 20:06Z, `build_done` 20:04Z, pit = clean 1-block terraces (look Chika). `dig:locked` = 8 bots in a 15-wide pit (8 s skip, not a leak). Bigger pits need a bigger settings.keepOut/quarry box (blueprint `quarry` refuses any other ground).
- [x] 20:05Z foreman: hunt_spawn 12 hunters report `trip kills:0 moved:{}` ~65x/60 min and 0 edible reaches the depot, yet Juri and Ume each carry mutton:6 + wool (they DO kill) - the trip report does not count kills and the meat stays in the pockets until the hunter dies (keep_inventory off, 43 deaths/h). Herds seen are 2-5 head and `leave:2` protects most of each | hunt: bank raw meat to the food chests every trip, count kills from the drop pickup, skip herds that are all protected by `leave` — top model 20:1xZ: hunters bank after every trip, eat their catch, truthful trip report (haul/banked/left).
- [x] 20:04Z helpdesk (Fuuka, hung): eat_muster step 3/3 'eat' hangs at Fuuka (-354,69,-482): food=20 and inv empty, so there's nothing to eat and no hunger. Make eat a no-op or time out when food>=20 or no food item is held, then finish the muster. Depot is -346,70,-460. | ticket: no movement for 3 min on job eat_muster, task "army:step 3/3 eat" @-354,69,-482 — top model 20:1xZ: `eat` verb returns at once when food is 20 and is bounded to 20 s.

- [ ] 20:12Z top model (code engineer): NOT IN MY FILES, for main: (1) lib/army.js bank tosses everything over 64 cobblestone/dirt that no chest took - must honour settings.targets (army_jobs now keeps all cobble in the pockets while the BUILD chests have no room, iron_core tossJunk/needHaul honour the target). (2) a bot without a bed respawns at the world spawn ~540 blocks out and walks back alone (Riko 19:26Z, Fuuka 19:43Z at 13,62,-12). (3) the commute base <-> NE quarry crosses the ravine (Hina "fell from a high place" 20:03Z). (4) farm_bank: the bank is SAND at y62 (ground -445,-424 / -440,-426 = sand) - only dirt tiles can be tilled: farm_pass planted:0 x19.- [ ] op 20:24Z: base_mine_pad: Mei on a small island/pillar at -14,85,12 (area 49) loops no_route -> [-330,null,-500] 'build: cannot reach the site' + dug_out pit x6; rescue refused (not hung, moves to -18,72,4); Honoka/Sumire no_route to -338,69,-460/-360,69,-500 are the same far-depot travel_fail (34 bots timeout d=147) | pad job with y=null target should refuse/route from a walkable spot; check travel timeout to depot -338,69,-460
- [ ] 20:26Z helpdesk (Kaede, place_failed): place_failed 'locked' for dirt at -322,63,-507 (base_mine_pad); 6 retries from -334,69,-505 failed. Check the lock's owner and release logic, and make the pad skip locked cells. | ticket: dirt at -322,63,-507: locked @-334,69,-505
- [ ] op 20:33Z: base_mine_pad now ~15 bots (Ichika Ume Sayaka Aoi Mio Noa Yui Koharu…) HUNG/place_failed 'locked' dirt at -323,64,-508 (same lock as line 42 -322,63,-507), x100 events; pad squad stalls on one locked cell | pad must skip locked cells / expire the lock; no torches in stock (coal 10 carried, depot 0)
- [ ] 20:35Z foreman: sleeper never slept all night although the bed stands at -341,70,-456 (ground: white_bed): Nagisa (hp5) only logs job_slice 20 s xN, zero in_bed/sleep_failed events; army_jobs sleeper() swallows the "monsters nearby" error with no event and a weak sleeper cannot clear them -> the night stayed real, 45 deaths/h (16 skeleton + 6 creeper in 30 min). Should: report sleep_failed for every reason and get the bed area cleared (call guards / retreat + retry) instead of looping silently (board stop-gap: guard_bed escort, 6 bots, radius 14).
- [ ] 20:35Z foreman: weak bots never heal at muster: Chika hp2/f17, Kotori hp1/f12, Karin hp4/f17, Yuzu hp4/f16, Madoka hp4/f17, Botan hp6/f16 stood at muster (-358,69,-478) all night; natural regen needs food >= 18, they sit at 15-17 and never eat. Should: a bot with hp < 16 eats until food >= 18 (canteen / withdraw), else it idles as a one-hit victim.
- [ ] 20:35Z foreman: hunt_spawn kills but never hauls: trips Rin kills:5 haul:0 banked:0 (site -104,87,-421), Yui kills:1 haul:0 (-546,111,-482), Nozomi kills:1 haul:0 (-624,68,-574); sites 220-280 blocks from the base, 21 of 45 deaths/h are on hunt_spawn. Should: pick up the drops after each kill, cap the hunt radius / hunt as a squad instead of lone 250-block trips (board stop-gap: hunt_spawn when:day).
- [ ] 20:35Z foreman: settings.craftTable -346,70,-460 is grass - the depot table stands at -347,70,-462 -> obtain_failed "no crafting table within reach" x67/30min (42 bots). Should: craft uses any real crafting_table within ~4 of settings.craftTable (or re-registers it) instead of failing (board stop-gap: craft_table_fix steps job puts one on the registered cell).
- [x] op 21:05Z: fill_field2_void (8 bots) place_failed 'locked' dirt @-272,62,-502 x38, gaveUp 82 — same lock-skip gap as lines 42-43 (not a new bug) — top model 22:36Z: same lock-contention fix.
- [ ] 21:05Z foreman: `locked` cells are now ARMY-WIDE, not one pad: place_failed x917/30min across 40 bots - fill_field2_void -304,62,-504 (8 bots, x398, 20 min zero output, bots pillar out and leave dirt stubs x -324..-304 / z -507..-501), base_hall -343,67,-525, base_mine_pad -322,63,-507 (20:26/20:33 lines still open). I paused fill_field2_void. Should: a lock older than ~60 s whose owner is not standing at the cell expires, and a build pass skips a locked cell after 1 try instead of 6 retries per bot
- [ ] 21:05Z foreman: DEADLOCK torch-gate: base_hall/base_depot `build_blocked "no torch carried or in stock"` x72 (29 bots) + `declined build: no torch` x30 while depot coal 0 - a whole blueprint (chests, furnaces, rows) waits for its last decoration; obtain torch fails `missing #coal:4` x124/30min. Should: build places every non-torch cell and leaves torch cells open (report them), never declines the job for a missing torch
- [ ] 21:05Z foreman: food never reaches the depot: depot holds 0 of every food (cod/salmon/chicken/mutton/beef/pork) while miners carry it underground - Juri y16 cod3 beef3 chicken9 mutton10, Nagisa y26 cod12 salmon4 - so quartermaster cannot cook it and Noa sits hp1/f4 `canteen_unreached` x3, 13 bots weak. Also a steps plan's start-bank took Karin's porkchop:2 before my `eat` step (eat_weak 21:04Z, banked x2 porkchop). Should: miners/hunters/fishers bank raw food on every surface visit (keep 2-3 for themselves); bank before a steps plan keeps food
- [ ] 21:05Z foreman: a ONE-BLOCK DECK over the ravine: base_tree.png shows a solid field of red (air-below) cells x -331..-318 / z -433..-417, ground reads y68 grass at -326,-431 and -322,-425 while the ravine floor there is y44-57 (19:37 line). Mobs spawn under it; a bot that breaks one cell drops 15-24 blocks. Should: take the deck off and fence the rim (or fill it solid when cobble allows) - never deck a hole; plan-base keepOut should also forbid decking inside the keep-out
- [ ] 21:05Z foreman: hunt_spawn is the death machine: 29 of 61 deaths/1h, trips `kills:0 haul:0 banked:0` for 12 hunters, hunters on hills y96-112 at -494,-297 (d229) and -213,94,-579. Cut to bots 3 (from 12) on the board; the freed hp20 bots now fall to mine_iron (fuel deficit 0.95). 20:05Z/20:35Z lines still open: hunt must pick up drops + bank, and stay within ~120 of muster
- [ ] 21:07Z op: mine_iron miner Nagisa boxed in at -361,15,-509 (walkable 1) looping mine_reconnect step_nofloor x many (to -360,16,-466), also mine_lost ticket + no_route -371,69,-509; rescued. Should: mine_reconnect must report/give up when boxed in instead of spinning silently
- [x] 21:18Z helpdesk (Nozomi, no_route): Nozomi at -316,50,-438: pathing to -264,?,-428 stops at a 122-cell area, though the look map east of the bot rises only 1 block per column (a walkable slope). Check the pathfinder's step-up/area limit or the goal's y=null handling. | ticket: cannot path to -264,?,-428 (walkable area 122) @-316,50,-438 — top model 21:20Z: Nozomi walked out by the natural ramp at the ravine's north end (-325,68,-409) two minutes later; since 20:15Z travel never steps below base level inside settings.keepOut, so new trips cannot end down there.
- [ ] 21:22Z op: base_field_1 (18 bots) build_blocked x26 'water cells wait: no bucket ... (3 iron_ingot)' - stock iron_ingot 2-4/224, iron 15; planks/iron_ingot/torch/string targets have NOBODY as producer (torch obtain_failed 'stick:3' x many). Need a smelt/bucket producer (quartermaster furnace of raw_iron:59 carried by mine_iron squad) + planks/stick producer; bots idle-stuck on field_1 meanwhile
- [ ] 21:35Z helpdesk (Rin, hung): Rin hung at -360,69,-501 on 'army:build mine_head': a crafting_table already stands at -359,69,-501 beside it. Make the build step skip or reuse an occupied target and add a timeout. | ticket: no movement for 3 min on job base_mine, task "army:build mine_head" @-360,69,-501
- [ ] 21:36Z foreman: the depot crafting table does not STAY: craft_table_fix placed one on settings.craftTable -346,70,-460 at 21:00 (step place ok), at 21:32 `ground -346,-460` read "grass_block, above: air" again and crafting was dead army-wide (obtain_failed "crafted 0/1 stone_pickaxe", "crafted 0/16 torch", 0 bread while 48 wheat sat in the depot). Some job takes it back (tidy_spawn box x -408..-230 / z -559..-369 includes the cell, "stray blocks removed"; pads level the same ground). Should: the settings.craftTable cell is a protected asset (tidy/level/build never dig it) and the quartermaster re-places it when it is missing, instead of 50 bots failing every craft (extends the 20:35Z line)
- [ ] 21:36Z foreman: bots WALK INTO the ravine keep-out and cannot get out: Yukina (farm_1, hp7) stood on the ravine floor under -325,-425 (look: walls 9 up on the west, crafting_table -307,57,-436 = bots have been living down there), Wakana help_done "in a 2-deep pit with 1 cobblestone" at -335,67,-436, deaths 30 min "fell from a high place" x2 (Nozomi, Kaede). keepOut only forbids ZONES and roads (armyctl base keepout), pathing ignores it. Should: travel/work sites avoid a keep-out box the way they avoid lava (no route through it), plus a rim fence job along x -334..-289 / z -486..-408
- [ ] 21:36Z foreman: `farm` declines "farm: no seeds (stock 0, no grass nearby)" x3 on farm_1 at 21:31 while `stock wheat_seeds` = 78 in the depot + 107 carried and farm_bank banked wheat_seeds:78 one minute earlier - the farm's stock lookup sees 0. 9 farmers on a freshly built 27x27 field with empty cells. Should: farm withdraws seeds from the depot index (same chest index as `stock`) before declining (the w1 line "farm plants only what the farmer carries" is the same root)
- [ ] 21:36Z foreman: lumber_base burns bots on an empty plantation: 129 lumber_pass in 20 min with felled:0 planted:0 logs:0 in the finished 48x48 tree farm (zone tree x -282..-235 / z -454..-407, lumber_base.png shows the torch grid and ONE tree), depot saplings 0 while wood_spawn replants every sapling in the wild forest at x -190..-90. I paused lumber_base. Should: felling anywhere banks saplings, and the tree-farm job plants the empty 3-grid cells from stock before it reports a pass (a plantation with no saplings must decline, not walk)
- [x] 21:36Z foreman: torch has NO producer: `targets` says torch 224 "NOBODY - a target without a producing job moves no bot", depot torch was 0 while charcoal 362 and coal 20 sat in the same depot, and 13 miners reported mine_not_ready short:["torch","cobblestone"] x16; builders reported build_blocked "no torch carried or in stock" x9. I crafted 150 by hand with a steps plan. Should: the quartermaster (or a standing works job) keeps torch/stick/plank stock at target from banked logs + charcoal, like it bakes bread — top model 21:42Z: quartermaster `craftToTargets` makes one batch per round (torch 64 / planks 64 / stick 32) of the crafted target furthest under its target, from depot stock, and banks it (`crafted_to_target`).
- [ ] 21:40Z op: (1) no_route surface x10 from -359,69,-545 to -359/-367,null,-550/-558 (area120 island150, 4+ bots) – target column unloaded/water? (2) mine_head hang now also Tamaki/Kaede/Iroha (see Rin line 57, 5 bots, left:5 forever) (3) mine_iron no_branch at -434,16,-382 and no_way_to_entrance at -321,16,-375 (Ayame rescued), surface_stairs at -327,16,-488. (4) base_dorm/base_pen_1 unstaffed requires minHp10 while bots weak.
- [x] 21:59Z op: base_dorm squad (Yui Honoka +4, at -346,69,-540, area 120 island 150) no_route to -360/-362,69,-500 x10 and place_failed no_los at -343..-349,71,-537 (Nagisa, put:no_los 8) - the dorm site is a walled-off island from the depot side (same island 150 as line 63) | build/travel: dorm origin needs a walkable link or a different origin; pickaxes are fine (35 stone carried, depot 0 only) — top model 22:1xZ: the island is the MOAT of fill_dorm_void (x -371/-339, z -546/-526, 1 wide, 3 deep): the build job pad rule ran on blueprint `fill_void` and cut headroom from the fill floor up in the ring around the box. Fixed (fill_void in NO_PAD + the ring is never cut below the origin level); repair job `fill_ring_dorm` on the board (line 63 (1) is the same island)
- [x] 22:1xZ top model: INCIDENT 21:53Z "road trench" (3 road builders marooned at y66 on x -374, no_route x46): NOT the road job - every `fill_void` job got the PAD RULE (footprint+1, ground = lowest body block = y65, `air` y66..69 in the ring) = a moat 1 wide / 3 deep around its box; evidence: fill_field3_void `build_leftover -398,66,-491` + `fails dig:locked last -395,66,-459` (both OUTSIDE its box -405..-375/-490..-460), Honoka marooned -406,66,-491 at 20:59Z = 20 min before the first road pass, grass regrown at y65 under road 2 paving; rings = lines x -374/z -491/z -526/x -304/x -339 = road centre lines, which the roads then DECKED with cobblestone over air (road 2: 124 columns); fill_road6_trench itself dug x -376 and x -372 (z -557..-387) in 5 min | army_jobs.js blueprintCells: `fill_void` in NO_PAD + ring never cut below origin y; todo(): nothing is placed on an open fillable ground cell; road.js: `fillOnly` dirt sub-base under the paving with `unlid:true` (paving standing as a deck over air comes off, column filled from the ground up, re-paved). Repair on the board: fill_road6_trench widened to x -376..-372, fill_ring_field3 / _dorm / _field2 / _minepad / _field1 (fill_void ring, inner box kept, unlid)
- [ ] 22:05Z op: fish_lake (food 80% deficit, needs 8) UNSTAFFED: requires fishing_rod, stock rod 0, string 4 (need 2/rod), string has NO producer (spiders only); no bot can craft rods beyond 2 -> add a string source (spider kill/cobweb) or let fish_lake craft/ withdraw rods from 2 string. Dorm no_route x4 = same island 150 as line 64 (not re-logged)
- [ ] 22:06Z helpdesk (Iroha, place_failed): base_dorm builder: place oak_planks at -365,72,-543 fails no_los after reposition x3 and scaffold. Bot is at -363,69,-545, 3 blocks below the target, and the surrounding ground is flat with beds at y69. The target probably has no adjacent solid face to place against, so the build plan should place s | ticket: oak_planks at -365,72,-543: no_los @-363,69,-545
- [ ] 22:08Z foreman: the base has ZERO furnaces (REPORT.md "0 furnaces, 1 beds", 90 containers indexed) and no zone of the base plan builds one: raw_iron 104 carried by the mine squad, cod 6 + salmon 8 + raw beef/mutton/chicken in the depot, 0 cooked anything banked, iron 5/224 (deficit 0.98) so base_field_2/_3 park their bots on "waiting for water" (no bucket = 3 ingots). I placed 4 by hand (steps job depot_craft, -345..-344,70,-461..-460). Should: the core/depot zone blueprint owns a smelter row (4-8 furnaces, registered like chests) and the quartermaster keeps them fed — cooking doubles every food item we already catch
- [ ] 22:08Z foreman: ONE impossible cell auto-pauses a whole void repair: "BUILD STUCK: base_road_5, fill_field1_void, fill_minepad_void auto-paused (build_stuck) — 1 cells nobody could do: -305,69,-495", and base_mine_pad stays noted "14 columns are a deck over air (void_under_pad): -330,66,-507 | -330,66,-506 | …" under the pad where the whole mine squad works. Should: build_stuck skips the bad cell (report it once, keep it in `left`) and pauses only when NO cell progressed in a pass — a 1-cell defect must not abandon 500 good cells standing over air
- [ ] 22:08Z foreman: base_light lights everything EXCEPT the base: its plan says "torch grid pitch 8 over everything inside the wall line", but the passes are at -239,77,-542 (Ichika, x 100 outside the wall) and -335,71,-374 (Misaki, z 110 outside, torch x25) with 8 bots on the job, while the depot yard around -346,-460 and the dorm island are dark and deaths/1h = 8. Should: the light job clamps its grid to the wall box from the board and prefers unlit cells nearest the yard, the way the tidy job clamps to its box
- [x] 22:08Z foreman: mine_iron never starts a branch: mine_blocked x31/10 min {"task":"iron:to-entrance","why":"no_branch","at":[-330,69,-500]} (19 bots have hit it), 3 of 10 wanted bots staffed, iron 5/224 — the chain that fields, buckets and tools all wait for produces nothing while the miners loop at the stair mouth. Should: no_branch at the entrance digs the next branch or the job declines with that reason, instead of 31 silent retries — top model 23:56Z: two redstone ores in the -54 trunk gave every stone-pick miner `needpick`, which marked 66 untouched branches `need:3` → `no_branch` for all; kit-fetch/hand-back rotated through 50 bots. Fixed in iron_core/iron_miner (other lane, work check BEFORE the kit, level fallback by pick tier, squad-wide single reports); 12 miners underground on y0, 610 stone + lapis banked in 13 min.
- [x] 22:08Z foreman: bots walk to a bank they cannot open, forever: bank_unreachable x33 in one minute {"cat":"build","failedOpens":8,"freeSlotsInCategory":451,"from":[-350,69,-539]} by 18 bots; cost 25818 m walked/15 min = 6 m per banked item. Should: after 2 failed opens the bot marks that chest unreachable for N minutes and banks into the nearest reachable chest of the category (or drops the fetch step) instead of repeating the walk every pass — top model 22:20Z: they stood INSIDE the dorm: an OPEN fence gate has no collision shape and the pathfinder took it for a wall; strictMovements now treats shapeless openables as passable (probe: partial → success).
- [ ] 22:07Z helpdesk (Sumire, build_stuck): fill_ring_field1 fails on every cell (first -332,66,-415; bot at -335,67,-431). Bot has 60 cobblestone for 64 cells and no fill/place failure reason. Check the fill/place reach and standing position, and add a withdraw of more cobblestone before the job. | ticket: 64 cells of fill_ring_field1 left but I failed twice on each (first: -332,66,-415) @-335,67,-431
- [ ] 22:19Z op: fill_ring_dorm BUILD DONE but dorm-side bots (Ume Honoka Nagisa Yui at -362..-345,69,-539) still no_route to -360/-390,69,-500/-507 (area 120, island 150/151) x60 - moat repair did not reconnect the island (line 64); Ichika at -348,66,-492 island 16 area 105 too; also fill_cave_road5 VOID UNDER PAD at -306,66,-505
- [x] 22:32Z op: place_failed why:locked at -394,62,-448 item dirt x11-49 per bot from ~12 bots (Hazuki Hinata Rin Akari Riko Ume …) on base_field_5 build_pass gaveUp:22; also farm_2/3 'no seeds' declines (seeds 0 in stock, farm_4 farm_pass skipped:182 by 47 bots?); base_road_6 last cell -375,69,-555 unreachable — top model 22:36Z: `locked` is contention, not failure: placeHard breaks at once (no retry, no ticket), the builder leaves the cell for 45 s and ends the pass after 8 lock hits; farms 2-4 cut to 1 bot until seeds exist.
- [ ] 22:41Z foreman: 13 bots of base_field_5_pad stand in ONE 3x3 corner and block each other's cells: place_failed why:"entity" tried ["wait for entity" x6] from Chino/Aoi/Iroha/Botan/Kokoro/Honoka all at -393..-398,64..68,-448..-452 (REPORT: place_failed x936/30min by 37 bots), build_pass gaveUp 23 of 31 while left:70 barely moves. Should: the build job hands each bot a DISJOINT work cell (claim per cell, spread over the box) instead of every bot racing the same nearest cell - "entity" is the same contention the 22:36Z `locked` fix addressed, one block higher
- [ ] 22:41Z foreman: the dispatcher activated base_field_5 (field_block) while its own pad base_field_5_pad still had left:70, so the field is decked over the unlevelled ground: build_pass {"job":"base_field_5","gaveUp":23,"fails":{"put:entity":1,"dig:no_los":2}} and place_failed {"at":[-394,68,-449],"why":"noref: no solid block within 6 below or beside"} x2 bots - a field block placed into air over the pad hole. Should: `after` for a zone job means its pad job reported build_done, not just the previous zone (one site = ONE height); board stop-gap: base_field_5 paused 22:38Z
- [ ] 22:41Z foreman: the `level` blueprint digs BELOW its own target y and traps the bots in the hole it makes: origin y68 at base_field_5_pad, yet dug_out {"pit":true,"from":[-395,64,-449],"to":[-399,68,-449],"sky":true} (Tamaki) and {"from":[-393,67,-448]} (Ayame), then no_route {"from":[-394,66,-451],"to":[-371,69,-500],"area":9,"island":9} x3 bots + pillared_out x5 (Chino) - the pad's west edge at x -405 is the rim of a pond 6-9 deep (look Kurumi: a..i gradient to water y62 at x -416). Should: level never digs a cell below origin.y, and a pad whose box touches a >=4-deep drop shrinks its box or gets a retaining fill first
- [ ] 22:41Z foreman: seeds live in the farmers' pockets, so every other field starves: wheat_seeds depot 0 / carried 167 (Sumire 76, Suzu 35, Karin 34, Rena 30) while farm_3 farm_pass {"skipped":578,"seeds":0,"planted":0} x23 bots, farm_4 {"skipped":171,"seeds":0}, farm_2/3 decline "farm: no seeds (stock 0)" and Himari/Sayaka sit at muster with that reason. Same for food: REPORT "0 edible in stock" while 62 cooked_cod + 27 bread + 14 cooked_salmon are carried. Should: `bank` treats wheat_seeds and cooked food as depot goods (keep a small ration/seed reserve, bank the rest) so a job can withdraw them
- [ ] 22:41Z foreman: a solid deck of FLOATING blocks ~8x6 at x -333..-326 / z -500..-495 right beside the mine entrance -330,69,-500, plus a ~40-block floating trail x -323..-308 / z -523..-501 and a floating line x -271..-259 / z -493 - all RED on two independent mapshots (/tmp/foreman/base.png and farm_2.png, same boxes). Nothing of ours should stand on air: whatever pad/road job built there decked a void instead of filling it (cf. line 74 fill_cave_road5 VOID UNDER PAD -306,66,-505). Should: the pad rule fills the column to ground before placing, and the audit that finds floating blocks files a repair job — top model 22:42Z: (a) -333..-326/-500..-495 is the pad over the MINE STAIRWELL (2-wide, 3-high stair under it: air below is the walkway, not a void) - mapshot should not paint the stair mouth red; (b) the trail by road 5 and the line on road 2 are paving decks over the fill_void moats/cave found 22:17Z: the re-activated road jobs take decks ≤3 deep off and fill bottom-up (road.js unlid sub-base); re-check with mapshot after base_road_2/5 report build complete.
- [ ] 22:42Z op: base_pen_2_pad (level) stuck: 25 bots build_pass done:0 left:232, place_failed x250+ at -399,66,-542 'occupied by torch and cannot dig it (protected)' (Wakana, Sayaka) | level/build should treat a protected torch cell as removable (or skip it and place around it); also seed_forage/smelt_depot paused by op (idle: no seeds/cod/iron in depot, see 22:41Z seed bug); farm_3 crops_vanished (replanted 43 vs harvested 0 near -392,-482) not investigated
- [x] 23:00Z op: farm_5 (-380,-450) FARM DEGRADING dry cells 147 (best 0); farm_pass dry=0 for other farms, farm_5 skipped 494 planted 0, no seeds; look Riko shows crops but no water tiles near -370,-496 -> farm_5 water never placed/plugged? (fish_lake unstaffed = line 66, not re-logged) — top model 23:02Z: my own 22:55Z dispatcher rule ("a predecessor that only waits for material lets the successor start") started farm_5 behind a field still waiting for WATER; the rule now never applies to a `needs: water` note, farm_5 is paused again, base_field_5 re-activated (5 buckets are in the depot, waterFrom = the lake).
- [ ] 23:12Z foreman: THE CHEST INDEX IS BLIND TO THE DEPOT - every consumer job starves on material that `stock` counts. Proof in one minute: I re-activated bake_bread (depot wheat 534, bread 0) -> step {"job":"bake_bread","i":1,"do":"withdraw","ok":false,"why":"none of wheat in the chest index"} x9 bots; smelt_depot -> withdraw "none of salmon in the chest index", "none of raw_iron in the chest index" x20. REPORT says 90 containers registered, 87 indexed. Same root, same minute, on the biggest squad: base_field_6_pad (11 bots, x -405..-375/z -420..-390) sits at build_pass done:64 left:469 with obtain_failed dirt n:64 "ingredients did not arrive" (Rena Noa Nanami Tamaki Nozomi Riko Yukina) and place_failed at -380,68,-397 "noitem: none of dirt carried, in the depot index, or craftable" while `stock dirt` = depot 8734 (plus travel_fail to the depot -338,69,-460 "timeout/stop" x19). Also the 22:41Z seeds line. It is why GATE 1 stays open (0 edible in stock) with 534 wheat on the shelf | the index must cover every registered container (the 3 unindexed ones first) and `withdraw` must fall back to a scan of the depot boxes before failing the step
- [x] 23:10Z foreman: the mine works the level WITHOUT the iron: `armyctl mine` = y16 branches 80 (open 0) ore blocks 446 | y-54 branches 80 (open 73) ore blocks 10 <- working, while iron 5/224 is the worst deficit on the board (0.98) and mine_iron holds 10-12 bots. On top of it 30 bots report mine_blocked job mine_iron task "iron:to-branch" why no_branch at -208,-54,-519 x54 although that very level shows 73 OPEN branches - the claim/assign path contradicts the level map | no_branch while open>0 is a planner bug; and a level whose ore count collapses (10 vs 446) should hand the squad back to the iron level y16 — top model 23:56Z: two redstone ores in the -54 trunk gave every stone-pick miner `needpick`, which marked 66 untouched branches `need:3` → `no_branch` for all; kit-fetch/hand-back rotated through 50 bots. Fixed in iron_core/iron_miner (other lane, work check BEFORE the kit, level fallback by pick tier, squad-wide single reports); 12 miners underground on y0, 610 stone + lapis banked in 13 min.
- [ ] 23:10Z foreman: 7 furnaces sit unplaced in the depot and the world has NONE (REPORT "0 furnaces", charcoal 279, coal 14, iron_ingot 2, cod/salmon raw in stock) - nothing in the base plan owns a furnace bank, so no ore is ever smelted and cooked food only comes from luck | a `smelter` zone beside the depot (x50 scale, lit) in plan-base, built like any pad+structure, and smelt_depot feeds it
- [ ] 23:10Z foreman: base_road_12 (x -375..-235/z -404..-402) burned 20 min of 6 bots for ONE placed block: build_pass done:1 left:505 gaveUp:75 fails dig:locked:3 last -270,68,-404, with 3063 m walked/15 min and 8 chest visits without a deposit - a road that gives up on 75 cells a pass is a commute, not a job | build must abort/report a blueprint whose gaveUp exceeds done, and dig:locked (our own torch/asset in the lane) must route around or lift the asset - board stop-gap: base_road_12 paused 23:09Z
- [x] 23:10Z foreman: at night 22 of 50 bots stood on guard_night at the muster clump although a bed + an active sleeper (Tamaki) exist and deaths/1h = 1 - the "NIGHT SPONGE until a bed stands" never lets go after the bed stands, so pads and farms lose their squads every night (waiting is waste) | with settings.nightSkip and >=1 registered bed, guard_night caps at a small watch and day jobs keep their head-count - board stop-gap: guard_night bots 22 -> 6 at 23:09Z — top model 23:11Z: board fix: guard_night bots 50 → 4, priority 93 → 60 (a small armed watch; with nightSkip day jobs keep their squads). The bootstrap writes 50 for the bed-less first nights: `base set`/first registered bed should cut it - noted for armyctl.
- [ ] 23:08Z helpdesk (Shiori, no_route): Bot at -313,50,-439 is in a 120-block walkable pocket, open-sky, not boxed-in, so auto-escape never fires. Pathing to -102,?,-276 fails. Run the pillar/staircase escape when path area is under ~200, or let goto place filler (bot has 94 dirt). | ticket: cannot path to -102,?,-276 (walkable area 120) @-313,50,-439
- [ ] 23:22Z op: fill_field7_void finished with sealed:40 (done 0) yet void_under_pad still reports 40 air cells under pad field7 (-370,65,-398 …): 'sealed' pockets under a pad are counted by the audit but never filled -> fill_void should fill sealed cells reachable from the side/above pad edge, or audit should ignore them; farm_3 crops_vanished repeats (replanted 41 vs harvested 1, seeds 0, dry 0) = trampling/not water, see line 81; fish_lake unstaffed = line 66 — top model 23:25Z: known, minor (natural cave pockets under a solid pad: the pad is sound); the audit should report sealed pockets once per job, not per pass - deferred behind the iron/diamond chain.
- [ ] 23:24Z helpdesk (Koharu, place_failed): base_pen_3_pad: dirt target -314,68,-394 holds a red_tulip that dig refuses (locked), and clear failed. Planner should skip or treat locked flowers as done, or unlock breaking them. | ticket: dirt at -314,68,-394: occupied by red_tulip and cannot dig it (locked) @-313,69,-402
- [x] 23:37Z op: base_pen_4 build_stuck left:1 cell -322,69,-385 (fails put:locked/put:rejected, 9 bots); fill_pen4_void build_pass sealed:18 done:0 yet void_under_pad n:18 fires (sealed pockets counted as void, job "done" without filling) - mine_iron still no_branch (see 22:08Z line), so 27 fit bots idle; I activated base_pen_4/base_field_7/base_road_12 (bots 8/8/6). — top model 23:56Z: two redstone ores in the -54 trunk gave every stone-pick miner `needpick`, which marked 66 untouched branches `need:3` → `no_branch` for all; kit-fetch/hand-back rotated through 50 bots. Fixed in iron_core/iron_miner (other lane, work check BEFORE the kit, level fallback by pick tier, squad-wide single reports); 12 miners underground on y0, 610 stone + lapis banked in 13 min.
- [x] 23:45Z foreman: mine_iron is a CLOSED LOOP that cannot produce the army's #1 deficit (iron 6/224 = 97%): level y16 is worked out (`mine_no_branch {level:16,needPick:0} "level worked out: open the next level"` x16), so miners `mine_level_fallback {from:16,to:-54}` onto the diamond level where stone picks give `mine_blocked why:"needpick"` — and all 50 bots cycle mine_iron->muster->mine_iron (`banked {job:"mine_iron",items:{cobblestone:32,stick:8}}` x528, `mine_blocked no_branch` x126 @-330,69,-500 in 30 min, 19786 m walked/15min). Worse, `armyctl.js mine level 8` is ACCEPTED by the CLI and then refused in the field (`mine_level_refused "would move stair rows already dug (row 66): use a landing y (multiple of 16)"` x12) | iron_miner: when the working level has no branch left for the carried pick tier, open the next landing itself (or pause the job and report) — never fall back to a level whose ore the squad cannot break; armyctl `mine level` must validate the landing multiple-of-16 rule at CLI time. Board stop-gap: opened landing y0 (fresh branch, stone-pick iron) 23:44Z — top model 23:56Z: two redstone ores in the -54 trunk gave every stone-pick miner `needpick`, which marked 66 untouched branches `need:3` → `no_branch` for all; kit-fetch/hand-back rotated through 50 bots. Fixed in iron_core/iron_miner (other lane, work check BEFORE the kit, level fallback by pick tier, squad-wide single reports); 12 miners underground on y0, 610 stone + lapis banked in 13 min.
- [ ] 23:45Z foreman: `mine_not_ready {missing:["pick_spare"],short:["cobblestone"],note:"fetching from the depot"}` x433 by 49 bots in 30 min while the depot holds stone_pickaxe:50 (+54 carried, every bot has one) and cobblestone:325 — the "fetching from the depot" step never actually withdraws, so the whole squad re-reports the same shortage every slice | iron_miner readiness check: withdraw pick_spare/cobblestone from stock before declining, and decline ONCE per bot per shift instead of every slice
- [ ] 23:45Z foreman: the FALLBACK sponge refuses work and 28 of 50 fit bots stood in one clump at the muster by day (-367..-348 / -491..-475, visible as the white blob in the base mapshot): `declined {job:"tidy_spawn",why:"tidy: every tile was tidied in the last 30 min"}` x199 by 49 bots — doctrine says a bot idling at muster by day is a planning failure, so the any-time sponge must never decline | tidy: when its box is clean, widen the box / re-scan at the next ring instead of declining, or `settings.fallback` must chain to the next sponge (lumber/build) so no bot lands on `muster`
- [ ] 23:45Z foreman: the only cobblestone producer is dead while cobblestone is 325/1728 (81% deficit) and 20 furnaces + the hall + field rims wait for it: quarry_sw (19x19, ravine quarry zone) answers `declined {why:"build: complete"}` x2 and `build_pass {done:1,left:0,fails:{"dig:locked":3,last:"-323,58,-474"}}` even after I patched `params.args.depth` 8 -> 14 and re-activated — a build that is told to go deeper must recompute its cell list from the new args (rev bump), and the quarry must not be `dig:locked` inside its OWN approved zone | build: invalidate the completed-cell cache when params.args change; zone lock must exempt the job that owns the zone. Board stop-gap: quarry_sw paused again 23:44Z (it only churned 16 bots)
- [ ] 23:45Z foreman: the container/furnace REGISTRY does not see what stands in the world, so smelting and banking both dead-end: `census func` finds 4 furnaces at -345..-344,70,-460..-461 (placed 22:07Z) yet REPORT.md still says "0 furnaces" and quartermaster reports `furnaces {took:13,loaded:0}`, with charcoal:262 and iron_ingot:6 unused; same blind spot for chests — `bank_unreachable {cat:"salvage",failedOpens:0,freeSlotsInCategory:339,from:[-355,69,-502]}` x89 by 35 bots, i.e. zero failed opens, no salvage chest is even resolvable from the muster/mine side (distinct from the fixed 22:08Z fence-gate case) | the quartermaster scan must register every furnace/chest it walks past (census already finds them) and the bank picker must fall back to another category's chest instead of reporting unreachable every pass
- [x] 00:03Z op: bank_unreachable x93 by 38 bots {"cat":"food","failedOpens":1,"freeSlotsInCategory":332,"from":[-331,66,-480]} + travel_fail timeout/stop to -332,69,-498 x6 (see 22:08Z line 72, 23:45Z line 96); Wakana HUNG at -340,69,-460 self-recovered; Misaki no_route -> -288,null,-455 and Tamaki mine_iron surface_line at -352,0,-595 (y0 = unloaded/void column, not investigated) — top model 00:10Z: most had failedOpens 0 and 330 free slots = bank visits INTERRUPTED by a re-assignment; an interrupted visit is no longer reported as unreachable/full.
- [ ] 00:20Z foreman: the dispatcher starves a HIGH-priority repeat `steps` plan by reassigning its bots to LOWER-priority jobs mid-pass: `plan_interrupted {"job":"bake_bread","at":5,"why":"the dispatcher reassigned the bot to mine_iron|tidy_spawn|base_pen_3_pad"}` x12 between 23:12 and 23:18 although bake_bread is priority 96 and mine_iron 86 / tidy_spawn 60 / base_pen_3_pad lower still; every pass died at step 5 (`wait`), so the army baked 0 bread in 30 min with 2170 wheat in the chests and 82 bread for 50 bots (REPORT "WHEAT NOT BAKED"), and the job then paused itself with `plan_idle` | dispatcher/assign: never pull a bot off an active `steps` pass for a job of lower priority; a bot in a repeat plan is only free at the end of a pass (or on `wait`). Board stop-gap: bake_bread re-activated with bots 6 at 00:13Z
- [ ] 00:20Z foreman: `level` pads and `farm` finish a zone with whole TREES still standing in it - `look Yukina`: an 11-wide oak crown at x -383..-373 / z -467..-462 stands INSIDE field_3, wheat (`w`) on all four sides; `look Sakura`: a 5x5 oak at x -376..-372 / z -489..-485 stands in the middle of the MUSTER yard where 21 bots mill about. Both zones report built/tilled, so nothing will ever remove them; a field with a tree in it is what the owner sees first | build blueprint `level`/`platform`: fell trunk+crown inside the zone box before levelling (the `fell` verb exists, replant:false), and the pad audit must flag a `_log` column inside a finished zone. Board stop-gap: job clear_yard_trees posted 00:15Z
- [ ] 00:20Z foreman: a stock deficit with NO producing job steers nobody and nothing says so out loud: `armyctl.js targets` prints "NOBODY - a target without a producing job moves no bot" for string 2/40 (95%), iron_ingot 1/224 (100%), torch, stick and planks, while fish_lake (maxBots 8, a lake at -476..-430 right next to the base, zero bots in the mapshot) can never start because a rod needs 2 string - and at the same moment 16 of 50 bots carried task "army:muster" | `status.sh`/`wait` must raise "DEFICIT WITHOUT PRODUCER: <key> <have>/<target>" as a top problem (it is the one thing the board alone cannot fix), and `targets` should refuse to hold a target no job produces. Board stop-gap: wool_beds posted 00:16Z (19 penned sheep + shears instead of spider string)
- [x] 00:20Z foreman: the `herd` handler makes the WHOLE army answer a 3-bot pen job every slice: `declined {"job":"herd_cows","why":"no cow to lure within 250 of the pen (7/10 inside, breeding on cooldown)"} x29 by 26 bots` and `herd_chickens x26 by 21 bots` inside one minute, each decline a full 250-block entity scan; meanwhile the 3 bots that DID take it walk 160-200 blocks per animal (Mei luring a cow at -137,-397 from -337,-394, Kaede d179, Yuzu d167) for 3 missing cows - 16377 m walked/15 min, 3 m per banked item | herd: when the pen is short but no animal is in range, decline ONCE at job level (park the job with a note) instead of per-bot-per-slice, and cap the lure leg at ~64 blocks - beyond that it is a hunt/transport plan, not herding (same family as line 93) — top model 00:24Z: "nothing to lure / breeding on cooldown" now rests the JOB for 5 min on the board (`restUntil`, honoured by the dispatcher) instead of each bot declining in turn.
- [x] 00:22Z foreman: IRON NEVER REACHES THE DEPOT INDEX, so the whole iron chain is dead downstream of the mine: `targets` reports iron 79/224 and `stock` lists raw_iron only as CARRIED (Fuuka 10, Madoka 8, Himari 6 …), yet a `steps` plan gets `withdraw {"item":"raw_iron"} ok:false "none of raw_iron in the chest index"` (00:18:41) and the same for iron_ingot (targets: iron_ingot 1/224, 37 banked at 00:10:48 and gone) - 12 miners bank cobblestone and lapis every pass but their raw iron stays in their pockets, so shears (2 ingots), buckets and iron tools cannot be crafted and my wool_beds plan died on step 1 twice | the mine bank step needs an ORES/raw_iron destination chest (register one like tools/food/build), `bank` must not silently keep an item whose category has no chest, and `stock`/`targets` must mark an item that exists ONLY as carried - the board currently treats it as available (distinct from line 96, which is furnaces/salvage) — top model 00:30Z: it does reach it - and left it within a minute: `forgeArmour` turned every 8 ingots into armour (10 sets worn, `forged` events 00:19Z), so no plan ever found an ingot. The forge now makes iron pickaxes (to 8) and shears (to 4) FIRST and keeps a 32-ingot reserve before more armour. Raw iron in miners' pockets comes up with their hauls (by value).
- [ ] 00:28Z helpdesk (Noa, no_route): Noa at -317,52,-439 is stuck in a 120-block pocket (cliff of 9+ to the west) and cannot path to -117,?,-319. Check for a missing exit or a wrong goal, and choose a reachable wood_spawn target. | ticket: cannot path to -117,?,-319 (walkable area 120) @-317,52,-439
- [ ] 00:55Z foreman: the forge has NO DIAMOND TIER, so the dragon roadmap is gated by a chest full of diamonds: `stock diamond` = depot 48 + carried 8 (Shiori 8, Hazuki 6) and `recipe diamond_pickaxe 4` says OK on every ingredient, yet the quartermaster keeps making `forged {"made":{"iron_pickaxe":3}}` x10 in one minute with iron at 84% deficit, and `targets` holds no diamond key at all - no diamond pickaxe means no obsidian, no portal, no nether, while miners on y-54 keep banking diamonds nobody spends | quartermaster/forge: once >= 12 diamonds are indexed, forge diamond_pickaxe (to 4, for the mine squad) and diamond_sword (to 2) BEFORE more iron armour, and `targets` needs a diamond_pickaxe/diamond_sword key so the chain is visible. Board stop-gap: steps job toolsmith_diamond posted 00:52Z (Chino, 4 picks + 2 swords)
- [x] 00:55Z foreman: `herd` breeds far past its own `want` and can never cull, so pens are 3-4x over capacity and the whole army bounces off them: `bred {"kind":"cow","why":"all refused (love cooldown)","inPen":37,"gateShut":true}` with params.want 10, `bred {"kind":"chicken","inPen":28}` want 10, and `declined {"job":"herd_cows","why":"cow 37/10 in the pen, nobody ready to breed yet"} x28 by 25 bots` + `herd_chickens x16 by 15 bots` in the same minute (herd_cows/herd_sheep/herd_chickens each show staffed "-" on the board while 14 bots stood at muster). 65 penned animals and the depot holds leather 15, mutton 2, beef 0, feather 30 - a full pen is meat and leather, not a dead end | herd: when inPen > want, hand the surplus to a cull (kill kinds down to `want`, haul the drops) or at least park the job with `restUntil` instead of a fresh 250-block scan per bot per slice (same family as line 101). Board stop-gap: hunt job cull_pens posted 00:53Z (kinds cow+chicken, leave 12, 6 bots)
- [ ] 00:55Z foreman: ONE impossible cell freezes the whole base build programme and nothing ever re-opens it: REPORT "BUILD STUCK: base_road_6, base_dorm, base_field_6, base_pen_4, base_road_11 auto-paused (build_stuck) - 1 cells nobody could do: -375,69,-555", i.e. 5 of the base's unfinished structures (the DORM for 50 bots among them) sit paused for hours over a single cell, `events 60 build_stuck` shows nothing since, so the auto-pause is silent and permanent; meanwhile 14 of 50 fit bots stood at muster by day | build: auto-pause must skip the impossible cell (report it once as a `cell_impossible` with the reason) and finish the rest, and a build_stuck job must re-try its stuck cell when the blocking item changes (or hand it to tidy/fill) - a base whose dorm is 99% done is worse than one not started. PROOF: I re-activated base_dorm 00:53Z and 9 bots produced `build_pass {"blueprint":"dorm","done":0,"left":10,"gaveUp":1,"fails":{"put:no_los":3,"last":"-368,73,-538"}}` + `build_stuck {"left":10,"cells":"-350,73,-543 | -344,73,-543 | -350,73,-538 | -344,73,-538 | -356,73,-543 | -356,73,-538"}` within 90 s - the 10 leftovers are ROOF cells at y73 that no bot can see from below (`put:no_los`): the builder must stand ON the finished roof course (or scaffold from the wall head) before it calls a roof cell impossible. Board stop-gap: base_dorm paused again 00:57Z
- [ ] 00:55Z foreman: a permanent TRAVEL DEAD ZONE in the depot rows that REPORT itself flags as unowned ("WARNING STORM: travel_fail x35/30min (18 bots), most: timeout/stop @-359,69,-512 x4 - nobody is resolving it"): `travel_fail {"why":"timeout/stop","to":[-359,69,-512],"tries":8,"fails":0,"last":"fail","d":56}` x4 at 00:37 and the same at [-360,69,-512] x2 (tries 9) at 00:43, while `look Chika` shows that z band is the chest wall (`chest -371..-366,69,-506`, rows at z -503/-500/-497) - bots are being sent to a cell inside/behind the depot rows and burn 8-9 tries each time | travel/bank: a target that fails with tries>=8 and last:"fail" must be marked unreachable for that bot pool (and the chest whose access cell it is re-registered on a walkable side), instead of re-issuing the same goal to 18 bots every half hour
- [x] 00:55Z foreman: seed OVERFLOW - the farms bank what nobody can use and the army throws it away again: `targets` wheat_seeds 7539/224 (3400% of target, +1222 carried) against wheat 681, and bots now discard them by hand (`junk_tossed {"item":"wheat_seeds","n":78}` x3 by Sumire at 00:45), yet every farm_pass still ends `banked {"job":"farm_7","items":{"wheat_seeds":64}}` x9 - chest slots and trips spent on a dead item while bread is baked by nobody (617 wheat, 0 bread in 30 min) | farm/bank: cap a key at its target (seeds beyond `want` stay in the field as re-plant stock or go to a compost/feed chest, never into the depot index), and `junk_tossed` on an item with a target must raise a board note instead of silently dropping stock — top model 00:58Z: the glut rule ran AFTER the deposit; it now runs before the chest visit (stock ≥ 512 → surplus seeds are dropped, never banked). Composting the surplus into bone meal is the better use - backlog.
- [ ] 01:25Z foreman: the army's registered depot crafting table DOES NOT EXIST, which is the whole warning storm: `settings.craftTable`, `armyctl template toolsmith` and `recipe` all name -346,70,-460, but `look Karin 12` shows that cell is solid ground one block above the depot floor (furnaces at -345/-344,70,-460/-461) and the only table there stands at -337,69,-460 (others at -359,69,-501 and -345,69,-523); result: REPORT "obtain_failed x28/30min (14 bots), most: crafted 0/1 x8", `errors` "iron_core:placeAt Server refused to place crafting_table ... still air" x5, and mine_not_ready missing pick_spare | settings.craftTable + template toolsmith + the recipe hint must point at a table VERIFIED to stand (re-register the nearest real one), and `craft` should fall back to any table within ~8 blocks instead of failing the plan. Board stop-gap: toolsmith_picks_fix posted 01:22Z crafting at -337,69,-460
- [ ] 01:25Z foreman: a `hunt` job whose site is already culled never ends and never hands its bots back: cull_pens (6 bots, active since 00:53) logged `trip {"kills":0,"haul":0,"banked":0,"left":0,"site":[-330,69,-390]}` 70+ times in 60 min (Himari x16, Aika x8, Hazuki x8, Rena x8, Mio x6, Yotsuba x5), `look` at the pens shows 2 cows + 3 chickens left (params.leave 12, so the cull can never fire again) and Aika/Hazuki/Himari stood on the SAME cell -337,69,-455 for 45 min | hunt: N trips with 0 kills and 0 haul (or inPen <= leave) must decline and pause the job - 6 of 50 bots walked an hour for nothing while mine_iron was 4 bots short. Board stop-gap: cull_pens paused 01:21Z
- [ ] 01:25Z foreman: fit bots are parked at muster by DAY while an active job is below its own want: `field` shows Akari Botan Hina Ichika Mashiro Mei Nagisa Riko Tamaki Yuzu (all hp>=14, food>=16) on task "army:muster" at -350..-364,69,-478..-487 - the white clump in the base mapshot - while the board reads mine_iron "16 want 20 of 2-26 (deficit 0.75 of fuel 55/224)" and fish_lake "1 want 2" | dispatcher: fill an active job up to its `want` from muster before parking any bot; 20 % of the army stood still at day 10306 with fuel at 6 % of target
- [ ] 01:25Z foreman: bots call placeAt with TOOLS instead of blocks: `errors` has "iron_core:placeAt | Server refused to place diamond_sword at (N,N,N): the block is still water" x3 (17/h) and "... still air" x2, plus "place stone_axe" x4 (7/h), all from the mine squad (Chino carries the army's only diamond_sword) | iron_core placeAt/equip: a held item that is not a placeable block must never reach placeAt (filter to block-items and re-equip the pick after a fight) - every attempt is a lost tick and it buries the real placement failures in the same counter
- [ ] 01:25Z foreman: the field blueprint counts cells DONE that a tree stands on: `look Hotaru 14` shows an oak filling x -389..-379 / z -469..-461 (~90 cells) inside base_field_3's 27x27 crop block (-403..-377 / -488..-462, reported done), the mapshot shows the same crown in the middle of the wheat, and farm_3's one bot reports "waiting for growth" right beside it | build field_block + the farm audit: a soil cell with a log/leaf above it is not done - fell first, or raise a `tree_on_field` audit next to crops_vanished, instead of leaving a 90-cell hole in a finished field. Board stop-gap: clear_yard_trees re-activated 01:21Z — top model 01:27Z: board fix `clear_field3_tree` (lumber, clear:true, the field box). CODE still open: the pad rule clears 4 headroom but a crown above it shades the crop and the trunk cells count as done - `level`/`field_block` must fell any tree whose trunk stands in the footprint before reporting complete.
- [x] 01:30Z op: build chests FULL (digest 'CHEST FULL build', bake_bread bank ok:false 'still carrying cobbled_deepslate:32' x13, mine_iron banked cobbled_deepslate x128 by 38 bots): mine junk fills the build category so oak_planks can't be banked -> mine_iron should toss/cap cobbled_deepslate (glut rule like seeds) or a build chest must be added; Riko hung at muster (-353,69,-481, walkable 120, nothing wrong) rescued; Kaede/Sumire no_route to depot -338,69,-460 = ravine keepout line 59; fish_lake = lines 66/100 — top model 01:33Z: stone glut rule in bank() (stock ≥ max(3x target, 4096) → junk stone is dropped before the chest visit, like surplus seeds); also fuel 6/224 with 1704 logs: the furnace round now always gives the log a slot while charcoal is wanted.
- [ ] 01:34Z helpdesk (Sayaka, no_route): Sayaka at -315,52,-440 is in a 120-cell pocket and can't path to -256,?,-439. East side climbs in 1-block steps (1..5), so check the step/climb rules and whether that exit is wrongly blocked. | ticket: cannot path to -256,?,-439 (walkable area 120) @-315,52,-440
- [x] 01:42Z op: no_route Noa/Aoi from -312,67,-508 (walkable area 65, island 65) to -369,69,-500..-503 (spawn/muster side) x3+, bots on the east side of the ravine cannot reach the muster area | check for a missing bridge/exit near -312,67,-508 (see ravine keepout line 59) — top model 01:44Z: not the ravine: a 3-deep hole at -312,67,-508 (escape staircase of the 22:1xZ road test); `fill_hole_field2` fills it.
- [ ] 01:44Z helpdesk (Ume, build_stuck): fill_hole_field2 fails on all 112 cells (first -313,63,-510) with 185 dirt held; bot at -311,65,-510 beside a ravine. Check reach/goto range and the ravine cells' standing spot. | ticket: 112 cells of fill_hole_field2 left but I failed twice on each (first: -313,63,-510) @-311,65,-510
- [ ] 01:46Z op: mine_blocked mine_iron "iron:surface(no_branch)" why surface_line want:up at -328,-54,-614 x3 (Madoka +2) — -54 level, digging up to surface finds a bad line (see also 00:03Z line 97, y0 case); mine stair itself self-repaired (7 cells, 0 left) — top model 01:48Z: noted for the mine backlog (3 occurrences at the far end of the -54 trunk, z -614; the stair repaired itself): `toSurface` from the trunk end must walk the trunk home lane, not look for a surface line.
- [ ] 01:52Z op: HUNG mine_iron "iron:start" at muster yard, walkable 80, terrain clear (look Koharu 8): Koharu -365,69,-483, Riko -352,69,-480, Misaki -350,69,-484 (Ume/Aoi/Hina also stuck on iron:start at -337..-362,69,-458..-478) while mine_iron wants 25, staffed 5; iron 13/224 - iron:start does not leave the yard (see 01:25Z muster-parking line); no_route Ayame -> -103,?,-297 is the far NE wood_spawn forest (bot at 76 y, wood site reachable for others). Board: tidy_spawn bots 20, wood_spawn bots 12
- [x] 02:00Z foreman: the 01:27Z board fix `clear_field3_tree` fells NOTHING: 108 `lumber_pass {"job":"clear_field3_tree","felled":0,"planted":0,"logs":0}` by 32 bots in the last hour, and /tmp/foreman/farm_6.png still shows the whole oak crown (x -389..-379 / z -470..-460) sitting in the middle of the wheat of field_3/field_6 while farm_3/farm_6 report "waiting for growth" beside it | a `lumber` job with `clear:true` on a box that is FARM SOIL never picks a trunk (farm/zone protection, or it only accepts trees with a walkable stump): the fell path must accept a trunk standing on farmland inside its own box, or `level`/`field_block` must fell it at build time (extends line 113). I paused clear_field3_tree 01:58Z - 32 bots walked for an hour for 0 logs — top model 02:03Z: lumber found no trunk (felled:0): paused; `clear_field3_crown` = build `clear_area` 17x17x12 above the crop level (4 bots). CODE still open: `level`/`field_block` must clear crowns over the footprint before reporting complete.
- [x] 02:00Z foreman: PROOF for line 58 (the depot craft table does not stay): `events 60` shows `banked {"job":"tidy_spawn","items":{...}}` with **crafting_table:23** in one hour (+3 more by clear_field3_tree) - the groundskeeping sponge mines our utility blocks. Right now `ground -346,-460` = "grass y69, above: air" (settings.craftTable EMPTY again) and REPORT says `obtain_failed x39/30min (23 bots), most: crafted 0/1 x24` | tidy/salvage must never dig a crafting_table/furnace/chest/bed (a registered asset list, not a stray-block list); until then every craft in the army dies ~30 min after each repair. Board stop-gap: craft_table_rebuild posted 01:57Z (places one on the cell + a spare at -347,69,-460) — top model 02:03Z: tidy's keep-list now includes settings.craftTable, every registered furnace and bed (it only knew the depot chests); a table stands at -346,70,-460 again (probe).
- [ ] 02:00Z foreman: the seed-glut fix of line 108 did NOT hold - wheat_seeds 6530 in the depot + 2533 carried = 8775 of target 224 (3900%, was 7539 when the line was closed), single bots hauling whole boxes of them (Nozomi 738, Yukina 554, Yui 485, Rena 430) - and that is why `errors` counts "army:683 Unable to withdraw, Bot inventory is full" x39 (158/h, the top error of the army): a farmer whose 36 slots hold seeds can withdraw nothing else | the glut rule must also cap what a bot KEEPS (seeds over ~256 stay in the field), and withdraw must free/skip instead of failing; 9 farm jobs banking a dead item is pure trip cost — top model 02:03Z: the fix stops NEW seeds entering chests (bank drops them when stock ≥ 512); it does not drain the 6500 already banked, and farmers keep what they harvest until their next bank. Wanted: a `compost` routine (seeds → bone meal) for the quartermaster - backlog; `withdraw` of seeds must cap at what the job needs (herd took whole stacks).
- [ ] 02:00Z foreman: the floating decks of line 80 were never repaired because the repair jobs are PAUSED (base_road_2/5/6, fill_cave_road5 all paused since) - three fresh mapshots still show them, plus NEW ones: a 30-block floating line x -295 / z -559..-530 and a cluster x -322..-305 / z -526..-500 (farm_2.png), floats along the ravine rim x -330..-317 / z -432..-416 and the mine-pad stair x -321..-316 / z -477..-468 (farm_1.png), and a floating tree CROWN at -80,-276 left standing by wood_spawn after the trunk was felled (wood_spawn.png) | nothing owns floating blocks: `tidy_pass` reports `floats:0` on every tile, the float audit only looks inside the base box, and lumber does not clear the crown of a tree it fells (leaves decay, logs do not). A `floats` audit over the mapshot box should post a repair job instead of the operator finding them by eye. I re-activated fill_cave_road5 01:59Z — top model 02:03Z: those road/fill jobs are paused because they reported COMPLETE; what mapshot paints red is paving over SEALED air pockets (caves) deeper than the 3-block unlid rule. Cosmetic/safe to walk; a `fill_void` with depth 8 per cluster is the repair when hands are free.
- [ ] 02:00Z foreman: 50 bots, ONE placed bed - and beds we ALREADY OWN sit in a chest: `stock` white_bed:3, gray_bed:1, white_wool:8, shears:8 in the depot, REPORT "1 beds", while 2 of the 4 deaths of the last 30 min are phantoms (Himari 01:31, Juri 01:31) and phantom_membrane keeps turning up in the bank lines | sleeping only resets the SLEEPER's phantom timer, so a single camp bed can never stop phantoms for 49 bots: a crafted bed must be placed the day it exists (bed slots on any lit, level cell near the muster - not "when the dorm is finished", which is blocked by line 106), and the bed audit should count placed beds against enlisted bots instead of only firing on bed_missing — top model 02:03Z: base_dorm re-activated with beds:6 (4 banked beds + wool); shears 8 exist: the P4 engineer adds shearing to the sheep pen routine so the dorm can grow to 50 (each bot must sleep itself to reset its phantom timer).
- [ ] 02:01Z op: mine_iron "iron:surface(pick_worn)" mine_blocked why surface_stairs at -330,6,-499 x9 (6 bots) and surface_line at -328,-32,-593 x4: miners with a worn pick cannot climb back to the surface (stairs blocked); not the same as line 92. Also herd_sheep sheep 111/10 in pen (line 105 family) - job paused 02:01Z
- [x] 02:07Z top model: BUGS line 105 (herd cannot cull) fixed in `herd` - one visit inside the shut pen shears woolly sheep and culls adults beyond `want` (event `pen_harvest`), breeding stops at 3x want; `cull_pens` (hunt) is obsolete and paused; operators: do not pause herd_* because the pen is full
- [ ] 02:26Z foreman: the cull fix of line 105 overshot and the army EXTERMINATED its own flock: five `pen_harvest` passes in 14 min on herd_sheep (pen_1 x -402..-376 / z -540..-528) took adults 111 -> 88 -> 71 -> 55 -> 20 -> 10, `culled:16` on every single pass, `babies:0` on every single pass, yielding mutton we cannot use (food 4760/448 = 1062%) while the thing we actually need from sheep is wool for 50 beds (line 124, white_wool 8 in depot, 6 dorm bots failing to craft a bed right now) | `herd`: the cull is sized per PASS (a fixed 16) instead of by `adults - want`, and 4 bots run the pen in parallel so each one culls the same surplus again; breeding never restarts because a pass that culls does not also breed. Cull at most `adults-want`, take a pen lease (one bot per pen per pass), and shear-only while `adults <= 2*want`
- [ ] 02:26Z foreman: a BUILD SITE has no crafting table, so a build that needs a crafted part stalls forever 80 blocks from the only table: base_dorm bots Mei/Koharu/Kanade/Aoi/Hina/Noa report `obtain_failed {"item":"gray_bed","why":"crafted 0/1"}` x9 and `{"why":"ingredients did not arrive: {"gray_wool":3,"#planks":3}"}` 02:19-02:24Z while standing in the dorm (-368..-341 / -544..-537); settings.craftTable is at -346,70,-460, and `stock` has crafting_table 21 in the depot + 23 banked by tidy in one hour | `build`: a zone whose blueprint contains a crafted block (bed, door, torch) must place a crafting table inside the zone on the first pass (it is 4 planks and we own 21) instead of sending every builder on an 80-block round trip that then fails anyway — same root as line 124 (beds exist but never get placed)
- [x] 02:26Z foreman: a job whose resource is EXHAUSTED keeps its full head-count and walks the army off the map: cane_wild held 8 bots (16% of the army) at d175-d184 from base (Fuuka -269,64,-343, Kaede -261,64,-341 f16, Kurumi -260,63,-338 f15) for `banked {"sugar_cane":12}` in a whole hour, last `cane_trip {"cut":0,"banked":6,"left":0}` at 02:02Z = the stand is empty, and 3 of them then logged `no_route` home from the ravine rim (-298..-313,50..58,-434..-447) x12 | `cane` (and every gather job): a trip that finds `left:0` at its stand must decline the bot, not re-walk 180 blocks; any job whose target is >120 blocks out needs `shiftMin` or it never banks. Paused 02:25Z, cane_farm staffed instead
- [ ] 02:26Z foreman: the ENCHANTING gate (roadmap P5) can never be staffed because the pens declare the wrong product: `targets` says bookshelf 0/15 = 100% and enchanting_table 0/1 = 100% "NOBODY", which need 45 books = 135 LEATHER + paper, yet herd_cows (pen_3, 37 cows) and herd_chickens (pen_4, 28 chickens) both sit at 0 bots because `produces` on the herd jobs is food, and food is 1062% satisfied, so the deficit scaler gives them minBots 0 | job `produces` must list the real outputs (leather/feather/wool, not just food) and `targets` needs leather+paper keys, otherwise every non-food animal product is unreachable while 65 animals stand penned and fed
- [ ] 02:26Z foreman: the mine hauls stone junk 90 blocks home and throws it on the base: inside 60 s `junk_tossed` Chika cobbled_deepslate n:1280, Rin n:768, Karin n:88 x36, Mei tuff n:192, Fuuka tuff n:55, and `stock` cobblestone 20729/1728 (1200%), cobbled_deepslate banked 4542 by mine_iron in one hour | the decision not to keep deepslate/tuff belongs at the FACE (never pick it up once stock > 2x target), not at the depot gate; tossing at the base litters the yard the tidy sponge then has to clean, and the haul is ~3 m walked per banked item (REPORT cost line)
- [x] 02:37Z top model: BUGS 129 - the 8 bots came from an operator answering `UNSTAFFED cane_wild` while the job RESTED itself (restUntil, all stands cut): `wait` no longer reports a resting job as unstaffed; an empty wild stand is now shared squad knowledge (asset_audit.json, 30 min) so a new bot never walks out to learn it again; cane_wild back to 3 bots + shiftMin 20, returning bots PLANT at cane_farm themselves. Operators: more wild cane = more `params.sites`, not more bots
- [ ] 02:37Z helpdesk (Kotori, hung): mine_iron stalls at task 'iron:start' at -353,69,-486: open flat surface, has stone_pickaxe, no ore target or path chosen. Add a fallback search or timeout. | ticket: no movement for 3 min on job mine_iron, task "iron:start" @-353,69,-486
- [ ] 03:00Z foreman: the mine is now worked out on ALL FIVE levels, so the "open the next level" fix of line 92 has no next level and 19 wanted miners churn: `armyctl mine` prints y16/y0/y-16/y-32/y-54 each "branches 80 (open 0, claimed 0)", `mine level 16 dry` answers "y 16 is a level of this mine already ... the squad goes back to it", and Rin's slice reads `delegate iron_miner: {"declined":"mine: no branch for this pick on level -32 (need better pick: 0, claimed: 0)"}` with job_start mine_iron->muster->tidy_spawn->muster inside 34 s (02:52:06-02:52:40), Karin+Rin `hung {"task":"iron:start"}` at the surface (-364,69,-479 / -359,69,-488) while iron is 8/224 (96%) and `census ores` lists 357 exposed iron_ore (-367,16,-494, -362,45,-488 …) | `mine`: when every level's ledger is empty, EXTEND (new branch ring / longer branches / a second shaft) or release the head-count and hand the front to the exposed-ore job - never keep demanding 19 bots for a face that declines every one of them. Paused mine_iron 03:00Z, ores_ravine 6-16 activated instead — top model 03:02Z: stop-gap `mine level -48` (last free landing); the mine engineer adds a GROWTH rule (branches 128 → 256+, trunk beyond branch 80, iron-rich levels first, `mine_exhausted` once per hour only at a real limit).
- [ ] 03:00Z foreman: a 95-100% deficit can sit forever because `targets` cannot see a PAUSED producer: it prints "iron_ingot 8/224 96% NOBODY - a target without a producing job moves no bot", "string 2/40 95% NOBODY" and "bookshelf 0/15 100%" although ores_ravine (produces iron+fuel) and string_hunt (spiders) existed on the board, paused, all night; `targets`/`wait` only ever report the deficit, never "the producer of this key is paused" | the deficit scaler should name the paused producers of any key over ~50% deficit (targets line + a `wait` digest topic) so a daemon re-opens them; today only a human reading jobs.json finds them — top model 03:05Z: accepted, open: `targets`/`wait` should name a paused producer ("PAUSED producer: string_hunt") instead of NOBODY. string_hunt is active again (night, 4 armoured bots).
- [ ] 03:00Z foreman: every bed job and the recipe helper are hard-wired to WHITE wool, so 323 wool in the depot cannot become beds: `recipe white_bed 12` answers "white_wool x36 depot 3 MISSING 33 <- craft from white_dye x36 + black_wool x36" while stock holds gray_wool 169, light_gray_wool 108, black_wool 46, and the wool_beds job crafts `white_bed` (its first step withdraws raw_iron, of which we hold 0) - 5 finished beds (3 light_gray, 2 gray) lie unused in a chest and the board still registers 1 bed for 50 bots | bed/wool logic must treat any `*_wool`/`*_bed` as the same item (like planks/logs in the same helper): `recipe white_bed` should offer gray_bed, and the bed jobs should craft from whatever colour is in stock instead of asking for dye — top model 03:05Z: the BUILD path is colour-free since 02:45Z (dorm places gray beds, beds:50); the operator plan `wool_beds` and `recipe white_bed` are not: use item `bed` (generic, craft.js picks a colour with 3 wool in stock) in plans; `recipe` should resolve generics - open for armyctl.
- [ ] 03:00Z foreman: bots with a FULL inventory circle the depot chests and deposit nothing, burning the walk budget: REPORT "[circling] muster: 22 chest visits in 15 min, nothing deposited", "chest-circling bots (15 min, 0 deposits): Yuzu(5x) Hotaru(8x)", `errors` "army:683 | Unable to withdraw, Bot inventory is full." 79 times / 62 per h (last 26 m ago) and `putFuel | destination full` 9x, with 90 containers registered but only 87 indexed | bank/withdraw: a bot that cannot deposit into its category must widen to another category or place a new barrel (we hold 1895 oak_log) and must never re-enter the withdraw path with a full box - one deposit failure should file `bank_full`, not a silent retry loop — top model 03:05Z: bank() now drops surplus dirt/gravel/sand before the visit when ≥ 1024 are in stock (stone and seeds already had that rule), so full pockets empty at the first visit instead of queueing at full build chests.
- [ ] 03:10Z op: mine_blocked mine_iron 'iron:trunk 69:1' no_filler at -328,16,-288 -> -328,16,-277 (Botan, Nozomi x2), dripstone cave, look shows void columns beside the trunk (no filler block) — trunk continuation needs a filler/skip rule; also clear_field3_tree was re-activated with felled:0 x73 by 25 bots (see line 120), paused again 03:10Z
- [ ] 03:22Z op: HUNG mine_iron 'iron:surface(lost)'/'pick_worn' at -328,0,-670 (Tsumugi, Kaede, Rena, Wakana, Kurumi; walkable 80) + Koharu mine_blocked no_way_to_entrance at -342,-31,-646 / surface_needpick -339,-32,-646; stair broken flight 125 y-48 at -328,-487 (Honoka) - miners with lost/worn pick strand on the far trunk (y0..-32, z -646..-670), family of lines 118/125; string_hunt plan done, paused
- [ ] 03:30Z op: lava deaths 2x/15min in mine (Nozomi 03:18, Tsumugi 03:23 'tried to swim in lava') + Rena burned 03:25 at -328,0,-606 while on guard_night 'army:help: collect' at y0 (guard_night bots wander into the deep mine trunk to help); Koharu heartbeat gone after HUNG iron:surface(lost) -339,-32,-645 (= line 139) | mine paths/goto must avoid lava-adjacent cells at y<10; guard/help must not leave the surface
- [ ] 03:35Z foreman: the mine squad (16 bots = a third of the army) spent the night on a level with NO ore and nobody reported it: `mine` says "working level y -48 ... level y -48 branches 7 (open 3, claimed 1), length 32, ore blocks 0" while the same ledger holds "level y 16 ... ore blocks 638" and the job's own plan says "ONE squad on the iron/coal level y16"; meanwhile `targets` showed iron 51/224 (deficit 0.77) for hours and the only output of that level was 2 lava deaths (line above) | the mine should report `level_exhausted` (and fall back to the best level in its own ledger) when the working level's ore-block count is 0 / no ore was banked in N passes - an operator only finds this by typing `mine` by hand. Board stop-gap: `mine level 16` 03:33Z
- [ ] 03:35Z foreman: torch placing in the mine burns 296 errors/h without ever placing light: `errors` "89 / 296 per h / iron_core:q14 | Server refused to place torch at (N, N, N): the block is still air" (largest single error signature of the whole army), and `stock torch` = 0 in the depot with 745 carried | iron_core q14: the torch is placed against a face that is air (no support block) and the failure is swallowed and retried forever - place against a wall/floor block or skip the cell and report once; an unlit trunk is why mobs and creepers meet the squad at y0/-48
- [ ] 03:35Z foreman: a bot was kept at a night lakeshore by a job it declined every single pass, and died there: `events` "Chino declined x2 {job:fish_lake, why:'fish: no rod'}" then "Chino death {job:fish_lake, pos:[-438,63,-423], task:'food:fish 2/7'}" (status showed "weak: Chino(hp0/f17)", stock fishing_rod 0 in depot) | dispatcher/fish: a bot that declines its job for a MISSING TOOL must be handed back to the board (and the job noted `no_tool`), not re-posted to the same exposed site - it stood in mob country all night producing nothing. Board stop-gap: fish_lake paused 03:34Z (food is 3757 edible, 5.7/min vs 1.0 needed)
- [ ] 03:35Z foreman: `placeAt` retries into blocks that can never accept a place: `errors` "34 / 204 per h | Server refused to place cobblestone at (N,N,N): the block is still water", "22 / 126 per h | ... the block is still rail", "8 / 48 per h | ... cobbled_deepslate ... the block is still rail" - i.e. our own minecart rail and open water are being paved over, ~380 failed places/h | lib/army.js placeAt: treat rail/water/other non-replaceable targets as "skip + report once" (never overwrite our own rail), the caller should pick the next cell instead of looping
- [ ] 03:35Z foreman: a 4-block pit sits in the base's main mine route and `tidy` never sees it: `ground Yukina -345,-499 -344,-498 -343,-497 -342,-497 -341,-496` gives y68 y68 y68 **y64** y68 (hole at -342,64,-497, 5 blocks off the depot road, red floats around it in base.png at x -345..-341 / z -499..-496) while `events` shows "42 bots tidy_pass x231 {tile:[-360,-415], found:0, pillars:0, holes:0, floats:0}" | tidy's hole/float detector finds nothing anywhere (same symptom as the 02:00Z float line): it must scan the columns of its tile against the base's ONE ground level y68 and fill any column ≥2 below it - a hole on the road every miner walks is a fall death waiting
- [x] 03:35Z top model: foreman's repeating plan `toolsmith_diamond` turned ALL 263 diamonds into 54 diamond pickaxes + 51 swords in 5 minutes (then looped on 'none of diamond' x40 = warning storm). Tools for 50 bots are fine, but nothing was left for the enchanting table (2), armour or the reserve - removed. RULE for board plans: a `repeat:true` crafting plan needs a stop (`until`/stock floor) - keep ≥ 32 diamonds in the depot; craft per need, not per stock.
- [ ] 03:36Z helpdesk (Madoka, build_stuck): base_dorm: 11 cells failed twice each, first -359,69,-539; bot at -366,69,-542 in cave. Log the failure reason; check cell reachability/placement. | ticket: 11 cells of base_dorm left but I failed twice on each (first: -359,69,-539) @-366,69,-542
- [x] 03:40Z op: BED SPOT WRONG Y: sleeper bed target -341,70,-456 is 2 blocks above ground (ground -341,-456 y=68 grass, air above; -340,-456 cobble) so Ume (light_gray_bed in hand) gets place_failed noref 'no filler block to build a support' and spawn_bed_missing fires from 6+ bots since 03:36Z - the bed cell should be y=69 (on the ground) or the placer must carry a filler; base_dorm (11 leftover roof cells y73 no_los, lines 106/147) unchanged, not re-logged — top model 03:43Z: the camp bed spot was levelled away by the yard pad; 49 beds stand in the dorm (probe: 98 bed blocks) - sleeper now uses the dorm bed -363,69,-539 and the camp bed left settings.respawnBeds.
- [ ] 03:39Z helpdesk (Misaki, no_route): Muster target -351,69,-486 is unreachable from Misaki's 121-cell pocket at -301,58,-442 (higher terrain east, lower west). Pick a reachable muster point or add a route. | ticket: cannot path to -351,69,-486 (walkable area 121) @-301,58,-442
- [x] 03:58Z foreman: the `level` blueprint calls a pad COMPLETE while columns inside its own box sit 1-2 blocks BELOW pad level, and that rim is the travel dead zone that came back after the 00:03Z fix: base_mine_pad note says "build complete" for zone mine x -337..-323 / z -508..-492 y68, but `ground Madoka -334,-498 -333,-498 -332,-498 -331,-498` = y66 stone, y67, y69 CHEST, y68 - the haul chest stands on a 2-block rim and REPORT logs "WARNING STORM: travel_fail x47/30min (15 bots), most: timeout/stop @-332,69,-498 x9" (tries 9, fails 0, last ok, d 3) | build/level: completion must compare EVERY column of the box against the target y, not only the cells it managed to place; a haul chest may only be registered on a cell whose 4 neighbours are at pad level — top model 04:25Z: (1) base_mine_pad's 6 'leftover' cells and fill_minepad_void's 26 were THE MINE STAIRS (-330/-329,-501..-507): build now skips ground/fill cells above the stair floor (stairCols from iron_mine.json) instead of fighting the miners (`stair_broken air:blocked@-329,68,-501`); (2) padAudit judges every column of a level pad (low = pad cell not solid, high = natural ground in the headroom; water/crops/furniture/keepOut/stairs/other blueprints' holes are not terrain), the quartermaster audits one pad in view per round -> `pad_uneven` + the pad's own job re-activated (max 1/h). Proof: audit run through the nearest bot on all 14 pads: mine/dorm/pens/field 2,4,5 = 0 low; yard 1, field_3 20 (the oak grove + pond corner -385,-469..-464 y66), field_6 18, field_7 3 -> those four pad jobs re-run as the repair (04:20Z/04:24Z). The foreman's columns -334/-333,-498 read y68 today. NOT done: 'haul chest only on a cell with 4 level neighbours' (mine files, not mine to edit).
- [x] 03:58Z foreman: 49 beds now STAND in the dorm (top model 03:43Z, line 148) and bots still die of insomnia because nobody but the sleeper ever lies down: `deaths 30` = "03:24:50 Chino was slain by Phantom" + "03:27:29 Yuzu was slain by Phantom" (2 of 6 deaths), REPORT still counts "1 beds", and no bot reports in_bed except the sleeper - phantom spawning is per-bot (time since THAT bot slept), so nightSkip protects only the sleeper | army_jobs.js sleeper/night: at dusk every bot near settings.respawnBeds should claim a free dorm bed and sleep (that also sets its respawn); the sleeper keeps skipping the night for the ones in the field — top model 04:24Z: every bot owns a dorm bed (roster index -> settings.respawnBeds minus the sleeper's) and lies down every ~3rd dusk (due 25 min after its last rest; phantoms need 60): army_jobs.js bedDue/bedtime, hooked into withHandover (api.stop is wrapped so a running slice yields ~dist/3+25 s before 12600; not for delegate/steps/scout/sleeper, not underground, not > 150 blocks away; real nights = 1.5 s in bed only). Proof: 04:22:50Z `slept {own:true}` by Mio, Tsumugi, Kokoro, Madoka, Hotaru in five different beds, night over 5 s later; a walker the dispatcher re-assigns on the way retries from its next slice. REPORT now reads `20 furnaces, 49 beds`.
- [x] 03:58Z foreman: the headline the owner reads LIES about the biggest flow in the base: REPORT FIELD ANOMALIES says "WHEAT NOT BAKED: 5378 wheat in chests, 0 bread baked in 30 min - is qm_scan staffed?" while `events` shows bake_bread (6 bots) banking bread without pause ("14 bots banked x422 {job:bake_bread, items:{bread:6}}", board banked 1h bread:2517) and `stock bread` = depot 4844 | inspector: the "not baked" anomaly counts bread from a source that misses bake_bread's bank events - a false top-line anomaly costs every operator a wake-up — top model 04:24Z: the `craft` verb now reports food it makes as `cooked {raw:'wheat->bread'}` (plans are producers too; cook() stays the only reporter for qm_scan), inspector foodLedger adds breadIn30 = bread banked by ANY job net of canteen draws, and WHEAT NOT BAKED needs: no bread cooked AND none banked in 30 min AND < 512 bread on the shelf. Proof: REPORT 04:13Z has no WHEAT NOT BAKED line (wheat 5857, bread 5116), latest.json breadIn30 41634 - that number is haul churn (cull_pens/bake_bread bank the same 244-496 loaves again and again), which is why it only gates the alarm and `made30` still counts `cooked`.
- [x] 03:58Z foreman: torches never reach the depot, so every torch-gated job (base_light, the base_hall/base_depot deadlock of line 50) starves while `targets` reports torch 583/224 = done: `stock torch` = depot 0, carried 494 (Aoi:32 ...), and craft_table_fix was written for "torch depot 0" | bank/quartermaster: torch must be a banked category (carried-only stock is not stock), and `targets` should score DEPOT stock for items that jobs withdraw — top model 04:24Z: craftToTargets judges the DEPOT (+ <= 25 % of target for loads carried beyond the pocket kits of 16 torches / 8 sticks per bot), hard ceiling depot + carried-beyond-kits >= 2x target, a key that made nothing rests 30 min (enchanting_table took rounds from the torches); pockets rule banks torches > 32 outside build/light/ores/deck (the 7 bots with 24-32 were all miners = kit 48); `targets` prints have = depot + carried and scores SUPPLY rows on the depot. Proof: `crafted_to_target torch` 04:12Z made 160, 04:19Z 64+128+128 (before: 583/224 = never), depot torch 0 -> 93 and rising while the pockets fill (carried 619 of 800 kit).
- [x] 03:58Z foreman: roads are cut as trenches through rising ground instead of ramping with it, leaving 4-block walls along the paving (a bot on the road cannot leave it, a bot beside it cannot enter - see the no_route tickets at the base edge): `ground Nagisa -270,-491 -270,-492 -266,-492 -262,-492` = cobble y68 (road 11) then natural grass y72 y72 y72 one block north, visible in /tmp/foreman/farm_2.png as the float line at x -272..-261 / z -492; fill_road6_trench is the same hand patch for road 6 | build road blueprint: where the natural ground of a road cell is >=2 above the road y, cut a 3-wide 1:1 ramp band on both shoulders (or follow the ground) instead of leaving a vertical wall — top model 04:25Z: blueprints/road.js `shoulder=4`: the road keeps its height, s cells out from the paving natural ground may stand at most s above it (cut back in 1-block steps on both sides), and no turf roof stays over the paving (the y72 row over road 2 at x -272..-261 = the red float line); shoulder cells are `natural:true` air = the build job cuts ONLY natural ground there, never a column of another build job's structure, never under a torch/chest. Proof: `ground` across road 2 at x -270: z -492 y72 grass -> y68 cobble, z -489 y71 -> y70 and falling, build_pass left 130 -> 47 at 04:23Z; roads 1-11 re-activated (rev+1, 3 bots each) as the repair.
- [x] 04:15Z top model: URGENT self-found: the quartermaster's `adoptStrayChests` booked the second halves of our own depot double chests as stray old-base chests (`settings.legacyChests.found` 7 entries) and `drainLegacy` emptied them (`legacy_drained` 1278 items) and took chests DOWN (`legacy_chest_removed -364,69,-509` x2). Fixed: a container touching a registered one or standing inside a build job's footprint is never a stray; legacyChests cleared. The auto-reactivated base_depot build re-places what was removed.
- [ ] 04:29Z helpdesk (Chika, place_failed): place_failed 'unreachable' for dirt at -394,67,-492 (bot at -395,69,-492, 2 below, reposition tried): code owner must check the placement reference face/reach, or skip the target. | ticket: dirt at -394,67,-492: unreachable @-395,69,-492
- [x] 04:31Z top model: DEPOT CHESTS DID NOT MERGE (owner; main 04:15Z: 80 chests = 70 single + 10 halves): a chest faces its placer, and placeBlock clicks in the same tick as its forced lookAt - the look packet leaves one physics tick later, so the server still had the yaw of the walk along the aisle (60 of 80 faced east/west). depot_rows chests now carry facing:south + half; build: placeChest (stand in the aisle, look, wait 2 ticks, sneak-click the PARTNER side = joins exactly that chest / sneak-click the floor while the partner is missing; facing, type and joined neighbour read back, else taken back and retried) + redoChest for standing chests (claim on the board -> off settings.chests -> contents banked elsewhere (bank avoid) -> opened again: empty or nothing is dug -> dig -> placeChest); missing chests before re-makes; placeOriented (beds, gates) looks first too. Proof: probe 04:31Z = 84 chests, 0 single, 84 halves = 42 pairs all facing south (rows Rs Ls x7), settings.chests books 42 half-0 cells and 0 half-1, base_depot auto-paused build complete, /tmp/depot.png. adoptStrayChests rule of 04:15Z untouched.
- [ ] 04:38Z foreman: the RAVINE swallows bots and has no way back out - 5 `no_route` tickets in 60 min from INSIDE it at y48-58 (Madoka -318,58,-418, Yuzu -315,51,-438, Misaki -301,58,-442, Nanami -317,52,-439, Kurumi -308,48,-447), every one "area 120, island 150" = the bot stands in a pocket with no path home, and the rim x -330..-293 / z -482..-412 is unrailed and unlit in /tmp/foreman/base_road_9.png and base_road_5.png | WORLD.md calls it keep-out but "NOT fenced yet - no blueprint fits": we need a RIM blueprint that follows an irregular edge (fence + gate where a road crosses, `pen` lids the whole footprint and `wall_ring` cuts field 1) and ONE lit stairwell from the floor to the lip, so a bot that falls in walks out instead of filing a ticket every time — top model 04:42Z: accepted, open: needs (1) a fence that FOLLOWS the rim (no blueprint can do it yet: `pen` fills its interior, `wall_ring` refuses fences) and (2) ONE built stair out at the south end; travel already avoids stepping below base level inside the keep-out and walks trapped bots to `keepOut.exit` (-325,69,-409), but pockets at y48-58 do not connect to it. Next code batch.
- [x] 04:38Z foreman: an OBSOLETE job came back on the board and starved the real producers behind it: cull_pens (hunt; declared obsolete and paused in line 126 because `herd` now culls inside the pen) held 6 bots (12 % of the army) for ~35 trips this hour with `kills:0 haul:0 banked:0` (Kokoro x9, Mio x7, Riko x6, site -330,69,-390), and its plan text still claims "pen_3 cows 37 of cap 10" while `herd_cows` reads inPen 12/10 and declines - so the job walks a squad to a pen that the herd job already keeps | nothing marks a retired handler: `put`/`patch` should refuse or loudly flag an id whose type-handler is obsolete (line 126), and a `hunt`/gather job whose trips return kills:0 haul:0 banked:0 N times in a row must release its head-count instead of re-walking. Paused cull_pens 04:37Z — top model 04:42Z: `cull_pens` removed from the board for good (herd culls inside the pen).
- [x] 04:38Z foreman: mapshot paints cobbled_deepslate with the unknown-block fallback, which is the SAME magenta it uses for mobs: road 9 (x -285..-283 / z -492..-402, paved with mine deepslate) is a 90-block magenta band in /tmp/foreman/base_road_9.png and base_road_5.png and reads at a glance as a swarm standing on the road - I nearly posted a guard job for a road | bots/army/mapshot.js COL: `/^(stone|andesite|...|deepslate|tuff|gravel)$/` never matches `cobbled_deepslate` (nor deepslate_tiles / polished_*); give the deepslate family the grey rule and make the unknown fallback a colour no entity uses — top model 04:42Z: mapshot COL knows cobbled_deepslate/bricks/polished stone; the unknown-block fallback is dull brown-grey, never the mobs' magenta.
- [x] 04:38Z foreman: the first line of every session is misleading - `ops/status.sh` prints "17/10 manager shards" because the 10 is hardcoded (`pgrep -fc '^node manager.js'`/10) while 17 shards run (pids 1356071-1356105, one start); a REAL shard loss would print "8/10" and look exactly as normal as today | ops/status.sh line 9: take the expected count from the manager/roster config (or print DOWN only when a shard died) so the count means something — top model 04:42Z: status.sh and escalate.sh take the expected shard count from the roster (50 bots = 17) and status.sh prints `!! SHARDS: n of m` when they differ.
- [ ] 04:43Z op: fill_cave_road1 BUILD DONE but BUILD STUCK 10 cells unreachable (-340,67,-500 | -343,67,-496 | -338,68,-503 …) + VOID UNDER PAD 94 air cells at -343,61..67,-499 (same class as fill_cave_road5, line 74); Hina no_route -> -285,?,-456 from -340,67,-503 (area 1 island 1, underground cave, pit_filled/pillared_out repeat); Chika no_route -> -123,?,-309 far NE. Job left paused; fill_void blueprint needs a builder that can reach cave cells
- [ ] 04:50Z top model: RE-RUNNING A ROAD OPENS THE CAVES UNDER IT: base_road_1 rev 5 took its paving off over the cave mouth at x -343..-335 / z -507..-496 (build unlid rule: a deck over a run <= 3 deep comes off to be filled from the ground), the neighbouring columns were 5-8 deep, builders and passers-by fell to y60-63 and `dug_out` x8 through the mine pad (trenches -337..-335,-502 and -333,-499..-496 = the foreman's "low columns by the haul chest"; padAudit caught them: `pad_uneven base_mine_pad low 6` 04:32Z). Repaired by board: fill_cave_road1 (done 04:41Z), fill_shaft_minepad (done), road 1 re-paved (26 paving cells -> 0 by 04:50Z, no dug_out there since 04:41Z); left: 3 sub-base cells under decks over a 4-deep void (-340,67,-523 | -338,67,-507 | -340,67,-505: put:unreachable) and build_pass done 31 / left 31 while it lasted = unlid and paving undid each other | build: unlid only when EVERY column of the open run (flood at y-1) is shallow, else plan a fill_void first; dug_out should fill its own trench behind it (rule 4)
- [ ] 05:05Z foreman: `clear_area` REPORTS COMPLETE WITH A WHOLE TREE STILL INSIDE ITS BOX: clear_field3_crown (origin -384,70,-465, w17 d17 h12 = x -392..-376 / z -473..-457, y70..81) auto-paused with note "build complete - BUT 3 cells are left (-387,80,-464 | -386,79,-465 | -386,76,-464)", yet `look Suzu 22` at 05:03Z prints a SOLID 9x8 `T` blob at x -389..-381 / z -468..-461 (≈55 tree blocks, ~50 wheat cells lost) sitting inside field_3's crop grid, and /tmp/foreman/base.png + farm_1.png paint the same crown - this is the job the top model created at 02:03Z to answer line 120, so the crown has now survived THREE attempts | build/clear_area: the completion check counts cells it ATTEMPTED, not the blocks actually left in the box - it must re-scan its box and refuse to report complete while any non-air, non-blueprint block stands in it. PROOF the cell list is FROZEN: I re-activated it 05:02Z (6 bots) -> `build_pass {"done":0,"left":3,"gaveUp":0}` + `declined {"why":"build: complete"}`, then patched the box BIGGER at 05:09Z (rev 2, origin -385,69,-465, w19 d19 h14, i.e. two blocks wider on every side and down to the trunk at y69) -> 11 bots produced the IDENTICAL `build_leftover {"left":3,"cells":"-387,80,-464 | -386,76,-464 | -386,79,-465"}` and "build: complete" again, so a rev bump does not re-enumerate the box. Job paused again 05:10Z; the tree still stands
- [ ] 05:05Z foreman: ONE WATER CELL SERIALISES THE WHOLE BUILD SPONGE: base_field_3 at 04:59Z shows `build_pass {"blueprint":"field_block","done":0,"left":1,"gaveUp":1}` x21 by 13 bots together with `declined x21 {"why":"build: the rest waits for a water cell in work"}` - 13 builders decline every pass and fall back to "army:muster" because a single water cell is claimed by one bot, and we DO hold buckets now (bucket 4, water_bucket 1), so this is not line 56's missing-bucket case | build: a cell "in work" must block only that cell, not the job - the others must take the remaining cells (and the water cell should be deferred to the end of the cell list, not gate the pass)
- [ ] 05:05Z foreman: REPLANTING REGRESSED IN THE WILD FOREST TOO, so we now cut everywhere and plant nowhere: `lumber_pass {"job":"wood_spawn","felled":2,"planted":0,"logs":10}` x45 by 12 bots at 05:00Z and `stock sapling` = 0 in the depot AND 0 carried across all 50 bots, while line 61 (21:36Z) recorded "wood_spawn replants every sapling in the wild forest" - felling banks logs and apples but no sapling ever enters stock, which is also why the floating crowns of line 123 keep appearing (x -135,-309 and -108,-315 in /tmp/foreman/wood_spawn.png) | lumber: felling must break the crown's leaves and bank the saplings it drops, and a pass with planted:0 and saplings 0 must report `no_saplings` instead of a silent success
- [ ] 05:05Z foreman: THE ANY-TIME SPONGE SATURATES AND THEN PARKS THE ARMY: at 05:00Z `declined x23 {"job":"tidy_spawn","why":"tidy: every tile was tidied in the last 30 min"}` for 15 bots, and `field` showed 16 of 50 bots (Akari Aoi Hinata Juri Kaede Kanade Kokoro Madoka Mashiro Nozomi Sakura Sayaka Tsumugi Yui Yukina Hotaru) standing in one 20x15 clump at x -350..-373 / z -479..-494 with task "army:muster" by day (time 6445), visible as the white cluster in /tmp/foreman/base.png - `settings.fallback` cannot do its job because the groundskeeping sponge runs out of work while real deficits (iron_ingot 43/224 = 81%) had their only producer paused | the fallback must never decline: when tidy has no tile left, status.sh/`wait` must raise "SPONGE SATURATED: N fit bots idle by day" as a top problem and the scaler should push them onto the worst deficit's producer. Board fix applied 05:02Z: smelt_depot active (6), mine_iron 15->19, guard_muster 4->1, clear_field3_crown active (6), farm_bank paused - the clump is down to 2
- [ ] 05:08Z foreman: THE IRON CHAIN HAS NO SHELF, so `targets` will report "iron_ingot 43/224 81% - NOBODY" for ever: I activated smelt_depot (6 bots) at 05:02Z to fill that deficit and every pass failed on its own materials - `step {"job":"smelt_depot","i":7,"do":"withdraw","ok":false,"why":"none of raw_iron in the chest index"}` x6 and `plan_idle {"why":"a full pass of the repeat plan produced nothing"}`, 10 s AFTER `banked x81 {"job":"mine_iron","items":{"raw_iron":11,"raw_gold":7}}`, with `stock raw_iron` = depot 0 / carried 7: the forge swallows every raw_iron and ingot the moment it lands (line 102's 32-ingot reserve is not holding at 46 ingots), so no plan, crafter or smelter can ever withdraw iron | either the forge must leave the `iron` target's worth on the shelf before it converts anything, or iron_ingot must stop being a stock target with no reachable supply - as it stands the 81% deficit permanently drags bots onto a job that cannot produce. I paused smelt_depot again 05:08Z (9 bots looped on it for 0 output)
- [ ] 05:34Z helpdesk (Mio, hung): Job base_hall 'army:build core' stalls silently at -342,69,-519 (open ground, hp 20). Inventory is only 64 dirt and tools, so the build likely waits for materials the code never withdraws from the depot (crafting table -346,70,-460). Fix: make the build fetch its materials or fail with a clear error | ticket: no movement for 3 min on job base_hall, task "army:build core" @-342,69,-519
- [x] 05:40Z foreman: A MINER DIES IN LAVA EVERY 3 MINUTES AND WALKS STRAIGHT BACK INTO IT: `deaths 30` = 8x "tried to swim in lava", ALL Shiori (deaths 12 on her dossier), 05:10:19 05:13:37 05:16:46 05:19:32 05:22:30 05:25:29 05:28:43 05:31:31, last death pos [-219,-32,-712] task "iron:to-face 67:1", and she is back at -326,-32,-619 on the same job now - that is the army's whole death rate (deaths/1h 10 {"mine_iron":10}). Root cause visible in `mine`: working level y16 has **0 open branches** (exhausted, as do y0 and y-16), so the squad of 19 has slid down to y-32 (30 open / 21 claimed) which is lava-lake depth | iron_core: a branch whose miner died to lava must be blacklisted (the claim is re-handed to the same bot after respawn), and the mine must report `level_exhausted` and refuse to keep 19 bots on a level with 0 open branches instead of silently drifting to the deepest one - top model 05:58Z: CAUSE: `walkLine` only asked "is the next cell SOLID?" - lava is not, so `to-face` stepped into a gallery that lava had run into after it was opened (-32 67:1 @108; Madoka died there first at 04:42 on her way out, then 04:45), and the death hook bumped the run while `claimBranch` handed the respawned bot "its own unfinished branch" again = 11/11 lava deaths of the hour on ONE branch. CHANGE (iron_core.js / iron_miner.js): every walkLine step and the gallery walk of mineBranch LOOK 2 cells ahead at feet/head/above (`clearHot`): a trickle is plugged from 2 cells away and the plugs are opened by digCell's liquid protocol (= the repair pass), a BODY of lava (hot column / lava both sides) or no filler = 2-high wall, 3 cells back, `why:lava`; a death or burn on/beside a branch releases the claim, skips it for that bot (1 h + `hazardBy` for ever) and, when burned, sets `hazard:'lava', hazardAt` on the branch record: nobody mines it, ONE other miner with >=16 filler may take it as `repair`, 2 tries / a dead repairer / a wall = `done why:lava`; ONE `mine_hazard {level,branch,at,kind}` (then `mine_hazard_sealed`); a burn while alive ends the routine and runs for the branch mouth. ALSO FOUND: the 05:40 cache wipe was a flip-flop - bots remembering the 60-s entrance and bots with the board's renamed each other's cache onto the one `.stale` file (the restored copy died within 2 min, `dug -1`): caches are now set aside PER KEY and come back, a recall follows the live cache; cache restored (levels, dug 132, -32 67:1 hazard on record); a lost ledger is RE-LEARNED by walking the open galleries (old code sealed every dug mouth `cave len 1`: 148 such records reopened) - level 16's 160 branches were re-learned in 10 min. `level_exhausted`/drifting to the deepest level is NOT in this fix. PROOF: mock walks (trickle: walked through, 0 burns, side source plugged; lake: wall at 1 cell, retreat, `lava`), offline ledger test (victim never re-claims, one report, repair by another bot, dead repairer closes); LIVE `deaths 25` at 05:58Z = no lava death since 05:34:48 (Shiori mining 16 27:1, hooks v2 on), 4 bank deliveries in 12 min. NOT YET SEEN LIVE: a repair claim at -32 67:1 (nobody works level -32 since the re-learn)
- [ ] 05:40Z foreman: THE CRAFT CHAIN DOES NOT RECURSE THROUGH PLANKS, so 19 miners run out of picks on top of 2677 logs: `obtain_failed {"item":"stick","n":8,"why":"ingredients did not arrive: {\"#planks\":4}"}` from Erika, Nozomi, Botan, Akari and Shiori at 05:31-05:33, and right after it `obtain_failed {"item":"iron_pickaxe","n":1,"why":"ingredients did not arrive: {\"stick\":2}"}` + `mine_not_ready x5 {"missing":["pick_spare"],"note":"the depot could not supply it: bot handed back"}`, while `stock` = oak_log depot 2626 / carried 103 and oak_planks 0, stick 20 of target 128; Akari is the QUARTERMASTER standing at the depot table (-346,70,-460) and still reports `crafted_to_target {"item":"stick","made":0,"stock":20,"target":128}` | obtain/craft: when an ingredient (#planks) is itself craftable from something in stock (oak_log), craft it first - today the chain stops at the first missing tag and hands the bot back. Board stopgap posted 05:40Z: steps job `craft_sticks` (Erika, Wakana) does log->plank->stick by hand
- [ ] 05:40Z foreman: WE THROW AWAY OUR ONLY BUILDING STONE: `stock cobblestone` = depot **0**, carried 1, yet the miners junk ~800 cobbled_deepslate per hour at the depot - `junk_tossed {"item":"cobbled_deepslate","n":320}` (Iroha), `n:189` (Shiori), `n:155` + `n:95` (Akari, Kokoro), `n:24` (Erika) in the last 3 minutes alone, and `errors 8` shows `iron_core:placeAt | Server refused to place cobbled_deepslate` 12x. Meanwhile everything stone-hungry starves: `audit_structure {"job":"base_road_12","missing":278,"of":429}`, base_hall `build_pass {"done":0,"left":3,"fails":{"put:locked":9}}`, and roads/walls on /tmp/foreman/base.png stop mid-run | the junk list must not contain cobbled_deepslate while cobblestone stock is 0 - deepslate is the same building block for roads, pads and walls (blueprints should accept either), or the quartermaster must smelt/bank it instead of dropping it
- [ ] 05:40Z foreman: 21 BOTS BURN THE DAY WALKING INTO A CHEST: `travel_fail x25/30min` all aimed at depot columns - `{"why":"timeout/stop","to":[-359,69,-500],"tries":8,"last":"fail","d":37}` x10 by 6 bots at 05:33:19, `to:[-359,69,-512] tries 7 d 56` x7, `to:[-359,69,-506] d 40`, and `ground Rin -359,-500` answers "ground y=69 (**chest**), above: air" (-358 and -360 are chests too, -359,-501 is a crafting_table) - the travel goal is the furniture block itself, so the bot can never stand on it, gives up after 7-8 tries still 37-56 blocks out, and REPORT counts it as standing time (33 % of bot-hours produced nothing) | lib/army.js travel / the depot withdraw path: a goal whose ground column is a chest/table/furnace must be converted to the adjacent walkable column (GoalGetToBlock / GoalNear range 1), and `travel_fail` with `last:"fail"` on the SAME target 25x in 30 min must raise a field anomaly instead of being retried for ever
- [ ] 05:40Z foreman: THE BASE PADS ARE FULL OF PITS AND THE JOBS THAT SHOULD FILL THEM AUTO-PAUSE INSTEAD OF ESCALATING: `audit_rough {"columns":483,"onPads":398,"holes":44,"bumps":37}` and `pad_uneven {"job":"base_core_pad","low":6,"high":26,"cells":"-351,66,-497 | -350,66,-497 | -349,66,-498 | -349,66,-497 | -342,64,-516 | -342,67,-513"}` - columns 2 to 5 blocks below the y68 pad in the middle of the core - and bots fall in and cannot climb out: `stranded {"at":[-345,66,-494],"area":120}` (Juri) then `no_route {"from":[-345,69,-494],"to":[-345,66,-494],"area":120,"island":150}` (Hotaru, trying to reach her). Every job meant to fix this is auto-paused: `BUILD STUCK: base_dorm, base_pen_4, fill_ring_field2, fill_ring_field1, fill_hole_field2 auto-paused (build_stuck) - 11 cells nobody could do: -359,69,-539 | -368,73,-538 | -362,73,-538` (pri 95/96 jobs!) | build: `build_stuck` on N cells must pause only those cells and keep the pass going, and a pad that is still uneven after the level job "completed" must file a ticket rather than sit paused while the hole swallows bots
- [ ] 05:39Z helpdesk (Rena, no_route): Rena at -311,49,-445 is stuck in a 120-block pocket with steep walls. No route to -346,70,-460 (21 up). Treat small walkable areas as boxed-in so the pillar/stair escape runs. She has 64 cobbled_deepslate. | ticket: cannot path to -346,70,-460 (walkable area 120) @-311,49,-445
- [x] 05:50Z top model: SHEEP ESCAPED (owner: 7 in pen 1, 76 outside) + STRAY BLOCKS beside the depot + "holes in the dorm": ONE cause = the escape edits (lib/army.js digOut/stairUp) treated our own buildings as a cave and outlived the bot. Evidence: Koharu died 03:41:08Z at y-15 in the mine, her running digOut went on at the respawn bed (`dug_out from [-333,-15,-563] to [-388,69,-540]` = INSIDE pen 1, 62 s later), left 11 cobbled_deepslate step blocks on the pen floor + one on the fence post -376,70,-532; the flock count fell 24 -> 7 from 03:42Z on (sheep hop the fence from a block beside it). Iroha 04:15Z `dug_out ... to [-358,69,-538]` = in the dorm; Akari `... to [4,64,-6]` = world spawn. The 26 single grass blocks at y69 E of the depot rows (x -356..-341 / z -509..-499) = dirt steps of bots that climbed out of the cave under road 1: stairUp's target was "start + 4", so it went on building steps in the open (Hotaru `from [-345,66,-494] to [-347,70,-505]`). FIX: army.js registry `ours()` (every cell of every build blueprint, board + archive; pen boxes; finished pads) -> digOut never runs inside something of ours (`shut_in`), stairUp dies with the bot (`escape_aborted`, seen 05:39Z), never digs a cell of ours, never places in a pen, stops when the bot is OUT, takes its open-air steps away again; placeHard removes its side supports; blocks.js: placeBlock refuses a block inside a pen (`pen`), digBlock refuses a cell of another blueprint (`ours`, build jobs pass `own`); herd: the herder clears `pen_litter` (05:34Z `found 4 cleared 4`, probe: 0 blocks inside, all four rings closed, gates open:false, nothing higher outside) and the job no longer rests while animals stand outside; tidy: rule `stray` (junk above the pad level inside a built zone that is no cell of ours) + its excludes come from the registry (archived structures were unprotected, pads on the board hid whole zones). Flock: want 30 + 4 herders, 7 -> 19 inside at 05:50Z.
- [x] 05:50Z top model: FIVE FURNACES AIR (-361..-357,69,-523): not a job - the creeper of 04:00:33Z at -358,69,-525 (terrain_debt `crater`, heard by 14 bots). Nobody came because 5 cells are under auditStructures' 6-cell/5 % rule and a crater was only logged. FIX: lost furniture / fence / water = damage by itself, a crater within reach makes that structure (and pad) the next one audited with threshold 1, one re-activation per structure per hour; base_hall restored from the archive: probe 20/20 furnaces stand 05:42Z.
- [x] 05:50Z top model: ARCHIVED JOBS WERE INVISIBLE: `prune` moved every finished pad/build to attic/jobs-archive.jsonl and (a) padAudit only judged pads ON the board = no pad was ever re-judged (first round after the fix: `pad_uneven base_core_pad low 6 high 26`, `base_yard_pad low 1` -> both re-ran), (b) `plan-base` read its pins from the board alone: the dry run laid the whole base out anew (core 70 blocks east). Both read the archive now; --put never re-puts a finished job, never puts a road that is >= 80 % paved under another number (road 13 = roads 1+3), keeps operator-tuned args (dorm beds 50) and never re-aims a delegate job. MY MISTAKE 05:37Z: the first --put moved mine_iron's entrance -330 -> -329 for 60 s; iron_core keys its cache by the entrance and set it aside twice = branch ledgers lost (levels + stair depth 132 restored by hand from the stairs_done reports; miners re-learn the branches).
- [x] 05:50Z top model: THE BASE IS BUMPY: `plan-base` emits INFILL `level` tiles (<= 31x31, odd sizes) for everything inside the wall line that is no zone pad, road or keep-out (+3), six chains nearest the plaza first, hills get `clear` = their height, a tile with a fall >= 6 or water is named and left out (7 tiles); 88 tiles put, 9 done in the first 10 min. build: a body cell on the ground layer (field path/soil) over a shallow hole is filled from the ground up (never a lid); an `under` cell 3+ deep is dropped as gravel (fillCell) instead of 10 builders spinning on `put:locked` (hall column -342,64..67,-516).
- [x] 05:50Z top model: FIELDS BUMPY: probe of fields 1-3 = 19 tiles farmed one step DOWN (soil block gone with an escape staircase/creeper, the terrain-following farmer tilled the dent) + the path line z -428 of field 1 open 2 deep; no trampling (no dirt tile inside any field). farm: a plan field is ONE level - a bare/ripe tile 1-2 low is filled back with dirt and tilled at y, 1-2 high is cut (`farm_pass st.levelled`); base_field_1 restored for the path line.
- [x] 05:50Z top model: CANE FARM 100-180 blocks from the army (unticked chunks): blueprint `cane_block` (plus-tiling of isolated water cells, 268 cane cells in 21x21), plan-base zone `cane` at x -282..-262 / z -489..-469 (85 blocks from muster), `cane_farm` points at it, `cane_lake` (wild mode) transplants the lake stand after `base_cane`; every growing job > 128 from muster reports `farm_far` once an hour.
- [x] 05:50Z top model: (main 8) `obtain` gave up when a withdraw came back short (`#planks` drained, 2677 logs in stock): one retry with the short ingredient counted as pockets-only -> logs -> planks -> sticks (`obtain_retry`). (main 9) containers: walk to a standable aisle cell within 3.5 of the chest/furnace (`walkTo`), not to the chest cell with range 2.
- [ ] 05:50Z top model: NOT a wall hole: main's dorm probe used the slot width 29, the dorm blueprint is 27 wide (x -369..-343): all 476 cells stand except 10 roof torches (no_los) + 1 bed; gates shut. Left open: (a) herders bring 5-8 sheep in tow but `herded n:0-2` - the 1-wide gate; a second gate or a funnel would refill the pen faster; (b) crafting_table inside pen 3 at -314,69,-395; (c) `stranded` in the caves under the depot (Aoi -369,39,-512) = the cave system of lines 74/04:43Z, needs fill_void from inside.
- [ ] 05:52Z foreman: THE MINE STAIR REPAIR FIGHTS ITS OWN TORCHES: `errors 8` top signature is LIVE and climbing - 41x `iron_core:placeAt | Server refused to place cobblestone: the block is still wall_torch` (246/h, last 0m ago), alongside `stair_repaired {"job":"mine_iron","y":16,"fixed":3,"left":1,"still":["wall:open@-330,17,-490"]}`; the repair wants a wall cell that its own stair torch occupies and never breaks the torch first, so the same cell is retried for ever and `left` never reaches 0 | iron_core placeAt / stair repair: treat a torch (or any replaceable non-solid we placed) as diggable before placing the wall block, or skip the cell and count it done
- [ ] 05:52Z foreman: SOMETHING BRIDGES ON FOOT INSIDE THE BASE (movement is read-only, rule 4): base audit finds 12 placed blocks above y68 that belong to no blueprint, and they sit in ADJACENT PAIRS/TRIPLES like a walked bridge, not like a dump - hall -369,69,-531 / -370,69,-530 / -371,69,-530 cobbled_deepslate, road_8 -399,69,-459 + -400,69,-459 cobblestone, field_5 -384,69,-454 + -385,69,-454 cobbled_deepslate, dorm_pad -368,69,-533; all four sites are on the infill/build fronts whose bots carry cobbled_deepslate by the hundred (junk_tossed Tamaki 382, Riko 440) | terrain_guard: the placements pass the guard - log the job+task of every place outside a blueprint cell and refuse it
- [ ] 05:52Z foreman: TWO OF OUR JOBS UNDO EACH OTHER ON THE FIELD PATHS: base audit `STRUCTURE base_field_6 12 cells MISSING + 4 wrong` and every wrong cell is the same line x=-399 - cobblestone@-399,69,-415=wheat, @-399,69,-406=wheat, @-399,69,-397=wheat: the field_block blueprint wants a cobblestone path there, the farm job tilled and planted it, so the build job will re-place cobblestone over live wheat for ever (and the audit will report it for ever) | farm: the field_block's path cells are not soil - exclude the blueprint's path lines from till/plant, do not just level them
- [ ] 05:52Z foreman: A PRODUCER RAN ACTIVE ON SOIL THAT DOES NOT EXIST YET: cane_farm was active for the whole hour with "last 10 passes all 0" (FIELD ANOMALIES) and 0 bots, because its ground is `base_cane` which is `paused (after base_cane_pad)` and base_cane_pad sits ACTIVE at priority 46 with 0 bots - with 50 bots all claimed by pri 60+ jobs a pri-46 pad is never served, so the whole chain pad->block->cane stalls silently and the audit reports `base_cane 8 missing + 137 wrong` | dispatcher: a job whose `after` chain is not built should not be active (or should decline loudly), and a pad that GATES an active producer must inherit that producer's priority
- [ ] 05:52Z foreman: THE INFILL PROGRAMME IS TOO NARROW FOR 50 BOTS (x30 scale rule): base audit measures 11125 columns NEVER LEVELLED inside the yard in 18 areas (worst -287,-536 x5024 on infill_n314_n558 [job paused], -250,-388 x…) while the dispatcher runs ~6 infill tiles at a time with 5 bots each and activates exactly ONE successor per completion (`job_activated after: auto-paused: build complete` x6 in 5 s at 05:45) - the 300-cell tiles finish in seconds (`build_pass done:0 left:0` then `declined build: complete` x4) so the squads spend the gap declining | dispatcher/plan-base: activate infill tiles in parallel up to the free head-count instead of one-per-completion, and hand a squad the NEXT tile in the same pass
- [x] 05:55Z top model: (main 10) JUNK DROPPED AT THE DEPOT WAS PICKED UP BY THE NEXT BOT: every glut rule of bank() tossed where the bot stood. `A.dumpJunk` = the ONE disposal: carried to `settings.dump` {at:[-333,69,-480] NW rim of the ravine keep-out, aim down the quarry_sw terraces, spread along z} and thrown in; unreachable/no dump = the bot KEEPS it (`dump_unreached` once per 10 min); < 96 items with > 6 free slots ride along. Proof: `junk_tossed` without `at` 60/min -> 2/min within two minutes, with `at` near the dump from 05:47:41Z. Left: iron_core.tossJunk (miners, underground, not my file) - skills/iron_miner.js:153 tosses on freeSlots <= 2 wherever the miner stands.
- [x] 05:55Z top model: (main 11) NO SHOVELS: `getTool(bot, kind)` (depot -> stone -> wooden; build takes a shovel on the way out and an axe at the first log cell, tidy before its digs), quartermaster shelf `stone_shovel` 40 / `stone_axe` 20 (CRAFTED batch 8).
- [x] 05:55Z top model: THE ARMY'S CRAFT TABLE WAS IN THE RAVINE: `craft_table_set [-307,57,-436] "the registered table was gone"` (04:00Z, again 05:39Z) - craftSpot called the table gone whenever its WALK there was interrupted and adopted any table within 48 blocks of where it stood = a lost table on the ravine floor; every craft then failed (`obtain_failed iron_pickaxe crafted 0/1`, `stone_shovel ingredients did not arrive`). Now only a bot within 6 blocks of the registered cell that sees no table may say so, tables below the base level / inside a keep-out are never adopted; settings.craftTable = the hall's table -345,69,-523 (probe: stands).
- [x] 05:55Z top model: (main 12) hunt: `preyNear` never returns an animal inside or within 6 blocks of a herd job's pen box, nor one within 8 blocks of a player holding a lure item (the shear verb opts out with `penned:true`) - `params.kinds` of hunt_spawn may be widened to cows/chickens under this rule.
- [ ] 06:14Z helpdesk (Kotori, mine_lost): Code owner: mine graph lacks nodes near -325,49,-453 (ledge beside ravine x=-328, 1-wide corridor south along x=-325). Nav budget ran out; add these nodes or raise the budget. | ticket: off the mine graph at -325,49,-453: budget @-325,49,-453
- [ ] 06:20Z op: HUNG on build fill_void (area 1-7) Nagisa+Erika fill_ravine_n at -324,63,-423, Rin+Hina fill_ravine_m at -317,58,-439 (06:14-06:17Z), Yui no_route -371,69,-500 / -363,69,-503 at the base edge; ravine builders stand on a 1-cell ledge (walkable 1) and cannot reach the next fill cell
- [ ] 06:25Z foreman: FILL_VOID LIDS THE RAVINE INSTEAD OF FILLING IT and burns the army's biggest error counter doing it: /tmp/foreman/fill_ravine_n.png shows a solid RED (floating) sheet along the ravine x -330..-317 / z -434..-415 while `build_pass fill_ravine_s {done:0,left:10937,gaveUp:9110,waiting:9043,sealed:465}` (same shape n: 6011/5224, m: 5504/4860) and the LIVE top error is `iron_core:placeAt | Server refused to place cobblestone at (N,N,N): the block is still air` 282x = **923/h** (+ cobbled_deepslate 534/h) - distinct from line 144 (rail/water) and 142 (torch): here the target cell has NO support face, the bot stands on the rim and throws blocks into open air, so 83% of cells "gaveUp/waiting" for ever and the few that stick become a deck over a 20-deep void | build/fill_void: a void cell may only be placed against a SUPPORTED neighbour (fill bottom-up from the floor the bot stands on, or from a descended ledge), an unsupported cell is deferred once - never retried per pass; capped the three jobs 19->11 bots, pri 94->70 by board 06:22Z
- [ ] 06:25Z foreman: A HAND-SET PRIORITY OUTRANKS A MEASURED DEFICIT, so the scaler talks to itself: `targets` said iron 56% (99/224) -> "mine_iron 2-26" wants **16** bots, `status.sh` gave it **3**, because fill_ravine_n/m/s (pri 94, cosmetic levelling, done:0 - line above) and 8 base_infill zones (pri 90) sat above mine_iron (86) and ate 29 of 50 bots; the audit measured the result as 31% of bot time producing NOTHING. Same mechanism starves herd_sheep (pri 85, 2 of 6 bots) while 69 of 99 sheep stand OUTSIDE pen_1, and base_pen_1 (58) / tidy_spawn (60, 0 of 8 bots) never get a body although both are the audit's own remedies | the dispatcher should let a producer with a >50% deficit outrank `build` levelling work (or `targets`/`wait` must raise "DEFICIT PRODUCER OUTRANKED: iron 56%, mine_iron pri 86 < fill_ravine 94" as a top problem); today only a foreman reading two screens finds it
- [x] 06:25Z foreman: the mine RECALL steers bots at a cell that is not in the mine: `mine_blocked {"job":"mine_iron","task":"iron:surface(recall)","why":"surface_blocked","at":[-328,0,-671],"want":"up"}` x4 bots and `recalled {"out":{"surfaced":false,"why":"blocked","pos":[-328,0,-671],"to":[-328,0,-507]}}` (Hazuki, 06:15:05) - **y=0** and **z=-671** are nowhere near the mine (site -330,69,-500, stairs x -325..-330 y16..49, deepest level y-48); the bots then dig up into stone or give up, and mine_iron carries 8 of the hour's 10 deaths (Hinata "tried to swim in lava" 06:02) | iron_core recall: `pos` looks like a default/stale record (y 0, z off by 164) - the surface target must be the stair head the bot came down, and a recall that cannot surface must walk the stair, not dig `up` — top model 07:05Z: the cell IS in the mine (level-0 trunk, hub -328,0,-507 north, offset 163 = mouth k 53; `to` = the hub). CAUSE (rcon `execute if block` probe of the cells): an abandoned-mineshaft corridor crosses the trunk at z -668..-670 - the HOME lane's floor cells -328,-1,-670 and -328,-1,-668 were `oak_fence` posts (rails -329..-326,-1,-669, cobweb -326,1,-670). A fence reads `isSolid` but stands 1.5 high: under the 2-high trunk nobody steps onto it nor reaches the cell centre past it, so the out lane (cobble floor) passed and the home lane did not = 13 miners `blocked` at -328,0,-671, 12 `hung`, 6 `recalled surfaced:false`, and `mine_trunk_end` wrongly cut level 0 at k 53. No lava wall and no kStop refusal was involved (planUp/onLevel ignore kStop). CHANGE (iron_core.js only): walkLine takes a fence/gate/wall floor OUT and lays filler, lifts a rail/cobweb lying in a floor cell before flooring it, cuts cobwebs in its own and the next line cells (digCell `opts.soft`; a web no longer asks for a pick), and does the same for the second lane (`wide`) so whoever walks OUT mends the HOME lane; walkTrunk: `lava` in one lane (clearHot walled it) -> ONE retry on the other lane with `wide` off (the wall stays shut) instead of ending the way home. Level 0: `kStop`/`trunkFail` cleared through the locked `update` (eval). PROOF LIVE: 06:46Z code in (ironGen bumped on 16 miners); -328,-1,-670 / -668 and -327,-1,-669 now read `cobblestone`; miners drove the trunk on to z -736 (k 75), level 0 open 0 -> 19 claimed, ore blocks 230 -> 347; Juri (-321,0,-720) and Kokoro (-251,0,-712) came home from BEYOND the crossing `recalled surfaced:true`; 06:46-07:03Z = 24x `recalled surfaced:true`, 0 `surface_blocked`, 0 mine `hung`, 61 raw iron banked (3x `interrupted` = stale slice, by design; 1 transient `blocked` at -327,0,-718, the cell was air a minute later). SAME CAUSE as `surface_line` -328,-54,-614 / -328,-32,-593 (lines 118/125)? NO: those cells probe as plain deepslate floor + air; `surface_line` = a leg that ended with NO failure record = an interrupted walk, since 02:2xZ reported as `interrupted`. NOT COVERED: lava across BOTH lanes on the way home still ends `surface_lava` (needs a by-pass gallery).
- [x] 06:25Z foreman: `pillared_out` leaves its pillar standing, which is where the audit's stray blocks come from: `pillared_out {"from":[-360,-25,-512],"blocks":14,"free":false}` + `dug_out` (Ayame 06:15:13) and the BASE AUDIT lists 9 placed blocks above y68 that belong to no blueprint, 6 of them cobbled_deepslate on FINISHED ground - hall -369,69,-531 / -370,69,-530, dorm_pad -368,69,-533, field_5 -385,69,-454 / -384,69,-454 and **field_3 -386,76,-464 = 8 blocks above the field**, a bare pillar standing in a wheat field | rule 4 and line 163 (dug_out should fill its trench) have a mirror: the escape pillar must be dug back down behind the bot, otherwise every rescue adds a junk column that the tidy sponge (unstaffed, see line above) never reaches — top model engineer 06:5xZ: army.js `trimPillar`: the pit escape stops pillaring once level with the ground beside it and takes its own (ledgered) blocks down to that level - what stays fills the hole flush; roofed escape trims before the ledger is cleared (`pillar_trimmed`)
- [x] 06:25Z foreman: an audit line cannot be parsed and that audit is silently blind: `errors 8` -> `army_jobs:auditBoomLine | Unexpected non-whitespace character after JSON at position N (line N column N)` 3x (11/h, last 3m ago), live | army_jobs auditBoomLine: the reader must tolerate a truncated/concatenated line (skip it and count it) and say WHICH file and offset - as it stands the boom audit reports nothing and nobody notices — top model 06:22Z: watched, no change: the ravine fill is strictly bottom-up (fill_ravine_s 12645 → 10879 cells in 30 min, ~60/min with 3 squads); builders on a 1-cell ledge wait until the floor rises to them (that is the design: the lowest open cell goes first, gravity blocks reach the rest). If a bot starves on a ledge: `armyctl.js rescue` after its own check. — top model engineer 06:5xZ: the crater-ledger tail read drops the cut first line, splits glued records, skips a bad line (counted once per process) - never throws
- [x] 06:35Z op: Yui/Tsumugi no_route (-378,66,-511)->(-363,69,-512) and (-340,66,-496)->(-367,69,-512) 'area 3 island 3' + shut_in inside base_field_4 x4: bots on the base_field_4 island cannot reach the depot row (food chests -371..-359,69,-512), same family as line 194; also CHEST FULL food (wheat,cooked_beef not banked) while food chests row is at -371..-359,69,-512 - a second food chest row is needed (code/plan-base); Karin MINE BLOCKED budget at -319,49,-483 iron:reconnect (mine growth lines 134/139) — top model engineer 06:5xZ: they stood in 3-cell POCKETS two blocks under our own paving (field-4 path / road 1), `shut_in` forbade every edit: army.js `pitExit` (walkable < 12, inside a zone, <= 6 deep, plain lid <= 2 blocks with nothing on it) reports `pit`, opens the lid from below, pillars the column flush with its own filler (lid material last) = the hole is filled and the paving closed again; rooms/pens untouched
- [ ] 06:45Z helpdesk (Erika, no_route): Code owner: Erika at -303,59,-440 is stuck in a 120-block walkable pocket. Terrain rises in steps of 2+ blocks to the east and south, so no path to -371,69,-512. Add a staircase cut or a pocket-exit fallback, or mark that target unreachable for wood_spawn. | ticket: cannot path to -371,69,-512 (walkable area 120) @-303,59,-440
- [ ] 06:50Z foreman: WE ARE FILLING A ZONE OUR OWN PLAN SAYS TO KEEP EMPTY: `armyctl.js base` -> keep-out `ravine x -334..-289 / z -486..-408 "ravine down to y41: future quarry, fence it"`, yet fill_ravine_n/m/s put 11 bots INSIDE that box (`fill_climb_out` at -315,49,-480 / -307,49,-472 / -321,51,-431 / -313,49,-486, all inside) against 21590 remaining cells (s 10534 + n 5770 + m 5286, gaveUp 18097) - at the measured 36 cells/min that is ~200 bot-hours to destroy the quarry the plan wants, and the REPORT counts the same three jobs as the army's three biggest do-nothings (6.3 of 13.5 standing bot-h). Line 195's top-model answer at 06:22Z judged the fill MECHANICS (bottom-up, by design) but not whether the fill should happen at all | the board must refuse a `build` zone whose box overlaps a keep-out (`put` validates verbs/items/coords already), and plan-base/`fill_void` must never enumerate cells inside one; the ravine's real job is a FENCE + a quarry ramp. Paused all three by board 06:48Z - overrule me if the quarry is cancelled
- [ ] 06:50Z foreman: A FULL DEPOT REPORTS ITSELF EMPTY AND THREE JOBS STARVE ON IT: `stock` = cobbled_deepslate 5338, wheat 17174, bread 4637 in the depot, while the SAME minute (a) `build_blocked fill_ravine_s x10 {"why":"no filler carried or in stock (cobblestone/cobbled_deepslate/stone/deepslate ...)"}` + `obtain_failed {"item":"cobbled_deepslate","n":192,"why":"ingredients did not arrive"}` x7 (Sayaka, Mei, Botan), (b) `declined herd_sheep x2 {"why":"no lure item for sheep (wheat) beyond the depot reserve of 64"}` - with 17174 wheat - so the 62 stray sheep are never lured back, (c) `canteen_unreached` 14 DISTINCT bots in 17 min and Mio/Chino/Riko at hp1 food0 wearing full iron. All three say "we have none" when the truth is "I could not REACH the chest" (depot row -371..-359,69,-512, the unreachable island of line 199) | obtain/canteen/lure must distinguish OUT OF STOCK from CHEST UNREACHABLE: report `depot_unreachable` (not `declined: no filler`), fail over to the nearest other container, and a reserve check must compare against the depot total, not against what arrived
- [ ] 06:50Z foreman: THE HERD JOB IS BLIND OUTSIDE ITS OWN FENCE: BASE AUDIT (camera) measures pen herd_sheep x -402..-376 / z -540..-528 = **30 inside, 62 OUTSIDE within 96 blocks** (farthest 96) and unchanged since the last audit, while the job's own census in the same minute says `pen_harvest {"adults":30,"want":30,"keep":30,"outside":0,"gateShut":true}` x4 (Aoi 06:45:33, Mashiro 06:46:42, Nanami 06:42:50) - the job counts only animals already inside the box, sees "want 30 / have 30 / outside 0", declares itself done and shears the same 30 sheep every pass forever; two thirds of our wool herd grazes loose | herd: the outside census must use the audit's radius (96), and a herd with more animals outside than inside must LURE before it shears (this is also why line 183(a) "herders bring 5-8 sheep in tow, `herded n:0-2`" never converges)
- [ ] 06:50Z foreman: LAVA IS KILLING AGAIN AFTER THE 05:58Z clearHot FIX, AND NOT IN THE MINE SQUAD: `deaths 30` = 3x "tried to swim in lava" in 30 min (06:18:05 Sumire, 06:18:50 Hazuki, 06:23:37 Karin) = the dominant cause of the hour's 6 deaths, but only Hazuki is on mine_iron - **Sumire is on eff_stair_trip (the stairwell) and Karin on base_field_3 (a surface field)**, so three different jobs walked into lava within 5 minutes; /tmp/foreman/base_field_6.png (centre -352,-16,-382) shows an open lava lake at x -328..-322 / z -384..-355 pressed against a water body, right under the field_6/field_7 corner of the base | the 05:58Z `clearHot` look-ahead lives in iron_core walkLine/mineBranch only: the same 2-cell hot check must sit in `A.travel`/the movement layer so a builder, a stair-layer or a farmer crossing an unlit cavern gets the same protection - and that lake wants a `mine_hazard`-style seal before field_6/field_7 are built over it
- [ ] 06:50Z foreman: `level` REPORTS A PAD FINISHED OVER GROUND THAT IS NOT LEVEL: BASE AUDIT counts 382 off-level columns of which **60 sit on FINISHED pads/roads/footprints** - NEW this round `-371,-446 -1 x20 (road_6)` and `-359,-447 -3 x6 (field_1_pad)`, plus `-289,-396 -17 x57 (yard)` and `-408,-474 +2 x34 (yard)` on ground the yard pad already claimed - i.e. a job said "build complete" and walked away leaving a 20-column trench in a finished road and a 3-deep dip in field 1's soil | build/`level`: the completion check must re-measure the box's column heights against the target y (the audit does it from a camera in 40 s) and refuse "complete" while any column is off-level, the same family as line 164 (clear_area completes with a whole tree inside its box) - as long as a pad can lie about being flat, the tidy sponge is chasing craters that finished jobs keep making

- [ ] 06:55Z top model engineer: JOB CHURN is the army's biggest walking sink: 982 `job_start` in 60 min = 19.6 per bot-hour (a switch every 3 min: handover banking + walk + kit each time; sampler 06:15Z: 13-21 % of all bot time in switch overhead, a miner was recalled 2 min after a 2-min commute) | dispatcher.log `ASSIGN Sumire` x12 in 5 min; same tick `Sumire fill_ravine_n -> base_field_1` + `Rena base_field_1 -> fill_ravine_n`; 121 of 300 `declined` in 92 min = base_field_1/3 `the rest waits for a water cell in work` (4 slots asked for, work for 0-1: every bot walks there, declines 4 min, the next one is STOLEN from a lower job = a cascade down the priority list) | dispatcher.js staff(): fill a vacancy from UNATTACHED bots before taking a bot that is sticky on another active job; army_jobs build: a job whose only open cells are claimed/waiting should shrink its own head-count (board `bots`) instead of cycling the pool through 4-min declines. Interim: base_field_1/3 set to bots 1 by hand- [ ] 06:58Z op: LEVEL builds stall: build_pass x210 base_infill_n404_n556 (done 0, left 26), n407_n556 (left 90), n407_n455, n407_n558 all fail {"put:occupied":8,"last":"-404,64,-544"} and 5 bots HUNG at level (Aoi -403,69,-528, Kotori -405,66,-527, Ichika -404,65,-540, Yuzu, Yui); Aoi look shows bot-made cobble ^ rows at z -527..-525 = cells occupied by our own blocks | build/level: treat already-solid fill cells as done, don't retry put on occupied
- [ ] 07:15Z op: base_infill_n231_n527 (level, 20 cells left) build_pass done:0 x392 in an hour, fails put:occupied last -231,65,-502, 5 bots hung at -244,67,-510 (Hazuki/Hotaru); base_cane 'water unreachable / bucket stayed empty' gaveUp:59 done:0 with 14 bots | level pass must skip/dig an occupied cell and end the job when only unplaceable cells remain; cane needs a reachable water source. Board: infill 12 bots, cane 14 bots.
- [ ] 07:15Z helpdesk (Hina, hung): Hung on army:build level in job base_infill_n231_n527. Bot at -244,66,-509 is in open flat ground with 64 dirt and hp 20, so it isn't boxed in or short of blocks. The infill loop is stalling with no target, no timeout and no logged reason. Add a stall timeout, log the target cell, and drop the job i | ticket: no movement for 3 min on job base_infill_n231_n527, task "army:build level" @-244,66,-509
- [x] 07:20Z foreman: TIDY'S MEMORY CONTRADICTS THE CAMERA, so the fallback sponge parks the army on ground it calls clean: at 07:14:28Z `declined x39 {"job":"tidy_spawn","why":"tidy: every tile was tidied in the last 30 min"}` by **34 of 50 bots** in ONE minute, while the BASE AUDIT of the same minute measures **470 columns off the base level y68** in 35 hole + 13 bump clusters, **70 of them ON FINISHED PADS/ROADS/FOOTPRINTS** (new since the last audit: -230,-501 -4 x57 yard, -358,-369 -4 x51 yard, -234,-489 -3 x24 road_2) | this is not line 94/167 "the sponge ran out of work" - tidy's per-tile "tidied at T" memo is simply WRONG (the audit re-measures those same tiles as off-level minutes later, so a pass that fails to fix a tile still stamps it tidied). tidy must stamp a tile only when the fix VERIFIED (re-read the column), and the audit's off-level cluster list should be the sponge's work queue - as it stands the one measurement we trust and the one job that should act on it never talk - top model 08:00Z: base-audit.js writes `work` (off-level columns per sign + 8x8 tile), `strays`, `floats` into base_audit.json; `tidy` takes the nearest unclaimed unit first (claim = blocks.js file lock, kept until the next audit), re-measures each column, fills ground-up / cuts / digs strays, reports `tidy_fix {kind,at,n VERIFIED}`; tiles only when the audit has nothing, then a 10-min decline; an unreached tile loses its stamp. Live: 84 tidy_fix / 509 columns by 21 bots in 25 min, audit 438 -> 320 rough columns, 0 `declined` of tidy_spawn
- [x] 07:20Z foreman: `no_los` IS THE BIGGEST SILENT FAILURE OF THE LEVEL BUILDS, at GROUND level (not roofs, lines 67/106): REPORT "WARNING STORM: place_failed x136/30min (29 bots), most: no_los @-242,67,-494 x24 - nobody is resolving it", plus `build_pass base_infill_n247_n401 {"blueprint":"level","done":0,"left":574,"gaveUp":3,"fails":{"dig:no_los":6,"last":"-246,71,-386"}}` - 12 bots, zero output for 20 min. A y71 cell over a y68 pad and a y67 cell in an open field are both reachable: the bot just never repositions to a face it can see (stand ON the neighbour column for a dig from above, stand BESIDE for a place) | build level: before calling no_los, reposition onto the adjacent column top / dig the cell above; two no_los in a row on the same cell = `cell_impossible` once, not 136 retries an hour. Board: bots 12 -> 3 on base_infill_n247_n401, 12 -> 2 on base_infill_n231_n527 (line 209) - top model 08:00Z: probes (Yuzu on a 4-high trunk, `noPath` to all 3 nearest stands = tree crowns, 8 `no_los` in 6 s; stands judged from the cell CENTRE while the bot stops anywhere in the cell): blocks.js findStands ranks walkable ground before leaves, tests the rim-edge eye (`lean`), 6 stands, placeBlock/digBlock walk until a face is really seen and square themselves on the stand (centreOn), no stand walkable = `unreachable`; placing prefers the rim above the hole; army.js placeHard: fill cells are filled from INSIDE (fillInside: step in, jump-place, ledger:false) or by gravityDrop down a deep slot, 2 geometry rounds, NO ticket + one event per bot/cell/10 min for no_los/unreachable; build: `offThePole`. Live: place_failed no_los 136/30 min -> 0, tickets 0, level done/min 134 -> 600+ in the first minutes; LEFT: dig:no_los on tree crowns (now felled whole, `tree_cleared`) and fill_ravine `rest:no_los`
- [x] 07:20Z foreman: THE AUDIT CRIES ABOUT A PLANTATION WE DELIBERATELY DOWN-STAFFED, and a real alert drowns in it: BASE AUDIT "NEW GROWTH wood_spawn ... 253 plants, ripe 80 %, in 30 min: 3 grew / 45 stood still / 5 cut = PLANTED BUT NO GROWTH for 60 min (light? water? chunks?)" - but /tmp/foreman/wood_spawn.png shows open daylight grass beside a river (light and water are fine), `targets` has log 2890/448 = 645 %, so the scaler correctly gave wood_spawn its minimum 2 bots and 200 ripe trees simply stand waiting until we need them | the growth audit must not alert on a plot whose crop is already RIPE and whose job is at minBots by a satisfied deficit (say "ripe stock standing by" instead), and "stood still" must not count mature plants - every false line here costs an operator shift - top model 08:00Z: audit_growth judges cane (tau 18 min) and saplings (45 min) against the expected share x loaded share, stall only below a quarter of it for 90 min / 3 h; a lumber plot with ripe >= 50 % never alerts (`backlog:true`, text `= backlog: N grown trees stand waiting`). Live 07:24Z: `ok GROWTH wood_spawn ... ripe 79 % ... = backlog: 224 grown trees`; wheat unchanged
- [ ] 07:28Z op: wood_spawn box (-145,-281) unreachable again: Koharu (-316,59,-439, sky, not underground) `no_route {"to":[-145,null,-281],"area":120,"island":150}` then `job_slice lumber: cannot reach the plantation` 07:27Z; log stock 2895 vs target 2940 so low urgency, but the flower-forest box (line 26 patch) is a separate island from the base | wood_spawn should choose a tree box in the same walkable component as the depot, or lumber falls back to the reachable tree farm
- [ ] 07:46Z foreman: THE MINE RECALL TASK SURVIVES THE JOB SWITCH AND EATS 90 s OF EVERY SLICE ON THE SURFACE (line 197 fixed the mine, not this): Tamaki stands at -258,69,-422 `sky true` on job base_infill_n232_n400 with task `iron:surface(recall)` and reports `mine_blocked {"task":"iron:surface(recall)","why":"surface_blocked","at":[-327,-16,-466],"want":"up"}` 07:44:01 - a cell 200 blocks away and 85 BELOW him; the slice then ends `job_slice {"r":"build: cannot reach the site","ms":92172}`. Same minute Iroha at -247,66,-387 (INSIDE her zone x -247..-235) reports `mine_blocked at [-326,51,-492]` + `job_slice ms:89443 "cannot reach the site"`, Tsumugi at n231_n403 `at [-330,37,-500]`, and the three stale y's (-16 / 37 / 51) are just where each bot last was in the mine. Cost: all 5 active base_infill jobs at ZERO output for 20 min while walking 5648 m/15min, 10 bots `no_route`, 40 % of 45.7 bot-h produced nothing | iron_core/army_jobs: `job_start` (07:43:16) must CLEAR the recall task and its stale pos - a bot with sky above it and y>=65 is already surfaced; `recalled surfaced:true` (07:43:59) must end the recall instead of re-arming it 2 s later
- [ ] 07:46Z foreman: THE BUCKET GATE FIRES WITH 36 INGOTS ON THE SHELF, so the two water jobs decline the whole army (line 56 was real iron hunger, this is not): `declined x7 {"job":"base_cane","why":"build: waiting for water (no bucket carried, in stock or craftable (3 iron_ingot"}` 07:43:03 and `declined x56 by 37 bots {"job":"base_field_3","why":"build: the rest waits for a water cell in work"}` 07:44:12, while `stock iron_ingot` = depot 36 + carried 6 and the craft needs 3 - the check says "not craftable" about the one thing we have most of. Board stopgap: I activated the paused `craft_buckets` steps job (withdraws 9 ingots at the depot table -345,69,-523) | the craftable test must count DEPOT stock, not only the asking bot's pockets (same family as 83/203); a job that needs a tool the depot can craft should post the craft, not decline for hours
- [ ] 07:46Z foreman: `herd` IS BLIND TO ITS OWN STRAYS - the want-cap counts only animals INSIDE the fence, so the flock outside is never fetched: `declined x9 {"job":"herd_sheep","why":"sheep 30/30 in the pen, nobody ready to breed yet"}` 07:41:36 in the same minute as `audit_pen {"job":"herd_sheep","msg":"PEN herd_sheep (sheep, x -402..-376 / z -540..-528): 30 inside, 68 OUTSIDE within 96 blocks (farthest 94)"}` 07:44:03 - the job declines itself into 0 staffed bots (board wanted 5 of 5-8) while 70 sheep trample the levelled ground outside, and `pen_harvest` keeps shearing/culling the 30 inside back to the cap so it can never rise. Board stopgap: `want` 30 -> 60 and base_pen_1 (fence ring, holes since 05:47Z) re-activated | herd: count inside+outside against `want` and send a LURE pass whenever outside > 0, before any breed/cull decision; culling down to want while strays stand outside is backwards
- [ ] 07:50Z foreman: THE `pen` BLUEPRINT BUILDS A RING ON UNLEVELLED GROUND AND THEN CALLS ITSELF DONE, so a pen can never hold its herd: I re-activated base_pen_1 (paused since a foreman ordered "the fence ring has holes. Rebuild the ring." at 05:47Z) and it finished in 90 seconds without laying a block - `build_pass {"job":"base_pen_1","blueprint":"pen","done":0,"left":0,"gaveUp":0}` -> `build_done` -> `declined x2 {"why":"build: complete"}` 07:46:24 - while the same audit measures "30 inside, 70 OUTSIDE within 96 blocks (farthest 93)" and the 05:40Z note records the same flock escaping "over step blocks on the pen floor". The ring IS complete; nothing in the blueprint owns the FLOOR, so a 1-block rise beside a 1-high fence is a permanent staircase out and every herder's `gate_closed` is theatre | blueprints/pen: the pen must level its interior to the pad height (or refuse to report complete while any interior cell is above the origin y) - a ring is only a fence if the ground inside it is flat. Board stopgap: build job `pen_1_floor` posted 07:48Z (blueprint level, interior x -401..-377 / z -539..-529 only, ring left untouched, 8 bots)
- [ ] 07:52Z op: Honoka (mine_iron) stuck on surface at -245,65,-385 (grass, open terrain, hp20): mine_reconnect too_far to -329,65,-504 x8+/s, mine_lost ticket + mine_blocked surface_too_far; the miner loses the graph on the surface ~120 blocks from the trunk - reconnect should walk (read-only) toward the nearest graph node instead of giving up, and back off the retry spam (family of lines 139/193)
- [ ] 08:02Z op: HUNG Kaede base_infill_n252_n558 at -253,69,-550 (build level: dig:no_los x8 last -250,73,-549, help fell 'no tree within 4', left 2962 gaveUp 3) and Mei hunt_spawn HUNG at -298,59,-586 then marooned on hill -278,80,-590 (island 57 area 121, no_route to -231,null,-541, travel_fail to -403,-488 d161, self dug_out) | level/hunt: a level pass in forest with no LOS gives up 3 cells of 2962 and hangs; hunt_spawn walks bots onto an enclosed hilltop island; base_field_3 unstaffed only because bots hp<10 (minHp:10), left as is — top model 08:03Z: known leftover of the 07:5xZ placement fix (`dig:no_los` on tree crowns is still open: a whole-tree fell before cutting a wooded hill is the next step for `level`); the NE hill tile (n252_n558, +13 high, 2962 cells) is the largest earthwork of the base - not a blocker. Mei on the hill -278,80,-590: watch; rescue only after its own check.
- [ ] 08:05Z op: base_infill_n278_n401 (level, 16 bots) done:0 x159, left 663, fails put:occupied last -255,65,-374; Ichika/Honoka HUNG at -254,66,-376 = same family as lines 208/220 (occupied fill cell retried, no_los); board: n278 16->4 bots, n252_n558 12->3
- [ ] 08:17Z helpdesk (Kanade, hung): Code owner: 'army:build level' for base_infill_n278_n401 stalls at -272,68,-395 with 64 dirt. The west side is a 1-block-higher plateau (x<=-273) that movement can't climb, and text_display markers sit at -273,69,-395 and -272,69,-393. Add a step-up or skip-unreachable-cell timeout. | ticket: no movement for 3 min on job base_infill_n278_n401, task "army:build level" @-272,68,-395
- [ ] 08:20Z foreman: THE OWNER'S RAVINE FILL HAS STOPPED MOVING AND ONE JOB WENT BACKWARDS (line 195's placement fix and the 07:0xZ leg/stair work did not reach it): line 202 measured s 10534 + n 5770 + m 5286 = 21590 cells at 06:50Z; 85 min and 22 bots later it is s 10546 + n 5509 + m 5004 = 21059 = 531 cells net, and fill_ravine_s is 12 cells WORSE than it was (10534 -> 10546, i.e. the squad digs more void than it fills). Per-pass proof from results.jsonl this hour: fill_ravine_s 68 of 72 build_passes `done:0` (the 4 others 2/10/18/19), m+n 182 of 187 `done:0`, live line `{"done":0,"left":10546,"gaveUp":8717,"waiting":8707,"sealed":547}` and `declined {"job":"fill_ravine_m","why":"build: no filler"}` while the depot holds cobblestone 7390 + cobbled_deepslate 3023. At 0.4 cells per bot-minute the owner's "渓谷は埋めないのか" needs ~550 bot-hours, i.e. it never finishes by hand | fill_void needs a method that does not depend on 8 bots sharing the handful of currently-supported cells: dump gravel/sand columns from the rim (gravityDrop per column, the ravine is 20-45 deep), or fill the floor y41->y52 as a walkable ramp+deck course by course with each bot owning a COLUMN, not a cell. Board 08:16Z: 8 -> 4 bots on each of the three (the ledge-waiting is by design per the job desc, so the job stays active - the 10 freed bots went to mine_iron/field repairs/tidy, not to the ledge)
- [ ] 08:20Z foreman: THE TIDY SPONGE WALKS BOTS INTO THE RAVINE IT IS TOLD TO EXCLUDE, AND THAT KILLS THEM: `death {"job":"tidy_spawn","pos":[-325,52,-420],"task":"army:tidy float -312,-375 (audit)"}` 07:58:28 - the work unit is a floating leaf cluster at -312,-375 (audit, 11 blocks, pen herd_chickens), but the bot died 45 blocks away at y52 INSIDE keep-out `ravine` (x -334..-289 / z -486..-408) which is also tidy_spawn's own `params.exclude` box [-337,-489,-286,-405]; the route to a float is planned with no floor check, so the sponge crosses the void and falls 16 blocks. Same hour Aika died at -333,53,-422 on fill_void, i.e. the rim is a killing edge for anyone who is not roped to it | tidy/travel: a `float` work unit must be reached from ABOVE its own column (stand on the pad, dig down onto the crown) and a route for a tidy/level job must refuse to cross a keep-out box - the exclude list is honoured for WORK cells but not for WALKING
- [ ] 08:22Z foreman: THE BASE AUDIT'S OWN REMEDY CANNOT WORK - `field_block` REPORTS "COMPLETE" WITH 19 CELLS MISSING (same family as line 218's `pen`, measured 60 s after I applied the remedy): audit 07:57Z "STRUCTURE base_field_6 (field_block, job paused): 10 cells MISSING + 4 wrong of 747 (cobblestone@-399,69,-415=wheat …)" and base_field_7 "9 cells MISSING"; I set both `active` at 08:16Z, and at 08:17:24 - three seconds of work later - `build_pass {"job":"base_field_6","done":0,"left":0,"gaveUp":0}` -> `build_done` -> `declined x3 {"why":"build: complete"}`, identically for _7. The blueprint counts a cell as satisfied when the wheat that GREW over the path is there, so the 19 holes in the field paths stay open for ever and the audit re-raises the same alert every 30 min with "job paused" as its cause. base_field_1 was worse: activating it pulled 13 bots that all `declined {"why":"build: the rest waits for a water cell in work"}` within 4 s (the cascade of line 06:55Z) - I paused it again at 08:22Z | blueprints/field_block: a cell whose plan block is cobblestone and whose world block is wheat/air is MISSING, not done (the audit and the builder must use ONE comparison - the audit's is right); until they agree, `armyctl.js job <id> active` is not a repair and the runbook's structure remedy is a no-op
- [ ] 08:23Z helpdesk (Riko, no_route): Riko at -308,60,-438 is in a 120-tile pocket ringed by cliffs. No path to -241,?,-386. Add a reachability check or a stair-cut exit. | ticket: cannot path to -241,?,-386 (walkable area 120) @-308,60,-438
- [ ] op 08:35Z: 3 deaths by Witch (Kaede x2, Erika) at -229,74,-531 on base_infill_n252_n558 (level, done 0 left 2938, hung x6 since 07:52Z, see line 220); job paused by op | level/build sites need a witch/archer escort or bots should retreat from witches; Sumire no_route -359,69,-509 was a self-resolved cave escape (shut_in base_hall, dug/pillared out)
- [x] (top model 09:1xZ: NOT the level handler - the NE tiles are a valley 3-6 below the pad, ~1300 columns; `level` fills with DIRT only and the depot had ~600 -> `obtain_failed dirt`, empty passes. Board: `fill_ne_valley` (fill_void, stone from 9000 in stock, to y67) and both level jobs `after` it as the dirt cap) 08:40Z op: base_infill_n247_n401 (level, site -245,66,-385) still HUNG after the 08:00Z no_los fix: Ume/Kokoro/Riko/Rin hung 08:23-08:36 at -240..-250,65-67,-378..-394 (area 80), build_pass x223 done:0 left:366 gaveUp:0 (10 bots, no output), Mio died there 07:55 | build level: bots hang mid-slice with zero cells done, not no_los; needs a look at the level handler. Board: bots -> 3
- [ ] 08:45Z op: level builds still zero output with many bots: base_infill_n252_n558 15 bots done:0 left 2938 (hung Yui/Erika -252,69,-537, dig:no_los x7), base_infill_n247_n401 12 bots done:0 left 366 put:occupied x8 (hung Nagisa) - same as lines 208/220/228; board: bots -> 2 and 3 (something raised them again)
- [ ] 08:50Z op: WORKER ERROR Aoi TypeError: Cannot read properties of undefined (reading 'y') at lib/army_jobs.js:3276:185 inside an Array.sort comparator (a sorted entry has no pos) | guard the comparator against entries without position
- [ ] 08:52Z foreman: THE FARMER IS THE VANDAL BEHIND THE "cells MISSING" ALERTS - `farm` tills and plants the field_block's COBBLESTONE PATH: base audit 08:28Z "base_field_1 4 cells MISSING (cobblestone@-350,68,-436=wheat, -350,68,-435=wheat, -349,68,-428=wheat)" and "base_field_6 10 MISSING + 4 wrong (cobblestone@-399,69,-415=wheat, -399,69,-406=wheat, -399,69,-397=wheat)" - every single missing cell is a path cell holding WHEAT, and the only jobs on that ground are farm_1 (Botan) and farm_6 (Kurumi, `farm_pass {"harvested":50,"planted":50}` 08:45:25). So the audit alert that the runbook answers with "re-activate the build job" is produced every 30 min by our own farm walking off its nine 9x9 plots onto the ring; re-activating base_field_1/_6 only starts a two-job tug of war (and cannot win, see the 08:22Z line) | army_jobs.js farm: the plot cells come from the field_block blueprint - till/plant ONLY where the plan says farmland, never a cell whose plan block is cobblestone/water
- [ ] 08:52Z foreman: A GUARD AT PRIORITY 95 CANNOT GET ITS BOTS AND BOTS DIE FOR IT: guard_treefarm `bots:4` has been staffed with 2 (Juri, Noa) all hour while 50/50 are online, and a Witch then killed 3 bots at -229,74,-531 - 18 blocks inside its own post -242,72,-543 radius 44 (kinds already include witch). Its `requires` {minHp:12, anyItem:[diamond/iron/stone sword|axe]} matches most of the army: `who fit has:sword` shows Tamaki(herd_sheep, pri 85, diamond_sword hp20), Yukina+Kokoro(quartermaster, diamond_sword hp20), Botan(farm_1, diamond_sword hp20) - all on LOWER-priority jobs than 95. Same shape on guard_infill_east (bots 2, staffed 2, 0 output 20 min) | dispatcher: a job's `bots` must be filled from bots held by lower-priority jobs (preemption) before anyone is left on a pri-85 job; guard slots staying empty is why escorts do not prevent deaths
- [x] 08:52Z foreman: A BUILD JOB WITH AN IMPOSSIBLE 2-CELL REMAINDER PULLS THE WHOLE FALLBACK POOL THROUGH A 60-BLOCK WALK: base_field_3 (site -390,69,-475) `build_pass x20 {"done":0,"left":2,"gaveUp":2}` over 17 bots at 08:44:42, immediately followed by `build_idle {"left":2,"cells":""}` and `declined {"why":"build: nothing I can do on the last 2 cells"}` (Koharu) - the job neither finishes nor self-pauses, so every sponge bot is dispatched there, walks, declines, and the cycle repeats (REPORT: "MOTION WITHOUT OUTPUT: base_field_3 - last 10 passes all 0"). Paused by me at 08:50Z | army_jobs.js build: N consecutive passes with done:0 and gaveUp>0 (or an empty `cells` list on build_idle) must pause the job itself and report once, not decline per bot for ever — opus 12:0xZ: `build` sizes the crew by the work that is OPEN NOW (left minus the cells that only wait for support/mate/material): under 8 open cells per builder everyone above rank ceil(open/8) in the crew (assign/) hands itself back 10 min, at least one stays and finishes. Live 11:36Z on base_yard_pad (14 open, crew 4 -> keep 2) and fill_ravine_n (2 open, crew 5 -> keep 1); 31 `build_tail` in the first 26 min.

- [ ] top model 09:1xZ: a `level` job never says how much FILL it needs: plan-base/put should measure the volume below the pad (skyshot/ground) and lay a `fill_void` sub-base first when it exceeds the dirt in stock; and ONE withdrawal at the build chests took 29 s (4 dirt, Tamaki) - MEASURED (opus 11:0xZ, timed probe `withdraw cobbled_deepslate 8` twice): 24 s of which 23.9 s WALKING, openContainer 0.05-2.3 s, the window transaction 0-1 ms - no queueing at the window; withdraw now takes the nearest container that covers the whole need instead of the fullest. The remaining cost is trips: batch sizes / a field depot, not army.js.
- [x] top model 09:0xZ: worker TypeError army_jobs.js tidy sort (`undefined.y` on a mixed dig/put pair) - comparator rewritten.
- [x] top model 09:0xZ: bots STARVED WITH BREAD IN THE POCKET (8 of 50 at food <= 10, a miner at 6 carrying 21): the worker eats only between slices, slices last up to 15 min -> `army.js mealReflex` (4 s timer, hands free), in-slice heartbeat now uses the newest lib (army_worker.js, rolling restart), `wait` digest condition `starving_fed`. Measured: hungry WITH food 8 -> 0. Empty pockets inside a long slice: the reflex ends the slice via the core alert queue -> canteen walk (13 visits in 4 min, avg food 15 -> 16).
- [ ] 09:12Z helpdesk (Mashiro, build_stuck): fill_ne_valley fails on all 43 cells (first -235,65,-393) from bot at -266,69,-395. Check the goto range and standing spot for placing cobbled_deepslate at y65, about 4 blocks below the bot's level. Also check whether the placement reference face exists. | ticket: 43 cells of fill_ne_valley left but I failed twice on each (first: -235,65,-393) @-266,69,-395
- [ ] 09:18Z helpdesk (Suzu, hung): army:build level hung at -254,68,-388 (job base_infill_n247_n401): bot is in open terrain with 64 dirt and full hp/food, so it isn't boxed in or starving. Add a stall watchdog and a no-reachable-target fallback that abandons the job. | ticket: no movement for 3 min on job base_infill_n247_n401, task "army:build level" @-254,68,-388
- [ ] 09:25Z foreman: fill_void SENDS BOTS INTO THE HOLE AND THEY BURN: Mashiro stood at -299,49,-476 (19 below base grade y68) and `look ... cave` shows no floor in any direction plus a creeper at -309,39,-471; deaths 30 min = 2x "tried to swim in lava" (Mashiro 08:54 fill_ravine_s, Ayame 08:55) + Kanade "burned to death" 09:00 (fill_ravine_n). Output for that risk: fill_ravine_s `build_pass x39 {"done":0,"left":10546,"gaveUp":8714,"waiting":8707,"sealed":547}` - 4 bots, ONE HOUR, zero cells placed (out 284: banked only). Paused by me 09:22Z; m/n still run. | army_jobs.js build/fill_void: place from the RIM standing on solid grade (never walk the bot below grade into an unlit void), refuse a column whose bottom is lava, and fill bottom-up per column so `waiting` shrinks instead of being re-scanned every pass
- [x] (top model 09:5xZ: LOOKED - the hole had NO walkable exit any more (the fill buried the natural north ramp, m stood 4 above n = the island). fill_ravine_n got a `ramp` (z -415, x -330..-323) that joins the floor to the slope: route foot -> rim verified (16 steps); `base keepout exit ravine -311,69,-415` (new CLI op); fill_ravine_m paused until n is level with it) 09:25Z foreman: BOTS ARE MAROONED IN THE RAVINE AND KEEP GETTING FAR TARGETS: 12 bots no_route in one status pass, 6 of them identical `no_route {"from":[-315,60,-437],"to":[-257,null,-386],"area":120,"island":150}` (09:16:08) and Hina `help_asked {"kind":"no_route","why":"cannot path to -261,69,-421 (walkable area 120)"}` -> the bot is standing on a 150-block island at the ravine floor y60 and movement is read-only, so it can never leave; the dispatcher keeps handing it hunt/canteen targets 60 blocks away. | lib/army.js travel + dispatcher: a bot whose walkable island is < ~400 and does not contain the target must report stranded ONCE (rescue/ladder job), and no job may be assigned a target outside its island
- [x] (top model 09:3xZ: mealReflex ends the slice of a starving bot with empty pockets -> canteen walk; Yukina hp20/f19 at 09:3x, 13 canteen visits in 4 min) 09:25Z foreman: A BOT AT 1 HP STARVES NEXT TO A FULL DEPOT: Yukina `canteen_unreached x2 {"food":5,"hp":1}` 09:15:41 (job fill_ne_valley / fill_void_n314_n516, NE valley), REPORT "FOOD IN STOCK BUT BOTS STARVE: 2910 edible in chests, yet 3 bots at food<=6: Himari(2) Juri(6) Yukina(5)" - the 09:0xZ mealReflex fix covers food in the pocket, this is the EMPTY pocket + unreachable chest case. | army_jobs.js canteen: on `canteen_unreached` fall back to the nearest reachable container/farm/animal in the island, else hand the bot back to muster instead of looping at 1 hp
- [x] (top model 09:5xZ: levels 16/0/-16 are at the cap (160 branches x 256). `mine level 32` = fresh iron-rich landing (24 is refused: landings are multiples of 16); iron_miner pickLevel sent everybody back to level 0 on the tie |32-16| = |0-16| -> the board's level wins when it lies in the iron band. 09:4xZ: level 32 has 13 branches, 6 claimed, 18 ore blocks. OPEN PART -> next line) 09:25Z foreman: THE IRON SQUAD MINES AN EXHAUSTED LEVEL 220 BLOCKS FROM THE HUB: `mine` says working level y16 with branches 160 (open 0, claimed 0) while the 11 bots of mine_iron are on level y0 at the far end of 256-long branches (Fuuka -325,0,-709, Iroha -310,0,-679, Sumire -310,0,-694, all 0 m/5min, status "4 stuck"); Fuuka's last slice: `job_slice {"r":"delegate iron_miner: {\"ended\":true,\"stats\":{\"rawDelivered\":0,\"ironPicks\":1}}","ms":900381}` = 15 min, 0 raw delivered. Result: iron 61/224 with 22% of the army on it. | lib mine: when the working level has 0 open branches, re-level automatically (y0/-16 both have 400+ ore blocks); cap branch length (256 = a 4-minute walk each way) and bank per branch, not per 15-min slice
- [ ] 09:25Z foreman: THE CAMERA AND THE BUILD JOB DISAGREE ABOUT THE SAME FIELD: base audit 09:0xZ "base_field_3 12 cells MISSING + 1 wrong of 747 (cobblestone@-399,69,-485=air, -399,69,-484=air)" while the job itself reported `build_pass x20 {"done":0,"left":2,"gaveUp":2}` + `declined {"why":"build: nothing I can do on the last 2 cells"}` (08:44Z, see the 08:52Z line) - so the runbook remedy "re-activate the build job" turns the whole fallback pool into a 60-block walk-and-decline loop and still cannot close the 12 holes. I activated it at 09:23Z and paused it again at 09:24Z rather than repeat that. | army_jobs.js build: take the cell list from the blueprint the auditor compares against (air cells at -399,69,-484/-485 are not in the job's `left`), and an unplaceable cell must be reported with a reason, not silently dropped from the count — STILL OPEN, reproduced on a SECOND field 09:56Z foreman: I restored base_field_6 from the archive against the audit's "10 cells MISSING + 4 wrong of 747 (cobblestone@-399,69,-415=wheat)" and 7 s later it reported `build_done` + `declined x2 {"why":"build: complete"}` + `build_pass {"done":0,"left":0,"gaveUp":0}` (09:51:03) and auto-paused itself. So this is not specific to field_3's two air cells: every `field_block` job's `left` list is blind to exactly the cells the camera measures, and the runbook's standard remedy ("re-activate the structure's own build job") cannot repair ANY field.
- [ ] (opus 11:2xZ: the LAVA half is DONE - in a solid fill lava cells are quenched FIRST, only by a gravity block dropped down the shaft or from a stand 2+ away that touches no lava (`avoidStand`), and no cell touching lava is ever worked while it burns; event `lava_blocked`. Deaths in the ravine: 6 lava + 3 burned in the 40 min before -> 0 in the 8 min after, fill goes on. MINE GROWTH still open) top model 09:5xZ: MINE GROWTH: when every iron-rich level is at K_MAX x MAX_BRANCH the fallback sends the squad to the far ends / to -32 (poor in iron) instead of opening the next landing (32, 48) by itself; `mine` should say EXHAUSTED per level and the digest should report it. Also fill_void vs LAVA (fill_ravine_s, 3 deaths): lava cells must be quenched from above before anybody stands below grade there - s stays paused. | opus 10:45Z: mine growth done: iron_miner growLevel opens the nearest unused landing itself when every dug level of y-16..48 is EXHAUSTED (board args.level + rev, one per hour, `mine_level_opened`); `armyctl.js mine` prints EXHAUSTED + mean blocks out per level.
- [ ] 09:35Z op: guard_treefarm bots Juri/Yotsuba/Tamaki all 'tried to swim in lava' 09:31-09:32 within 90 s (post -242,72,-543); job paused by me; same family as line 240 (bots walking onto lava) | guard travel/route must avoid lava; check where lava sits near the post
- [ ] 09:40Z helpdesk (Chika, no_route): Chika stuck at -521,49,-559: 120-block walkable pocket at lake bottom (water surface y62, e.g. -520,62,-566), ringed by higher ground. Add a swim-up/exit-water route so it can path to muster -367,69,-486. | ticket: cannot path to -367,69,-486 (walkable area 120) @-521,49,-559
- [ ] 09:55Z foreman: THE LEVEL BLUEPRINT BEGS FOR THE ONE FILLER WE HAVE RUN OUT OF: REPORT "WARNING STORM: place_failed x137/30min (26 bots)", live lines `place_failed {"at":[-242,68,-376],"item":"dirt","why":"noitem: none of dirt carried, in the depot index, or craftable"}` (Hina, base_infill_n247_n401) and the same for cobblestone at -231,65..67,-382 (Sakura), while `stock` this minute = dirt depot 74, cobbled_deepslate depot 2243, sandstone depot 2161, cobbled_deepslate_stairs 450, cobblestone 110 + 1814 carried. This is NOT line 83 (index blindness): the depot really is out of dirt and full of other solid fill. `blueprints level` shows fill='dirt' as a hard default and no job on the board sets it, so every infill/pad job stalls on a block we have 74 of. | blocks.js / army_jobs.js build level+fill_void: choose the ground filler from what the depot HAS (cheapest solid: cobbled_deepslate/cobblestone/sandstone/gravel before dirt) and fall back on noitem instead of failing the cell; `armyctl template build` should expose `fill`. Board workaround 09:50Z: patched base_infill_n407_n556 args {fill:"cobbled_deepslate"}.
- [ ] 09:55Z foreman: "BUILD COMPLETE" IS DECLARED ON GROUND THAT IS STILL 6 BLOCKS LOW: base_infill_n407_n556 (zone x -407..-405 / z -556..-526, w3 d31) carries `note:"auto-paused: build complete"` while the 09:24Z BASE AUDIT measures 47 columns of that same 3-wide strip below y68 — -406,-542 -6 x23, -405,-548 -4 x17, -408,-552 -5 x7, all three NEW since the previous audit. `blueprints level` defaults depth=3, i.e. shallower than the dent it is pointed at, and nothing re-reads the columns before the job pauses itself. Same shape on base_infill_n278_n401 ("build complete - BUT 12 columns are a deck over air") which the audit still reports as -278,-372 +5 x27. | army_jobs.js build level: size `depth` from the measured ground of the zone (armyctl ground / the base_audit work list) and re-measure the pad before writing "build complete" — a pad the camera still calls off-level must stay open.
- [ ] 09:55Z foreman: THE SPONGE SAYS "NO WORK IN REACH" WHILE THE CAMERA HOLDS A 302-COLUMN QUEUE (the 08:00Z audit-queue fix of line 211 is not reaching the bots): 09:45:54 `declined x21 {"job":"tidy_spawn","why":"tidy: the audit lists no open work in reach and every tile was tidied in the las"}` and 09:45:46 `tidy_pass x178 {"found":0,"pillars":0,"holes":0,"floats":0,"strays":0}`, against the 09:24Z audit's 302 off-level columns in 51 hole + 18 bump clusters (101 of them ON finished pads/roads) plus 10 floating leaf/log blocks at -230,-390 y77-78 x9 and -334,-369 y80 x1 — both inside tidy_spawn's own zone x -408..-230. Cost: tidy_spawn held 18 bots / 3.6 bot-h for 5 tidy_pass in the hour and is the biggest single slice of the 56 % no-output time. | army_jobs.js tidy: report WHY a unit is out of reach (claimed / excluded / no route) instead of one blanket decline, and if every unit is unreachable hand the bots back instead of re-walking the same tiles.
- [ ] 09:55Z foreman: A FILL JOB DECKED ANOTHER BLUEPRINT'S IRRIGATION: BASE AUDIT base_field_2 "2 cells MISSING + 13 wrong of 747, water@-297,68,-516=cobbled_deepslate, water@-288,68,-516=cobbled_deepslate, water@-279,68,-516=cobbled_deepslate" — the three water cells of the field block now hold exactly the filler block the neighbouring level/fill_void work uses, i.e. one of our own jobs wrote over a registered structure's cells (humans cannot touch anything). The crops on that row lose their hydration. | blocks.js / build: a level/fill_void cell lying inside another job's registered blueprint footprint must be refused (or handed to that job), never overwritten. Restored base_field_2 to the board 09:56Z as the repair.
- [ ] 09:55Z foreman: THE AUDIT'S OWN REMEDY IS NOT EXECUTABLE FROM THE BOARD: three of the four structure alerts (base_field_1 4 wrong, base_field_2 13 wrong, base_field_6 10 missing + 4 wrong) name jobs that `prune` had ARCHIVED, so `armyctl.js job base_field_1 active` fails and the operator must hand-extract JSON out of bots/army/jobs-archive.jsonl — which no cheap-model operator will do, so a broken field stays broken until a foreman round. | armyctl.js: `job <id> active` should restore from jobs-archive.jsonl when the id is not on the board, and `prune` must not archive a `build` job whose structure base-audit.js still compares against a blueprint.
- [x] (top model 10:4xZ: SOLVED - walkableArea counted every water cell, so the flood fill left her pond through a SUBMERGED passage (2000 cells) while the surface-swimming pathfinder had 45 nodes; now only surface water cells count -> area 40 -> digOut ran; digOut climbed ONE +4 flight in a 16-deep open pit -> now flight after flight (max 8). Chika walked out and hunts again 10:5xZ) HUNTER TRAPPED IN A LOW RIVER: Chika at -520,49,-559 (river surface y49, forest floor y62-65, banks 2+ high on the shallow arm x -524..-515 / z -566..-558). A* from her exhausts after 45 nodes (`noPath`) although `movements.getNeighbors` continues north into 2-4 deep water and `walkableArea` reaches 2000 -> find why the search stops where the water gets deeper than 1 (manager applyMovementSafety / liquid rules?), then give `travel` a swim-along-the-river fallback. Tried and failed: dropping blocks to her from the rim (new verb `drop` works, but the forest canopy catches the items; items despawn in 5 min). She holds in job `chika_hold` (quiet). Fixed on the way: walkableArea no longer lets a bot STAND on the air above water; `blueprints <unknown>` no longer throws.

- [x] (top model 10:5xZ: the LOWEST open cells were dandelions/poppies on the old ravine floor in 12-deep 1x1 shafts: `occupied`, rested 30 s, and every pass spent its 8 failures on them before reaching the two open layers. fillCell pulls a flower first, out of reach -> column rests 10 min; `occupied` rests 5 min. Measured: 0 -> 70 cells / 3 min with 2 bots on site; board back to 10 bots) 10:06Z op: fill_ravine_n (fill_void, 34 bots) build_pass x393 done:0 left:1386 gaveUp:653 waiting:649 sealed:837 put:occupied last -324,6x; 8 bots HUNG at -325,69,-423 (area 80) 09:54-10:04 after the 09:5x ramp fix, still zero cells placed | fill_void still stalls at ramp top / occupied cells (same family as lines 240/241); board: fill_ravine_n bots 34->4. Chika (no_route, lake pocket) = line 253, not re-logged.
- [ ] 10:20Z foreman: THE TIDY SPONGE REACHES THE AUDIT'S CRATERS AND REFUSES EVERY CELL (sharper evidence than line 254, which said "no work in reach" — it DOES arrive, then fills 0): 10:15:00 Nagisa `tidy_fix {"kind":"hole","at":[-231,-374],"n":0,"of":6,"blocks":0,"skip":{"occupied":6}}` · 10:15:22 Kotori `at [-236,-388] n:0 of:23 skip:{"occupied":21,"tree":2}` · 10:15:52 Ume `at [-235,-380] n:0 of:8 skip:{"occupied":7,"tree":1}` · 10:14:43 Nanami `at [-235,-542] n:0 of:21 skip:{"no pickaxe":11,"kept:coal_ore":3,"kept:copper_ore":5,"deep":1}` — then 10:17:09 `declined x85`. Those are exactly the 10:19Z BASE AUDIT hole clusters (-232,-377 -4 x20, -233,-390 -4 x19, -238,-386 -4 x13), so the queue is fine and the CELL FILTER is wrong: `occupied` fires on columns the camera measures 4-5 blocks BELOW y68, and a bot is sent to cut a bump with no pickaxe. | army_jobs.js tidy fillCell/cutCell: a cell below base level must be filled from the natural ground up whatever stands in it (flower/sapling/grass = break first, tree = hand to a lumber unit), and a `bump` unit must require a pickaxe in `requires` instead of walking there and skipping 11 cells.
- [x] (opus 11:2xZ: the pad now PUTS its own remedy on the board - one `fill_void` over the bounding box of the void columns, `unlid` MEASURED from the deck over them, id from the pad's id, pad `after` it without a note, 2 rounds max. base_infill_n278_n401: 12 void columns -> fill round 1 filled 8, round 2 the rest, pad re-checked and ended "auto-paused: build complete" with no void clause; n252_n558 armed itself the same way) 10:20Z foreman: A BUILD JOB REPORTS "COMPLETE" WHILE ITS OWN NOTE SAYS IT DECKED A HOLE, THEN PAUSES FOREVER: base_infill_n278_n401 note = `auto-paused: build complete - BUT 12 columns are a deck over air (void_under_pad): -254,65,-373 | -253,65,-375 | -253,65,-374 | -253,65,-373 | -251,65,-375 | -251,65,-374` — the job detected the exact doctrine violation ("never deck a hole", CLAUDE.md §1b) and still ended as a success, so nobody repairs it and the 10:19Z audit reports the same zone again (floating remains -278,-378 y72-73 x12). CONFIRMED LIVE 60 s after re-activating at 10:20Z: 10:21:06 `build_pass x8 {"job":"base_infill_n278_n401","done":0,"left":0,"gaveUp":0}` + `void_under_pad x6 {"n":12}` + `build_done` + `declined x6 {"why":"build: complete"}`, and base_infill_n247_n401 the same with `void_under_pad {"n":35,"cells":"-246,66,-373 | -245,66,-374 | ..."}` — so the BASE AUDIT's own printed remedy (`armyctl.js job <id> active`) is a NO-OP for every infill zone: the job re-declares complete in one pass and the craters stay. Board headcount returned to 2 (bots were walking 2698 m/15min for zero cells). | army_jobs.js build: a pass that ends with void_under_pad columns must report `build_incomplete` and keep the job ACTIVE (fill those columns from the natural ground up), never auto-pause as complete.
- [x] (opus 11:2xZ: probed - the 6 cells are the POSTS that carry the water caps, dug by the farmer's FARM_JUNK rule (cobblestone) and planted over. `farmSoil` in army_jobs.js: a farmer works only columns its zone's blueprint marks `soil:true` (113 of 729 skipped per pass, `notMine` in farm_pass) and never digs a block a blueprint of ours placed; the build job may take a crop/farmland off a cell IT paves. base_field_6 9 MISSING+4 wrong -> 0 MISSING, base_field_7 9 -> 0, all 18 posts stand) 10:20Z foreman: TWO OF OUR OWN JOBS FIGHT OVER THE FIELD-BLOCK PATH CELLS, SO THE STRUCTURE ALERT CAN NEVER CLEAR: BASE AUDIT base_field_6 "9 MISSING + 4 wrong of 747, cobblestone@-399,69,-415=wheat, cobblestone@-399,69,-406=wheat, cobblestone@-399,69,-397=wheat" and base_field_7 "cobblestone@-364,69,-397=wheat, -364,69,-388=wheat, -355,69,-397=wheat" — always x on a plot boundary, z every 9: the `farm` job tills and plants the field_block blueprint's cobblestone WALKWAY between the nine 9x9 plots. Re-activating the build job (done 10:20Z, the audit's remedy) repaves them and the farmers re-plant them next pass = an endless repave/replant loop that burns bot-hours forever. | army_jobs.js farm: the tillable set must exclude cells that the zone's own registered blueprint declares non-soil (cobblestone paths, water cells) — same family as the base_field_2 water-decking line above, but the offender here is `farm`, not `fill_void`.
- [x] (top model 11:0xZ: reproduced in 12 ms - 20 sticks stood in two tool chests nobody had opened for 6 h: stockMap counted them, C.solve said "have them, craft nothing", withdraw's 30-min freshness filter hid them. withdraw now visits STALE entries when no fresh chest holds the item (the visit re-indexes). Verified: the same bot crafted 12 sticks from logs on the next call) 10:20Z foreman: 2821 LOGS ROT IN THE DEPOT WHILE 34 BOTS CANNOT MAKE A STICK: `stock` = oak_log 2821 / birch_log 90 / oak_planks 102 / birch_planks 101 / stick 22, against `obtain_failed x74/30min (34 bots), most: ingredients did not arrive: {"#planks":10}` and 10:18:25 Kurumi `obtain_failed {"item":"stick","n":8,"why":"ingredients did not arrive: {\"#planks\":4}"}` — the miner then works without a replacement pickaxe (cf. the tidy "no pickaxe":11 above). Nothing on the board converts banked logs to planks: the wood is BANKED, the bottleneck is one crafting step. | lib/army.js obtain/restock: when an ingredient tag (`#planks`) is short but its source (`*_log`) is in the depot, withdraw the log and craft the intermediate instead of reporting `ingredients did not arrive`.
- [x] (opus 11:25Z: a cell that beat 2 DIFFERENT bots with unreachable/no_los rests 10 min army-wide - ledger bots/army/place_rest.json like chest_missing, atomic write + 5 s cache; placeHard returns `resting` at once and the event is sent ONCE per rest. Measured: place_failed 30.7 -> 6.3 per 10 min, geo 13.0 -> 3.6, worst cell 10 -> 2 tries; storm example 10:28Z one cell x6 bots in 39 s, now 1 line `resting x2`) (top model 11:0xZ, PARTLY: the cells are a level job's support columns at the SW corner and the real blocker is `noitem: dirt` - depot 36. Dirt had no target and no producer: `targets dirt 768`, hill cut base_infill_n252_n558 re-activated with its escort as the dirt source. OPEN: place_failed must rest a cell army-wide, 25 bots retried it) 10:20Z foreman: PLACE_FAILED STORM AT ONE COORDINATE NOBODY RESOLVES: `place_failed x119/30min (25 bots), most: unreachable @-406,65,-546 x10` — y65 is 3 blocks BELOW the base level y68, inside the pen_1 / field corner (x -402..-376 / z -540..-528 neighbourhood), and 25 different bots retry the same unreachable cell for half an hour; `errors` shows the same thing from the other side: 19x `iron_core:placeAt | Server refused to place cobblestone: the block is still air` (49/h). A cell that has refused N bots must be retired, not re-offered to the whole army. | lib/army.js placeAt / the work-unit queue: after 3 `unreachable` refusals mark the cell dead for the pass (and report it once with coordinates) instead of handing it to the next bot.
- [x] (opus 11:25Z: obtain now works in PASSES - each solves the chain on the pockets for the FULL n, fetches what it still lacks, crafts; what the depot promised and did not deliver counts as gone, so the next-best chain is taken. Probe before/after on an idle bot: `obtain stick 64` 63/64 -> 64/64. Failures now name the raw material (`got 0/1 missing {"#tool_stone":3,"stick":2}`) and an INTERRUPTED fetch (job switch, death) says nothing at all) top model 11:0xZ: obtain crafts `need - depot count` although the depot share may not arrive (Erika: 22/24 sticks): after the withdrawals, re-solve on the POCKETS for the full n.
- [ ] 10:28Z helpdesk (Rin, build_stuck): base_dorm_pad build fails on cell -346,68,-544 and one other, with 64 cobbled_deepslate in hand. Bot stands at -346,69,-541 on open ground. The cell sits under the '4' wall row at z=-544 (x -353..-343), which is higher than the bot. B pits at z=-543/-542 lie between it and the cell. Code owner: chec | ticket: 2 cells of base_dorm_pad left but I failed twice on each (first: -346,68,-544) @-346,69,-541
- [ ] 10:41Z helpdesk (Ichika, no_route): guard_infill_east: no path from -342,69,-478 (walkable area 120) to -361,69,-503. Check that the target is a standable tile and that the '^' strip at x -340..-338 isn't walling the bot in. Pick a reachable guard spot. | ticket: cannot path to -361,69,-503 (walkable area 120) @-342,69,-478
- [ ] 10:52Z foreman: BOTS STARVE WITH 3255 BREAD IN THE DEPOT BECAUSE THE EATER CANCELS ITSELF: `errors` = 23x `feed:eat | Consuming cancelled due to calling bot.consume() again` (43/h, last 1m ago) plus 9x `feed:eat | Promise timed out` in the dead-pid history, against FIELD ANOMALY "FOOD IN STOCK BUT BOTS STARVE: 3255 edible in chests, yet Botan(food 2) Honoka(1) Noa(6)" and 10:47:28 Honoka `canteen_unreached x3 {"food":5,"hp":20}` while 47 bots ran `canteen {"item":"bread","n":8}` in the same minute. A second consume() call on a bot that is already eating aborts the first, so a hungry bot can loop for minutes without swallowing anything; food<=6 then locks it out of every job with `requires.minFood` (herd_cows/herd_sheep asked for 6 bots and got 0 this hour). | lib/army.js feed/eat: one in-flight consume per bot - if a consume is running, await it instead of starting another, and report `eat_failed` once with the item.
- [ ] 10:52Z foreman: THE RAVINE FILL RE-WALKS A 4789-CELL BOX EVERY PASS AND PLACES 23 BLOCKS: 10:48:16 `build_pass fill_ravine_m {"done":23,"left":4789,"gaveUp":3974,"waiting":3956,"sealed":611}` (fill_ravine_n: done 0, left 1353, waiting 614, sealed 860) - 83 % of the box is `wait` because in fill_void a cell may only be placed once the cell BELOW it is solid, so 7 bots scan the whole ravine to find the few placeable cells; REPORT calls it "ZERO OUTPUT for 20 min while walking 2843 m/15min". At 23 cells/pass the owner's ravine (09-20 "渓谷は埋めないのか？") never closes. | army_jobs.js fill_void: hand each bot ONE column (or a small tile) and let it fill that column bottom-up to y68 before it moves on, instead of re-scanning the whole box per pass; count `wait` cells of unfinished columns as not-yet-work, not as work offered to every bot.
- [ ] 10:52Z foreman: PAD AND VOID-FIX PING-PONG AT THE SAME 4 CELLS FOR 20+ MIN: 10:47:31 Kokoro `pit_filled {"at":[-253,67,-371],"blocks":2}` / 10:48:28 Nozomi `pit_filled [-252,67,-371] x2` / 10:49:13 `void_under_pad x12 {"job":"base_infill_n278_n401","n":4,"cells":"-253,66,-372 | -253,66,-371 | -251,66,-372 | -251,66,-371"}` + `declined x4 {"job":"base_infill_n278_n401","why":"build: complete - 4 void columns handed to void_fix_base_infill_n278_n401"}` — void_fix fills 2-3 blocks at y66-67, the pad re-activates, measures the SAME columns as void again and hands them back; 2 bots have produced nothing for 20 min (REPORT top problem 3, 980 m walked/15min). The fill stops one layer below the pad floor, so the pad's void test can never pass. | army_jobs.js fill_void/void_under_pad: the handover must fill the column up to the pad's own floor level (and the pad must re-measure with the same y range it hands over), else a cell that is "filled" by one job is "void" for the other.
- [ ] 10:52Z foreman: A NEW 32-COLUMN, 9-DEEP CRATER IN THE MUSTER YARD WHILE THE SPONGE REPORTS "NOTHING TO DO": BASE AUDIT 10:24Z "NEW since the last audit: -398,-370 -9 x32 (yard), -406,-370 -4 x8 (yard), -388,-375 +2 x16 (yard)" (286 off-level columns in total, 93 of them on FINISHED pads/roads), against 10:47:19 `tidy_pass x293 (41 bots) {"tile":[-392,-447],"found":0,"pillars":0,"holes":0,"floats":0,"strays":0}` and tidy_spawn `declined x85` with 6.2 of 6.5 bot-h idle. -398,-370 and -406,-370 are inside the sponge box (x -408..-230 / z -559..-369) and the job's own note says "the sponge now works the base audit work list first (tidy_fix events), tiles second", yet the crew is on clean tiles (Rena stood at -322,69,-442 on the ravine rim) and never on the measured holes. | army_jobs.js tidy: take the work units from the LAST base-audit report (hole/bump clusters with their coordinates and depth) before any tile walk, and decline only when that list is empty.
- [ ] 10:52Z foreman: TWO PENS ASK FOR 6 BOTS, GET 0, WHILE 13.7 BOT-HOURS STAND AT MUSTER: board 10:47 `herd_sheep | 0 want 5 of 5-8` and `herd_cows | 0 want 1 of 1-4` with 50/50 bots online and STANDING 63 % (32.4 of 51.7 bot-h, muster 13.7); both jobs carry `requires.minHp 14, minFood 10` while the pens are over their cap (`declined {"job":"herd_cows","why":"cow 24/10 in the pen"}` / `chicken 22/10`) so the surplus is never culled and the shearing stops. A job the dispatcher itself says it WANTS staffed must not sit at 0 for an hour while fit bots muster - either the requires gate (food, see the feed bug above) or the producer scaling is eating the assignment. | bots/manager.js dispatcher: report WHY a wanted job got 0 bots (no candidate passed requires / scaled to 0) in the board line, and let a pen over `want` count as output so it is staffed for the cull.
- [x] (opus 11:2xZ, army_jobs.js part: a solid fill never places into a mate's body, the 2 cells over a mate below grade or its last open side; a builder boxed in below grade rides its own filler up (`fill_ride_up`, 10x in the first minutes, the pillar IS the fill), 3 failed rides -> help + muster; flower cells: `locked` retries in 20-45 s, `no_los` walks up to the plant once, only a plant out of reach rests the column; a fill that would end with PINHOLES (open cell, 4 solid sides) reports them and stays active. Probe of all 50 bots twice: 0 at walkableArea <= 3) top model 11:2xZ, OWNER SAW IT FIRST ("渓谷埋めで埋まってるbotいっぱいいる / 花のある位置にブロックが置けず穴が空いている / 何故気が付けない？"): WHY BLIND: the ravine is a keep-out and keep-outs were never judged by base-audit; nothing watched a bot's own walkable area; I measured events (cells placed), not the floor. DONE: `audit_fill` in ops/base-audit.js (per fill_void job: open columns, deepest, PINHOLES with coordinates; first run: ravine n 596 open / 9 pinholes, m 665 / 12, s 1015 / 13). IN WORK (opus): builders never close a mate in + ride up when boxed in (army_jobs.js), flower cells retried instead of resting 10 min, sensor `entombed` + heartbeat `boxed` (army.js).
- [x] top model 11:0xZ: fill order: the LOWEST-cell-first rule sent every builder to unreachable shaft bottoms (`put:rest:no_los` x1277 / 10 min, 0.8 cells/min/bot) -> board `params.order:'near'` on the ravine jobs: fill_ravine_m 1.0 -> 14.7 cells/min/bot. Bedtime trips every 25 min (42 slept + 44 bed_skip per hour, ~10 % of bot time) -> BED_DUE_MS 6 h. Churn 1210 ASSIGN/h (24 per bot) -> opus on dispatcher.js.

- [x] (opus 11:40Z: LOOKED - the y32 branch IS a proper lit gallery (probe z-601, x-376..-326: all air over solid floor); what held them was the ZOMBIE 3 blocks above the line (-377,35,-604, d 5.4, dy exactly 3.0, LOS through the gap): `threat()` took it, `defend()` then stared its full 25 s per walkLine iteration, so every 99-174 s walk budget ran out standing still (`budget` x11 by 7 bots, both ways). iron_core: defend disengages after 3 s from a mob that never comes within 3.5 and ignores it for 2 min (`mine_mob_ignored`); fights and convoy holds no longer count against the WALK budget; toSurface's legs now scale with the route planUp measured (branch+trunk+stairs cells x 1.5 s) instead of a flat 90 s. 15 min after the hot-load: mine_blocked/hung/recall_failed on mine_iron 7+3+3+9 per 15 min -> 0, 21 banked, recalls 3/3 ok. The lane logic never saw cave air as an obstacle - laneHard only flags unharvestable SOLID cells) 11:15Z op: mine_iron level 32 recall fails: 5 bots (Shiori, Aoi, Yukina…) hung at -374,32,-601 (branch 46 blocks west of hub -328,32,-601), mine_blocked why=surface_budget/budget to=[-328,32,-601] x8, hung 11:07-11:12Z, look shows cave corridor, zombie at -377,35,-604 | recall on the new level 32 exhausts its budget before reaching the hub; check walkLine/budget for level 32 branches
- [x] 11:25Z foreman: THE FILL SPENDS MORE STONE ON ESCAPE PILLARS THAN ON THE RAVINE: in 80 s on fill_ravine_s (16 bots, the 11:2x ride-up code live) `fill_ride_up {"from":[-314,49,-485],"blocks":20}` (Botan) · Iroha 20 · Chika 20 · Hotaru 11 · `fill_climb_out {"from":[-328,52,-464],"at":[-328,52,-461],"blocks":17}` (Yukina) · Chino 18 · Karin 12 · Aoi `blocks:0` = ~138 filler blocks laid to get bots OUT of the hole, against `build_pass x8 {"done":4,"left":10309,"waiting":8514}` in the same window - 34 blocks of stone per cell of actual progress. The BASE AUDIT bills the other end of it: 90 stray placed blocks above y68, 86 of them cobbled_deepslate, with a 5-block column at -365,74,-553 | -365,74,-552 | -365,74,-546 (infill_n372_n555) standing above grade. "The pillar IS the fill" only holds BELOW y68; above grade the rider leaves a junk tower and the next audit calls it a stray. | army_jobs.js fill_void: keep the builder ON GRADE (line 240: place from the rim, hand out columns, never walk below grade into the void); if a ride-up is unavoidable, the rider must take its own column back down to y68 when it surfaces and report `fill_ride_down`, and the ride cost must be counted in build_pass so the ratio is visible. — opus 12:0xZ, two halves: (a) `wallsIn` refuses any cell one above a bot's own feet in the 8 columns around it (self included), and the pit ordering puts the builder's OWN layer within 8 blocks first, one step up second, deep cells last - fill_ravine_s went 0.2 -> 25 cells/min/bot, so the escape cost per CELL fell ~23x (0.35 -> 0.015 escapes/cell); (b) `fill_flush`: with nothing it can do, a fill builder sweeps the 2 layers over grade inside its own box and digs every run of <=2 of its own filler blocks with open sky over it. MEASURED: the three y69 lines at x -328/-324/-318, z -461..-420 (114 blocks, audit "82 strays") are gone, 0 solid blocks left at y69 in x -334..-300 / z -465..-415, and the 2 cells fill_ravine_n could never close were exactly the ones those lines stood on.
- [ ] 11:25Z foreman: DISPATCHER OVERFLOW DUMPS HALF THE ARMY ON A BUILD JOB THAT PLACES NOTHING: fill_ravine_m carried `bots:6` on the board and held **24 bots** (dispatcher.js:398-402 overflow, any SQUAD job with bots>=3 that is not `saturated`), while its own passes read `build_pass x185 {"done":0,"left":153,"gaveUp":119,"waiting":114}` and `stock` showed depot cobblestone **0** against 3298 carried, i.e. 24 bots stood in a lava/zombie ravine waiting for filler that does not exist (3 deaths on the site at 11:18:11, Erika `help_asked build_stuck` "20 cells left but I failed twice on each", helpdesk answer "Inventory is empty... none of cobblestone in the chest index"). The YIELD throttle (dispatcher.js:74, per<0.34) misses it because the crew BANKS the deepslate it digs out down there, so `outAt` looks healthy while `done` is 0. Board workaround 11:2xZ: bots 6->2, which is the ONLY lever that takes a job out of the overflow pool (the >=3 test). | dispatcher.js: overflow must respect the job's own head-count (or a job whose last N build_pass `done` sum to 0 must be closed to overflow), and the YIELD throttle must count the job's WORK output (build_pass.done / tidy_fix.blocks), not `banked`.
- [x] 11:25Z foreman: THE ARMY'S FILLER LIVES IN POCKETS, NOT IN THE DEPOT: `stock` = cobblestone depot **0** / carried 3298 (Aoi 711), diorite 0/991 (Akari 192), andesite 0/574, granite 78/838, gravel 16/182 - and Aoi is the miner REPORT lists as "stuck 0 m/5 min at -373,32,-600", i.e. her pockets are full of the exact block 24 builders were waiting for. `errors` backs it: `army:683 | Unable to withdraw, Bot inventory is full.` 7x / 42 per h, and Karin `obtain_failed {"item":"gravel","n":32,"why":"the depot index promised 33 gravel, the chests gave 0"}`. So the mine banks 2664 cobblestone an hour and the depot still reads 0. SHARPEST CASE, 11:23:40, new job light_ravine: Hinata `declined {"why":"light: no torches and no coal+sticks in stock"}` and 7 s later `banked {"job":"light_ravine","items":{"stick":8}}` - she was STANDING AT THE DEPOT while `stock` reads torch 71 + 386 carried, coal 628, charcoal 246: withdraw handed her nothing out of three different chests, so a 2-bot safety job died on arrival. | lib/army.js bank/withdraw: a bot over ~80 % of its slots must bank (or drop, cf. the dirt/seed glut rule) before it takes another work cell, and a miner holding >256 of a junk stone must deliver it on its way past the depot instead of carrying it down the next branch. — opus 12:5xZ: fixed with the 12:2xZ line below (A.offload: surplus rides home on any trip that passes the depot).
- [x] 11:25Z foreman: BUILDERS TRY TO **CRAFT** STONE VARIANTS OUT OF QUARTZ INSTEAD OF USING THE COBBLE THEY CARRY: 11:17:20 Riko `obtain_failed {"item":"diorite","n":192,"why":"got 0/192 missing {"cobblestone":192,"quartz":192}"}` · 11:17:26 Riko granite "got 88/192 missing {"diorite":104,"quartz":104}" · 11:17:40 Koharu granite "missing {"quartz":192}" · 11:17:18 Nagisa andesite "missing {"diorite":96,"cobblestone":96}" - the ravine jobs' `params.args.mats:["cobbled_deepslate","cobblestone","stone","deepslate","tuff",…]` is read as a shopping list and obtain walks the CRAFTING chain for decorative stone (quartz comes from the Nether; we have none and never will at this stage), burning a pass per bot. | lib/army.js obtain / army_jobs.js fill_void: a `mats` entry is an ACCEPTABLE substitute to be taken from stock or from the pocket, never something to craft - restrict the fill filler to items that exist in the depot index or in hand (line 250's "choose the cheapest solid the depot HAS" is the same family). — opus 11:43Z: a SOLID fill never goes through obtain(): `bestOfMats` picks the material with the biggest DEPOT pile and `A.withdraw` takes it straight from the chest index; nothing there = `build_blocked no filler` + a 3-min decline. Last quartz `obtain_failed` 11:56:40 (a slice that began before the reload), none in the 30 min since.
- [ ] 11:25Z foreman: THE FILL SITE IS UNLIT AND THE MOBS DO THE ARITHMETIC: 11:18:11 `death x3 {"job":"fill_ravine_m","pos":[-290,58,-472],"task":"army:build fill_void"}` (server log: Erika 11:17:55 and Kaede 11:18:11 "was slain by Zombie") on top of `deaths 30` = 9x "was shot by Skeleton" in 30 min, deaths/1h 24 with keep_inventory OFF - the ravine floor is 10-19 blocks below grade, roofed by its own walls, so it spawns hostiles all day while 16-24 builders work in it unarmoured. Nothing in the fill job lights or guards the site. Board stop-gap 11:2xZ: new job `light_ravine` (type light, box -334,-486..-289,-408, step 7, 2 bots). | army_jobs.js fill_void: a build site below grade must light itself (a torch every ~7 blocks on the floor it is filling, before the crew descends) or carry a `guard` escort in the same way a night job does - a job that plants 20 bots in a dark hole and files their deaths as weather is a planning failure, not bad luck.
- [x] top model 11:3xZ: steps verb `craft` with n > 2 of an UNSTACKABLE item stops at 2 (`2/32 crafted (ingredients ran out?)` with 90 cobblestone + 28 sticks in the pockets, Yui 11:32Z): the craft loop counts by stack; work-around on the board = n:2 per step. Also: tools are now part of kitUp (army.js) - the quartermaster must keep the shelf (CRAFTED stone_pickaxe, asked of the army_jobs.js owner). — opus 12:5xZ: craft.js craftOnTable now crafts the WHOLE batch in ONE open window, counts the result as a total over all slots, checks a free slot for an unstackable result and waits for the server per run; the real cause was a click whose `updateSlot` confirmation Paper never sends (`craft:click not confirmed` in `armyctl.js errors`) - it aborted the batch, now the result count decides. Field: 206 short-batch reports in the 3 h before, 0 in the 25 min after.
- [ ] 11:38Z helpdesk (Hinata, hung): fill_ravine_s hangs at -317,51,-466: walled by X on N/E/S in a cave, and fill_void has no reachable target or timeout. Add a stuck-timeout and a path-fail fallback. | ticket: no movement for 3 min on job fill_ravine_s, task "army:build fill_void" @-317,51,-466
- [x] 11:46Z helpdesk (Chino, build_stuck): fill_ravine_m fails on all 76 cells (first -318,68,-440) despite 114 cobble. Bot at -318,69,-436 is ringed by '^' tiles, a glyph not in the look legend. Check whether '^' is bushes or a hazard blocking the goto/place standing spot, and log the placement error. | ticket: 76 cells of fill_ravine_m left but I failed twice on each (first: -318,68,-440) @-318,69,-436 — opus 12:0xZ: same cause, -318,68,-440 had one of the y69 filler blocks standing ON it; `fill_flush` takes them off (the '^' glyph is terrain above the bot's feet).
- [ ] 11:53Z foreman: A BUILD JOB HOLDS A CREW IT CANNOT SUPPLY - this is the 42% idle: `stock` = depot cobblestone **0**, dirt **0**, granite **0**, diorite **0**, cobbled_deepslate **3** (all of it in pockets: Kanade 576 cobble, Nozomi 412 dirt, Botan 392 diorite), yet fill_ravine_s ran with 14 bots and reports `build_pass {"done":14,"left":6550,"gaveUp":5585,"waiting":5583}` x184 by 36 bots, `build_blocked x14 {"why":"no filler carried or in stock"}` + `declined x20 {"why":"build: no filler"}` in ONE minute, base_yard_pad the same with `{"why":"no dirt carried or in stock"}`, and REPORT counts obtain_failed x66/30min by 28 bots + travel_fail x41 (timeout @-311,?,-447 x7 = the walk to the empty chests). A 6550-cell void cannot be filled by a depot that holds nothing | army_jobs.js build/fill_void: before a pass claims a crew, compare the cells left against the filler the depot index ACTUALLY yields (a withdraw that returns 0 must mark the index entry empty) and decline the surplus bots instead of walking them to empty chests - a job whose material is 0 must shrink to the bots that carry filler. Board stop-gap 11:52Z: fill_ravine_s 14->8 bots, mine_iron 11->14 to refill the stone.
- [ ] 11:53Z foreman: THE SEED PILE IS PASSED FROM BOT TO BOT AT THE DEPOT DOOR (the 00:58Z glut fix does not hold): 32 `junk_tossed` events in the hour, ALL wheat_seeds, ALL on a 3-block patch beside the depot -333,69,-480..-482, and the SAME stack travels - Botan n:1472 @11:48:39, Iroha n:1472 @11:50:04, Sayaka n:1472 @11:50:24, Kurumi n:1546 @11:44:03 - i.e. each bot drops it, the next picks it up on its chest trip and drops it again; `stock wheat_seeds` depot 844 / **carried 3078** (Iroha 1472) proves the loop. Same minute: `chest_full x9 {"cat":"food","left":["wheat"]}` with wheat **18944** banked against a food target of 448 | lib/army.js bank glut rule: a dropped surplus item must be blacklisted for pickup (or composted), never re-collected - and a FULL category chest must throttle its producers (4 farm jobs still till and replant into a chest that takes nothing). Board stop-gap 11:52Z: bake_bread re-activated to turn the 18944 wheat into bread.
- [ ] 11:53Z foreman: THE FALLBACK SPONGE DECLINES WHILE THE AUDIT HOLDS ITS WORK LIST (line 145 still open, now with the measurement beside it): 11:50:05 `tidy_pass x289` by 35 bots reports `{"found":0,"pillars":0,"holes":0,"floats":0,"strays":0}` and 11:50:08 `declined x45 {"job":"tidy_spawn","why":"tidy: the audit lists no open work in reach and every tile was tidied in the last..."}` - at the SAME minute `ops/base-audit.js` measured 274 off-level columns in 34 hole + 22 bump clusters (worst -243,-556 +13 x59, -398,-370 -9 x26, -232,-377 -4 x20), 82 stray blocks (yard x79, first -318,69,-461 cobbled_deepslate) and a float at -334,-369 y80, each with coordinates. tidy_spawn holds 14 bots, GEMBA counts 18 of 50 bots standing (10 at muster, 20 min) | army_jobs.js tidy: the sponge must read its units from the base-audit output (REPORT `BASE AUDIT` / `events audit`) instead of its own local scan that finds nothing anywhere, and "tidied in the last N min" must not mask a tile the camera still measures as off-level.
- [ ] 11:53Z foreman: `mine_level_fallback` SENDS THE IRON MINER TO y0 AND THE DELEGATE THEN DOES NOTHING: Hina 11:33:37 `mine_level_fallback {"job":"mine_iron","from":32,"to":0,"why":"iron wanted"}` -> `bot Hina` = pos **-309,0,-678**, hp20 food20, task "iron:branch 56:1", standing 20 min (GEMBA), and `job_slice` returns the IDENTICAL delegate result at 11:36:51, 11:51:52 and 11:52:22 (`{"ended":true,"stats":{"rawDelivered":4,"ironPicks":3,"dug":648,"advance":291}}` - frozen numbers, `ended:true` on every slice). y0 is the diamond/redstone band; iron peaks at ~y16 (docs/PLAYBOOK.md), so "iron wanted" must never step DOWN from y32 | army_jobs.js mine delegate: the fallback must move toward the ore's own band (iron -> y16, up from y32, not down to y0), and a delegate that returns `ended:true` with unchanged stats twice in a row must release the bot instead of re-slicing it forever.
- [x] 12:0xZ review row 5: the tidy sponge declined `no open work in reach ... every tile tidied in the last 30 min` x73 one minute after a fresh audit listed 274 off-level columns (the muster<->sponge ping-pong) | army_jobs.js tidy — opus 12:0xZ: with a FRESH `auditWork()` the 30-min per-tile memo is ignored, the whole `params.box` is searched, tiles holding measured columns come first and a 10-min claim is all that keeps two bots off one tile. tidy_pass 6/10 min -> 101/2 min, tidy_fix 3.2 -> 4.4 per min.
- [x] 12:0xZ review row 6: `farm` stood 90 s at "waiting for growth" with food 9002 against a target of 448 | army_jobs.js farm — opus 12:0xZ: nothing ripe, nothing to plant AND `stock.have('food') >= targets.food` -> decline 10 min + muster; below the target the wait stays (it is the right use of the bot).
- [x] 12:0xZ review row 3: CRAFTED was a stone-age table and the only automatic tool producer | army_jobs.js — opus 12:0xZ: + iron_pickaxe 4, diamond_pickaxe/shovel/axe/sword 4, bucket 2, shears 4, bow 2, arrow 32, shield 2; torch 64->256, planks 64->256, stick 32->128; a precious batch needs a SURPLUS (`CRAFT_RESERVE` diamond 64, iron_ingot 32). NOTE: a key only becomes a candidate when `settings.targets` names it - the board still has no iron_/diamond_ tool targets.
- [x] 12:1xZ owner "適当にブロック使うから見栄えが悪い" (`audit_surface`: 705 visible planned cells hold a substitute, road_12 199 / road_9 189 / road_11 80 / road_1 70) | army_jobs.js build — opus 12:2xZ: a cell that is the TOP block of its column in the blueprint and is neither fill body nor sub-base is VISIBLE: only the blueprint's own block is accepted there and only that block is placed; a substitute standing there is work again (dig + place) and with none in the depot the cell WAITS with a `needs:` note. Wood species and bed colours keep their families, soil cells keep theirs (the dirt cap is what makes ground ground). base_road_12 + base_road_9 restored from the archive to swap their 388 cells. Verified on the blueprints: base_field_7 122 EXACT cells, base_dorm 0.
- [x] 12:2xZ top model: THE ARMY'S FILLER AND SOIL LIVE IN POCKETS, NOT IN THE DEPOT (second instance, cf. 11:25Z): `stock` 12:08Z = cobblestone depot **0** / carried 926, dirt depot **0** / carried 582 - the ravine fill, both restored road jobs, base_yard_pad and every dirt cap are blocked on material 50 bots are carrying around. Every terrain job in the base is now supply-bound, not motion-bound. | lib/army.js bank/withdraw (a bot over ~80 % of its slots banks before it takes another work cell; a miner holding >256 of a junk stone delivers on its way past the depot) - not army_jobs.js — opus 12:5xZ: army.js `offload()` called from travel(): material above a personal 16 that the CURRENT job does not consume (build/deck/tidy/light/steps/delegate/haul/scan/portal keep their load, their keep-lists decide) goes back to the depot on any trip that passes a build chest (<=24 blocks, <=96 when overdue or after a job change), max one visit per 3 min, never underground. Measured 30 min: 14 visits by 13 bots, 1839 blocks banked, `build: no filler|no dirt|no cobblestone` declines 72 -> 23 per 10 min.
- [x] 12:2xZ top model: A BUMP THE CAMERA SEES AND THE BUILDERS DO NOT: base audit 11:37Z and 12:04Z both name `-243,-556 +13 x59 (infill_n252_n558)` as the WORST off-level cluster in the base; I re-activated the tile (its `level` args are w21 d31 clear14, so y69..y82 ARE cells of the job) and 4 builders reported `declined {"job":"base_infill_n252_n558","why":"build: complete"}` within a minute - todo() found nothing at all there. Suspect `bot.blockAt` returning null for a chunk the builder has not loaded (`if (!b) continue` silently drops the cell) while the spectator camera reads it fine. | army_jobs.js build todo(): a cell whose block cannot be READ is not a finished cell - count it as `wait` (and load/approach the column) instead of dropping it, else a build job declares itself complete over ground nobody looked at — opus 12:5xZ, MEASURED ON THE SPOT first (eval probe on Yotsuba @-262,69,-558 and `ops/skyshot.js`, the audit's own camera): TWO different causes, not one. (1) base_infill_n252_n558 is genuinely finished - camera and bot both read y68 over the whole tile, the +13 bump stands at x -263..-255, twelve to twenty blocks WEST of its box: the audit names the NEAREST job for a cluster OUTSIDE its footprint. (2) base_infill_n232_n400 had a real hole: col -232,-377 read `oak_log` AT grade, and `if (c.fillOnly && solid(b)) continue` accepted a TRUNK as ground, so a 4-deep pit with a tree in it looked filled. Fixes in army_jobs.js: `GROUND_TREE_RE` (a log, stem, leaves, mushroom block, cane or sapling in a ground cell is work - the main loop fells the whole tree, `level` being a TERRAIN_JOB), and a terrain job that reaches `left:0` now reports `level_skipped {col, d, why}` for every camera column inside its OWN box (max 5) or once with `none:true` - never a silent "complete". VERIFIED LIVE: 12:37:50 `level_skipped col [-232,-377] d -4 "the world reads oak_log at grade y68"`, 12:48:01 `tree_cleared at [-232,68,-377] logs 3` + `build_pass {done:1,left:4}` where the same job had reported left:0 all morning; base_infill_n252_n558 printed `level_skipped none:true box [-252,-558,-232,-528]`.
- [ ] 12:15Z op: base_portal_go kills/hangs scouts in the Nether: Chino/Hotaru died 11:59/12:02 (nether_shell placed 46/3, unsafe:true lava 1-4), Koharu HUNG 'standing in the far gate' -37,98,-76 then dug/pillared out to -28,121,-78 (nether_wastes, boxed in, area 2), tidy travel_fail d461 from the Nether | portal job must not send bots through an unsafe:true gate (lava at arrival) and a bot left in the Nether needs a way home; job paused 12:15Z
- [ ] 12:22Z op: WORKER ERRORS in army_jobs.js build: Chika 'TypeError: Cannot read properties of undefined (reading x)' at K (army_jobs.js:2649:20) and Himari 'ReferenceError: Cannot access putCap before initialization' at build (army_jobs.js:3323) (TDZ); also base_yard_pad level: Rin -366,67,-488 / Botan -360,69,-506 HUNG (put:resting x8, left 32) | army_jobs.js build: fix the undefined cell in K and move putCap declaration before first use
- [ ] 12:25Z op: fill_yard_sw build_stuck 8 cells (-396,65,-370 | -396,65,-371) all 'sealed' gaveUp 8, plus Chika error TypeError reading 'x' at army_jobs.js:2649 (K) 12:19:48 -> job paused by me; sealed pockets (same class as line 91) not fillable, check the undefined-position crash
- [ ] 12:3xZ foreman: THE SPONGE NOW REACHES THE MEASURED CLUSTERS AND REFUSES TO TOUCH THEM - `kept:` is why BASE NOT FLAT never falls (252 columns now, 278 at the 12:04Z audit, 274 at 11:50Z): after the 12:0xZ audit-queue fix the tidy units DO land on the audit's coordinates, but every bump unit returns `n:0 of:N blocks:0` - 12:15:58 Koharu `tidy_fix {"kind":"bump","at":[-381,-374],"of":10,"skip":{"kept:coal_ore":9,"kept:grass_block":1}}`, 12:17:09 Himari `at:[-382,-379] of:4 skip:{"kept:grass_block":1,"kept:copper_ore":1,"kept:coal_ore":2}`, 12:09 Kaede `at:[-407,-382] of:1 skip:{"kept:grass_block":1}`, 12:19:40 Misaki `at:[-388,-381] of:2 skip:{"kept:copper_ore":1,"kept:grass_block":1}`; hole units return `skip:{"occupied":N}` (12:07 Aika -407,-519 of:2, 12:08 Akari -408,-553 of:2, 12:19 Karin -407,-454). On a plains base EVERY natural bump is capped with grass_block, so a bump unit can never be cut and 42 bots produced 14 tidy_pass units of output in an hour (1.8 of 2 bot-h idle) | army_jobs.js tidy: above grade the cap block IS the work - grass/dirt is spoil and exposed ore is loot to bank, `kept:` must protect blueprint cells, furniture and crops only; a hole cell whose "occupied" block is not a planned block is work too, not a skip
- [x] (opus 13:15Z: not the material picker - THE HAND IS SHARED. placeAt always equipped a block; mineflayer names `bot.heldItem` AT THE THROW, and army.js' melee reflex had swapped the sword in between (same pid also logged `must be holding an item to place`). iron_core now has ONE guard `inHand()`: equip the block, verify `bot.heldItem` is it, one 150 ms retry, else no click at all - used by placeAt (again before every face), placeTorch, torchNear, placeTread (also after the 200 ms look) and pillarOne (after the 1 s jump window); no filler carried -> false, the caller reports no_filler. Exercised live: `stair_repaired` 6 cells verified:true 12:50Z, stairRepairs 20 / walls 44 / plugs 89 / torches 888 in 20 min, and no weapon or tool name in any place error since. A.placeHard was NOT routed through: it travels, obtains and files tickets (7 rounds) - not a two-line change and wrong underground, where movement is graph-only) 12:3xZ foreman: A BOT TRIES TO PLACE ITS SWORD INTO THE WORLD: `errors 8` LIVE signature `iron_core:placeAt | Server refused to place diamond_sword at (N, N, N): the block is still air` 6x = 7/h, last 16m ago (distinct from the torch/rail/air signatures of lines 186/197/262 - here the ITEM is not a block at all); the army carries diamond_sword:47 and every miner holds one | iron_core/lib/army.js placeAt: the material picker must filter its candidates to placeable BLOCKS (a tool/weapon/food in the hotbar is never filler) - one server hiccup away from a bot throwing its only weapon into a wall
- [x] 12:3xZ foreman: TRAVEL STORM NOBODY RESOLVES AT ONE COORDINATE: REPORT "WARNING STORM: travel_fail x57/30min (33 bots), most: timeout/stop @-240,?,-381 x6" - that is the infill strip x -232 / z -400..-370 whose 20 columns the camera measures 4 blocks BELOW grade (base_infill_n232_n400, auto-paused "build complete"; re-opened with depth 8 by this round), i.e. two thirds of the army walks into a dent it cannot climb out of | lib/army.js travel: a hop that ends "timeout/stop" at the same column for N different bots must be reported ONCE with the column and the job that sent them there, not 57 times - and a dent on the route is a job, not a retry — opus 12:5xZ: -240,-381 is the walk to base_infill_n232_n400, which declared itself complete over a tree-filled hole every pass (see the two lines above); the tile has real work again since 12:48Z and no travel_fail at that coordinate in the 10 min after.
- [x] 12:3xZ foreman (evidence FOR the 12:2xZ top-model line above, second tile + one hypothesis killed): `level` todo() returns an EMPTY cell list on ground the camera measures as off-level, and `depth` is NOT the cause (line 251's theory): I re-opened base_infill_n232_n400 with `args.depth 8` and a rev bump (its 1-wide strip x -232 / z -400..-370 holds the audit's -232,-377 -4 x20) and base_infill_n252_n558 (-243,-556 +13 x59) - both reported `build_pass {"done":0,"left":0,"gaveUp":0}` + `build_done` + `declined "build: complete"` within 90 s (12:24:09 n252 7 bots, 12:25:43 n232 3 bots) and auto-paused themselves again. left:0 with gaveUp:0 means the cells were never GENERATED, so no depth, mats or crew change on the board can ever reach this ground - every BASE NOT FLAT cluster owned by a `level` job is unfixable from the board until todo() is fixed | army_jobs.js build todo() for blueprint `level` — opus 12:5xZ, MEASURED ON THE SPOT first (eval probe on Yotsuba @-262,69,-558 and `ops/skyshot.js`, the audit's own camera): TWO different causes, not one. (1) base_infill_n252_n558 is genuinely finished - camera and bot both read y68 over the whole tile, the +13 bump stands at x -263..-255, twelve to twenty blocks WEST of its box: the audit names the NEAREST job for a cluster OUTSIDE its footprint. (2) base_infill_n232_n400 had a real hole: col -232,-377 read `oak_log` AT grade, and `if (c.fillOnly && solid(b)) continue` accepted a TRUNK as ground, so a 4-deep pit with a tree in it looked filled. Fixes in army_jobs.js: `GROUND_TREE_RE` (a log, stem, leaves, mushroom block, cane or sapling in a ground cell is work - the main loop fells the whole tree, `level` being a TERRAIN_JOB), and a terrain job that reaches `left:0` now reports `level_skipped {col, d, why}` for every camera column inside its OWN box (max 5) or once with `none:true` - never a silent "complete". VERIFIED LIVE: 12:37:50 `level_skipped col [-232,-377] d -4 "the world reads oak_log at grade y68"`, 12:48:01 `tree_cleared at [-232,68,-377] logs 3` + `build_pass {done:1,left:4}` where the same job had reported left:0 all morning; base_infill_n252_n558 printed `level_skipped none:true box [-252,-558,-232,-528]`.
- [ ] 12:33Z helpdesk (Kurumi, no_route): Kurumi at -350,69,-499 is in a 120-block walkable region that excludes the depot table at -345,69,-523. The look map (z -506 to -492) shows no cause. Only the '^' row at z=-492 is unexplained, with a gap at x=-344. Code owner: find the barrier between z=-506 and -523 and add a path or bridge. Add '^ | ticket: cannot path to -345,69,-523 (walkable area 120) @-350,69,-499
- [ ] 12:42Z helpdesk (Tsumugi, entombed): fill_ravine_s at -300,67,-462: bot stands on a 1-cell ledge (walkableArea 1) with 6-9 block drops on every side and has 127 cobbled_deepslate. This is not a real entomb. The below-grade check misfires and the pillar/staircase escape doesn't fire. Have the job place fill blocks itself instead of walk | ticket: boxed in below grade inside fill_ravine_s at -300,67,-462 (walkableArea 1) @-300,67,-462
- [ ] 12:55Z foreman: REGRESSION of line 247 (growLevel "done" 10:45Z): the mine kept 11 bots (22% of the army) on an EXHAUSTED working level and opened nothing — `armyctl.js mine` = "working level y 32 ... EXHAUSTED (160 branches x 256, none open) <- working" with y16 and y-16 EXHAUSTED too, while Aoi stood 40 min at -309,0,-678 ("iron:branch 56:1", hp20/f17) and her slice returned the IDENTICAL `job_slice {"r":"delegate iron_miner: {\"ended\":true,\"stats\":{...\"dug\":1892,\"advance\":849}}"}` at 12:37:30 AND 12:52:32, GEMBA "8x mine_iron produced nothing in 10 min", 2 stuck (Fuuka -406,-32,-525 0.8m/5min) — iron 99/224 all the while | iron_miner growLevel/pickLevel: a slice that ends with rawDelivered 0 on a level with 0 open branches must open the next landing or report `mine_exhausted` and RELEASE the head-count, never re-run the same ended slice for 15 min. AND the board cannot steer around it: 2 min after `mine level -32` (the only level with open branches, 30) the squad answered `mine_level_fallback x5 {"from":-32,"to":0,"why":"iron wanted"}` 12:55:20 and went back to y0 — which has 6 open branches for 11 miners, so pickLevel's "iron-rich first" rule outranks both the operator's level AND the ledger's open-branch count. Board stop-gap 12:56Z: mine_iron maxBots 16 -> 6 (= the open branches at y0)
- [ ] 12:55Z foreman: THE PORTAL AUDIT CAN NEVER BE CLEARED because the portal job only sees its 6 gate cells: `audit_structure {"job":"base_portal","nether_portal":"6 cells MISSING of 124 (cobblestone@-330,69,-518=air, cobblestone@-329,69,-518=air, cobblestone@-329,70,-518=air)"}` 12:35:00, but the owning job answers `declined {"job":"base_portal_go","why":"portal: the gate stands and burns (6/6 cells)"}` 12:31:05 — the registered structure is the 124-cell cobblestone shell, the handler checks only the obsidian frame, so re-activating the job (the documented repair) is a no-op and the alert returns every audit | army_jobs.js portal: repair every registered cell of its structure (shell + frame) before declining, or register only the cells it actually maintains
- [ ] 12:55Z foreman: A JOB WITH AN IMPOSSIBLE CONFIG HOLDS A SQUAD INSTEAD OF DECLINING: nether_hub (pri 95) kept 2 bots for 20+ min walking 1097 m/15min at ZERO output, both repeating `portal_scout_missing {"job":"nether_hub","why":"params.go needs exactly ONE pinned bot: set job.names:[\"<bot>\"]"}` (Kanade 12:51:22, Iroha 12:51:32) — the handler diagnoses its own misconfiguration perfectly and then takes the bots again on the next tick | army_jobs.js portal: a config error the board must fix = decline ALL bots + pause the job once with the reason (same class as a `build` that reports `left:0`); the dispatcher must not re-staff a job that declined every bot for a config reason. Board fix 12:54Z: `names:["Kanade"], bots:1`
- [ ] 12:55Z foreman: fill_ravine_s ASKED FOR 20 BOTS TO WORK 34 CELLS: `build_pass {"job":"fill_ravine_s","blueprint":"fill_void","done":34,"left":3915,"gaveUp":3112,"waiting":3105,"sealed":891}` 12:52:39 = 80% of the todo is unreachable/waiting, and the bots that do get below grade cannot get out by legal movement — 12:52:07 Suzu `hung {"task":"army:build fill_void","at":[-305,56,-471]}`, 12:52:20 `pillared_out {"from":[-306,57,-472],"blocks":4,"free":false}`, 12:52:34 `dug_out {"from":[-306,57,-472],"to":[-303,61,-472],"sky":true}` (movement is read-only: that is a 4-block junk pillar plus a staircase cut into a pad we are levelling) | army_jobs.js build fill_void: cells that can only be placed from a standing spot inside the void must not be counted as todo (they are what makes `waiting`/`gaveUp` 6200), and head-count must follow OPEN cells, not the raw box. Board stop-gap 12:53Z: bots 12 -> 6
- [ ] (top model 13:0xZ, board only: herd_chickens/herd_sheep minBots 1 while animals stand outside. OPEN, dispatcher: a herd job's demand must also count the audit's `outside` heads - `audit_pen` in base_audit.json - not only the item target) 12:55Z foreman: PEN UPKEEP IS GATED ON AN ITEM TARGET, SO THE HERD WALKS OUT AND NOBODY FETCHES IT: `audit_pen herd_chickens` = 14 inside, 8 OUTSIDE within 96 blocks (20 inside at the last audit), while the board shows `herd_chickens | 0 want 0 of 0-2 (deficit 0.00 of feather 384/64)` — feather stock is full, so the scaler staffs zero herders and the pen quietly empties; the same holds for herd_sheep (wool 8602/150) and herd_cows (leather 191/64) over 62 animals | dispatcher/targets: a `herd` job owns a PEN, not just an item — keep >=1 herder whenever its pen audit shows animals outside or a fence cell missing, independent of `produces`. Board stop-gap 12:54Z: herd_chickens minBots 1
- [ ] 13:05Z op: base_infill_edge_n259 (level) HUNG Himari -271,69,-559 / Hinata -266,69,-557; build_pass done:0 left:186 gaveUp:3 fails dig:locked x2 (-269,75,-559 = 6 above feet), dig:unreachable, dig:no_los | army_jobs.js build level: cells 6+ blocks above the pad are dig:locked/no_los for every bot; skip them or cut from the side/top instead of hanging
- [ ] 13:19Z helpdesk (Chika, hung): Job base_infill_edge_n259 (task army:build level) stalls at -272,69,-559: flat to the south, but cliff 5-8 blocks higher to the north (z<=-560). Add an unreachable-target skip/timeout. | ticket: no movement for 3 min on job base_infill_edge_n259, task "army:build level" @-272,69,-559
- [ ] 13:23Z foreman: THE FALLBACK SPONGE CANNOT ABSORB THE ARMY, SO 24 BOTS PARK AT MUSTER: after raising `tidy_spawn` 2 -> 16 bots the overflow bounces straight back — Botan at muster (-358,69,-481) reports `army:muster (tidy: every tile of the box is claimed by a mate right now (the audit list is fresh: back in 2 min))` 13:21Z, and `tidy_pass x1137 {"job":"tidy_spawn","tile":[-312,-383],"found":0,"pillars":0,"holes":0,"floats":0,"strays":0}` 13:17:01 — while the SAME minute the base audit counts 235 off-level columns in 34 hole + 21 bump clusters and 2 floating tree blocks inside the very box the sponge scans (x -408..-230 / z -559..-369). Report: 63 % of bot time produced nothing (30.8 of 48.6 bot-h) | army_jobs.js tidy: the sponge's work list is built from its own scan and is ~1 tile per bot; feed it the BASE AUDIT cells (ops/base-audit.js off-level clusters + floats + strays, with coordinates) and split tiles finer, so `bots` can scale to 数の暴力 instead of every extra bot declining
- [ ] 13:23Z foreman: `fill_void` CALLS A DECKED HOLE FINISHED AND THEN DECLINES EVERY BOT: `build_pass {"job":"fill_ravine_n","blueprint":"fill_void","done":0,"left":0,"sealed":944,"pinholes":64}` + `void_under_pad {"n":944,"cells":"-334,57,-423 | -334,58,-423 | -334,56,-422 …"}` + `declined x18 {"why":"build: 64 pinholes left in the floor"}` 13:16:02 — i.e. the north ravine is ROOFED, not filled: a 944-cell cavern sits under base level y68 at x -334..-330 / z -425..-408 (mob spawns under the base, a floor that can collapse), the job reported `left:0` (= complete) and still held 10 bot slots declining all of them for an hour. The runbook's own rule is "never deck a hole" | army_jobs.js build fill_void: a pass that leaves `sealed`/`void_under_pad` cells must not report `left:0`; fill bottom-up from a legal standing spot or refuse the box and say so once. Board 13:20Z: fill_ravine_n + fill_ravine_s paused (fill_ravine_s: done 0, left 3913, gaveUp 3107, sealed 921, ETA 163 h)
- [ ] 13:23Z foreman: THE MUSTER YARD ITSELF CANNOT BE LEVELLED — HOLES 2+ BELOW THE PAD ARE UNREACHABLE: `base_yard_pad` (active, blueprint level, zone yard x -370..-340 / z -490..-476) `build_pass x104 {"done":0,"left":32,"fails":{"put:unreachable":1,"last":"-367,66,-480","put:resting":7}}` 13:12:09 with 6 bots, nothing banked in 30 min, and the base audit still lists `-367,-482 -3 x11` on this job's own pad | army_jobs.js build level/fill: a cell whose floor is 2+ below the pad must be filled from INSIDE the hole (walk down the existing slope) or the cell must be skipped and reported, not retried for ever — same reach defect as fill_void (13:23Z line above) and the ridge cut (13:05Z op line)
- [ ] 13:23Z foreman: THE TIDY SPONGE SKIPS EXACTLY THE HOLES IT IS SENT TO: `tidy_fix x7 {"job":"tidy_spawn","kind":"hole","at":[-235,-389],"n":0,"of":13,"blocks":0,"already":0,"skip":{"occupied":11,"tree":2}}` 13:16:29 and `place_failed {"at":[-237,65,-375],"item":"dirt","why":"occupied by oak_log and cannot dig it (unreachable)","tried":["clear"]}` 13:16:30 — 11 of 13 hole cells were "occupied" (leaf/log litter of a felled tree), so the cluster returns at every audit (`-234,-390 -4 x11`, `-233,-377 -4 x9`) | army_jobs.js tidy: a hole cell blocked by log/leaves = clear it (dig the log, it is not terrain) or hand the tile to a lumber job; "occupied" must not count as done
- [ ] 13:23Z foreman: THE CANTEEN WALK NEVER ARRIVES WHILE THE DEPOT IS FULL: `canteen_unreached x3 {"food":6,"hp":20}` Tsumugi 13:16:33, Himari x3 {"food":5} 13:16:39, Botan x4 {"food":6} 13:16:41 — all three at muster, 20 blocks from the depot, with `stock` = bread 1536 depot + 151 carried and 17923 wheat; `errors` = 8x `feed:eat | Promise timed out.` (48/h). 6 bots at food<=6 = no sprint, no regen | lib/army.js canteen: report WHY it is unreached (no path / chest busy / slice ended) and retry from a nearer chest; related to the 10:52Z line (eater cancels itself)
- [ ] top model 13:2xZ, OWNER SAW IT FIRST ("道路Erikaバグってる"): base_road_9 DUG ITSELF INTO A TRENCH - 3138 cells "done" 12:09-13:19Z by 30 bots with `left` constant at 2; strip x -288..-286 / z -486..-468 is 1-9+ deep. Cause (to be confirmed by the terrain engineer): the visible-material swap treats the top block of a column as visible, digs the substitute, cannot place cobblestone (depot 0-200) and the block below becomes the next "visible" cell. All road jobs PAUSED; engineer ordered: swap only blueprint cells with the block in hand, `build_runaway` hard stop, fill + re-pave. WHY BLIND: every yardstick read 3138 cells/h as a healthy fast job -> gemba flag RUNAWAY (cells done >= 5x left while left does not fall) now tops the GEMBA block and says PAUSE FIRST.

- [ ] 13:40Z op: fill_road9_trench (fill_void) left 63, done 0, gaveUp 55, waiting 51: 'boxed in inside the fill' declines + Kotori hung 3 min in 'riding the fill up' at -286,68,-474 (fill_ride_up blocks:2 repeated) | tail cells sealed/waiting - job needs a finish rule (count sealed as done) or fewer bots; nether_fortress killed 6 bots by lava swim 13:36-13:37Z (job now paused, bots recalled to west_terrace_cut_2) - fortress route crosses lava
